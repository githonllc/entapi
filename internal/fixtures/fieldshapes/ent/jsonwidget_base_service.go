// Code generated by entdomain extension from schema "JSONWidget" (entschema/schema/jsonwidget.go). DO NOT EDIT.
// Source template: backend/pkg/entdomain/templates/base_service.tmpl
// Regenerate with: make generate

package ent

import (
	"context"
	"fmt"

	"github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/jsonwidget"
	"github.com/google/uuid"
)

// BaseJSONWidgetServiceHooks defines hook extension points for JSONWidget CRUD operations.
// Implement this interface in your service struct and call SetSelf to enable hooks.
type BaseJSONWidgetServiceHooks interface {
	BeforeCreate(ctx context.Context, req *JSONWidgetCreateRequest) error
	AfterCreate(ctx context.Context, entity *JSONWidget) (*JSONWidget, error)
	BeforeUpdate(ctx context.Context, id uuid.UUID, req *JSONWidgetUpdateRequest) error
	AfterUpdate(ctx context.Context, entity *JSONWidget) (*JSONWidget, error)
	BeforeDelete(ctx context.Context, id uuid.UUID) error
	AfterDelete(ctx context.Context, id uuid.UUID) error
}

// BaseJSONWidgetService provides CRUD operations for JSONWidget with Before/After hooks.
// Embed this in your service struct and call SetSelf to enable hook overrides.
//
// Example:
//
//	type myJSONWidgetService struct {
//	    ent.BaseJSONWidgetService
//	}
//
//	func NewMyJSONWidgetService(db *ent.Client) *myJSONWidgetService {
//	    s := &myJSONWidgetService{
//	        BaseJSONWidgetService: ent.BaseJSONWidgetService{DB: db},
//	    }
//	    s.SetSelf(s) // enable hook dispatch to this struct
//	    return s
//	}
//
//	func (s *myJSONWidgetService) BeforeCreate(ctx context.Context, req *JSONWidgetCreateRequest) error {
//	    // custom validation
//	    return nil
//	}
type BaseJSONWidgetService struct {
	DB   *Client
	self BaseJSONWidgetServiceHooks
}

// SetSelf sets the hook receiver. Call this with your embedding struct
// so that hook dispatch goes through your overrides instead of the no-op defaults.
func (s *BaseJSONWidgetService) SetSelf(hooks BaseJSONWidgetServiceHooks) {
	s.self = hooks
}

// hooks returns the hook receiver, falling back to self (no-op defaults) if SetSelf was not called.
func (s *BaseJSONWidgetService) hooks() BaseJSONWidgetServiceHooks {
	if s.self != nil {
		return s.self
	}
	return s
}

// ---------------------------------------------------------------------------
// Default no-op hook implementations
// ---------------------------------------------------------------------------

func (s *BaseJSONWidgetService) BeforeCreate(_ context.Context, _ *JSONWidgetCreateRequest) error {
	return nil
}

func (s *BaseJSONWidgetService) AfterCreate(_ context.Context, entity *JSONWidget) (*JSONWidget, error) {
	return entity, nil
}

func (s *BaseJSONWidgetService) BeforeUpdate(_ context.Context, _ uuid.UUID, _ *JSONWidgetUpdateRequest) error {
	return nil
}

func (s *BaseJSONWidgetService) AfterUpdate(_ context.Context, entity *JSONWidget) (*JSONWidget, error) {
	return entity, nil
}

func (s *BaseJSONWidgetService) BeforeDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

func (s *BaseJSONWidgetService) AfterDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

// ---------------------------------------------------------------------------
// CRUD methods
// ---------------------------------------------------------------------------

// GetByID retrieves a JSONWidget by ID.
func (s *BaseJSONWidgetService) GetByID(ctx context.Context, id uuid.UUID) (*JSONWidget, error) {
	return s.DB.JSONWidget.Get(ctx, id)
}

// Create creates a new JSONWidget from a CreateRequest.
func (s *BaseJSONWidgetService) Create(ctx context.Context, req *JSONWidgetCreateRequest) (*JSONWidget, error) {
	if err := s.hooks().BeforeCreate(ctx, req); err != nil {
		return nil, err
	}

	builder := s.DB.JSONWidget.Create()
	ApplyJSONWidgetCreateRequest(builder, req)

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterCreate(ctx, entity)
}

// Update performs a partial update of JSONWidget, only setting non-nil fields from the request.
func (s *BaseJSONWidgetService) Update(ctx context.Context, id uuid.UUID, req *JSONWidgetUpdateRequest) (*JSONWidget, error) {
	if err := s.hooks().BeforeUpdate(ctx, id, req); err != nil {
		return nil, err
	}

	builder := s.DB.JSONWidget.UpdateOneID(id)
	ApplyJSONWidgetUpdateRequest(builder, req)

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsNotFound(err) {
			return nil, fmt.Errorf("%w: jsonwidget %s", entdomain.ErrNotFound, id)
		}
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterUpdate(ctx, entity)
}

// Delete deletes a JSONWidget by ID.
func (s *BaseJSONWidgetService) Delete(ctx context.Context, id uuid.UUID) error {
	if err := s.hooks().BeforeDelete(ctx, id); err != nil {
		return err
	}
	err := s.DB.JSONWidget.DeleteOneID(id).Exec(ctx)
	if err != nil {
		if IsNotFound(err) {
			return fmt.Errorf("%w: jsonwidget %s", entdomain.ErrNotFound, id)
		}
		return err
	}

	return s.hooks().AfterDelete(ctx, id)
}

// DeleteBatch deletes multiple JSONWidgets by IDs.
// NOTE: Before/After hooks are NOT invoked for batch operations.
// If per-item validation is needed, iterate with Delete() instead.
func (s *BaseJSONWidgetService) DeleteBatch(ctx context.Context, ids []uuid.UUID) error {
	if len(ids) == 0 {
		return nil
	}
	_, err := s.DB.JSONWidget.Delete().
		Where(jsonwidget.IDIn(ids...)).
		Exec(ctx)
	return err
}

// ListWithCursor returns cursor-paginated entities using ID-based ordering.
func (s *BaseJSONWidgetService) ListWithCursor(ctx context.Context, limit int, cursor, order string) ([]*JSONWidget, string, error) {
	query := s.DB.JSONWidget.Query()

	if cursor != "" {
		cursorID, err := uuid.Parse(cursor)
		if err != nil {
			return nil, "", fmt.Errorf("%w: invalid cursor", entdomain.ErrValidation)
		}
		if order == "asc" {
			query = query.Where(jsonwidget.IDGT(cursorID))
		} else {
			query = query.Where(jsonwidget.IDLT(cursorID))
		}
	}

	if order == "asc" {
		query = query.Order(Asc(jsonwidget.FieldID))
	} else {
		query = query.Order(Desc(jsonwidget.FieldID))
	}

	entities, err := query.Limit(limit + 1).All(ctx)
	if err != nil {
		return nil, "", err
	}

	var nextCursor string
	if len(entities) > limit {
		entities = entities[:limit]
		nextCursor = entities[len(entities)-1].ID.String()
	}

	return entities, nextCursor, nil
}

// ---------------------------------------------------------------------------
// Builder helpers: Apply requests to ent builders
// ---------------------------------------------------------------------------

// ApplyJSONWidgetCreateRequest applies all fields from a CreateRequest to an ent Create builder.
// Exported for use in custom service methods that need manual builder control.
func ApplyJSONWidgetCreateRequest(builder *JSONWidgetCreate, req *JSONWidgetCreateRequest) {
	if req.Tags != nil {
		builder.SetTags(*req.Tags)
	}
	if req.Meta != nil {
		builder.SetMeta(*req.Meta)
	}
	builder.SetRequiredTags(req.RequiredTags)
	builder.SetRequiredMeta(req.RequiredMeta)
}

// ApplyJSONWidgetUpdateRequest applies non-nil fields from an UpdateRequest to an ent UpdateOne builder.
// Only fields that are explicitly set (non-nil) in the request are applied — true partial update.
func ApplyJSONWidgetUpdateRequest(builder *JSONWidgetUpdateOne, req *JSONWidgetUpdateRequest) {
	if req.Tags != nil {
		builder.SetTags(*req.Tags)
	}
	if req.Meta != nil {
		builder.SetMeta(*req.Meta)
	}
	if req.RequiredTags != nil {
		builder.SetRequiredTags(*req.RequiredTags)
	}
	if req.RequiredMeta != nil {
		builder.SetRequiredMeta(*req.RequiredMeta)
	}
}

// ---------------------------------------------------------------------------
// Entity → Response conversion
// ---------------------------------------------------------------------------

// JSONWidgetEntToResponse converts an ent JSONWidget entity directly to a Response DTO.
// Exported for use in service-level queries that bypass the base service.
func JSONWidgetEntToResponse(entity *JSONWidget) *JSONWidgetResponse {
	if entity == nil {
		return nil
	}

	resp := &JSONWidgetResponse{
		ID:           entity.ID,
		Tags:         entdomain.PtrNilSafe(entity.Tags),
		Meta:         entdomain.PtrNilSafe(entity.Meta),
		RequiredTags: entity.RequiredTags,
		RequiredMeta: entity.RequiredMeta,
	}
	return resp
}

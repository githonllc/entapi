// Code generated by entapi extension from schema "Category". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package petstoreent

import (
	"fmt"
	"net/url"
	"sort"
	"strconv"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entapi/examples/petstore/petstoreent/category"
	"github.com/githonllc/entapi/examples/petstore/petstoreent/predicate"
	entapi "github.com/githonllc/entapi/runtime"
)

// CategoryFilter is the typed query representation for Category.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type CategoryFilter struct {
	// id: int
	ID      []int   `json:"id,omitempty"`
	IDNEQ   []int   `json:"id_neq,omitempty"`
	IDIn    [][]int `json:"id_in,omitempty"`
	IDNotIn [][]int `json:"id_not_in,omitempty"`
	IDGT    []int   `json:"id_gt,omitempty"`
	IDGTE   []int   `json:"id_gte,omitempty"`
	IDLT    []int   `json:"id_lt,omitempty"`
	IDLTE   []int   `json:"id_lte,omitempty"`
	// name: string
	Name             []string   `json:"name,omitempty"`
	NameNEQ          []string   `json:"name_neq,omitempty"`
	NameIn           [][]string `json:"name_in,omitempty"`
	NameNotIn        [][]string `json:"name_not_in,omitempty"`
	NameGT           []string   `json:"name_gt,omitempty"`
	NameGTE          []string   `json:"name_gte,omitempty"`
	NameLT           []string   `json:"name_lt,omitempty"`
	NameLTE          []string   `json:"name_lte,omitempty"`
	NameContains     []string   `json:"name_contains,omitempty"`
	NameHasPrefix    []string   `json:"name_prefix,omitempty"`
	NameHasSuffix    []string   `json:"name_suffix,omitempty"`
	NameContainsFold []string   `json:"name_icontains,omitempty"`
	// Q is the single free-text term matched across Searchable fields.
	Q *string `json:"_q,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *CategoryFilter) Predicates() []predicate.Category {
	if f == nil {
		return nil
	}
	var ps []predicate.Category
	entapi.AppendEach(&ps, f.ID, category.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, category.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, category.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, category.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, category.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, category.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, category.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, category.IDLTE)
	entapi.AppendEach(&ps, f.Name, category.NameEQ)
	entapi.AppendEach(&ps, f.NameNEQ, category.NameNEQ)
	entapi.AppendEachSlice(&ps, f.NameIn, category.NameIn)
	entapi.AppendEachSlice(&ps, f.NameNotIn, category.NameNotIn)
	entapi.AppendEach(&ps, f.NameGT, category.NameGT)
	entapi.AppendEach(&ps, f.NameGTE, category.NameGTE)
	entapi.AppendEach(&ps, f.NameLT, category.NameLT)
	entapi.AppendEach(&ps, f.NameLTE, category.NameLTE)
	entapi.AppendEach(&ps, f.NameContains, category.NameContains)
	entapi.AppendEach(&ps, f.NameHasPrefix, category.NameHasPrefix)
	entapi.AppendEach(&ps, f.NameHasSuffix, category.NameHasSuffix)
	entapi.AppendEach(&ps, f.NameContainsFold, category.NameContainsFold)
	if f.Q != nil && *f.Q != "" {
		ps = append(ps, category.Or(
			category.NameContains(*f.Q),
		))
	}
	return ps
}

func parseCategoryIDQueryValue(raw, whole string) (int, error) {
	parsed, err := strconv.ParseInt(raw, 10, 0)
	if err != nil {
		var zero int
		return zero, fmt.Errorf("%w: field %q value %q is not a valid int: %v", entapi.ErrValidation, "id", whole, err)
	}
	return int(parsed), nil
}

func parseCategoryNameQueryValue(raw, whole string) (string, error) {
	return string(raw), nil
}

// ParseCategoryQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; this generated switch owns the
// field-local operator set, conversion and predicate slots.
func ParseCategoryQuery(q url.Values) (*CategoryFilter, entapi.ListRequest, error) {
	f := &CategoryFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				term := reserved
				f.Q = &term
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.ID = append(f.ID, parsed)
				case "ne":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDNEQ = append(f.IDNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseCategoryIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDIn = append(f.IDIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseCategoryIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDNotIn = append(f.IDNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGT = append(f.IDGT, parsed)
				case "ge":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "lt":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLT = append(f.IDLT, parsed)
				case "le":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "from":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "to":
					parsed, parseErr := parseCategoryIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parseCategoryIDQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parseCategoryIDQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, lower)
					f.IDLTE = append(f.IDLTE, upper)
				}
			}
		case "name":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "like":
				case "prefix":
				case "suffix":
				case "ilike":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, like, prefix, suffix, ilike, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Name = append(f.Name, parsed)
				case "ne":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameNEQ = append(f.NameNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseCategoryNameQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.NameIn = append(f.NameIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseCategoryNameQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.NameNotIn = append(f.NameNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGT = append(f.NameGT, parsed)
				case "ge":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, parsed)
				case "lt":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLT = append(f.NameLT, parsed)
				case "le":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLTE = append(f.NameLTE, parsed)
				case "like":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameContains = append(f.NameContains, parsed)
				case "prefix":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameHasPrefix = append(f.NameHasPrefix, parsed)
				case "suffix":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameHasSuffix = append(f.NameHasSuffix, parsed)
				case "ilike":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameContainsFold = append(f.NameContainsFold, parsed)
				case "from":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, parsed)
				case "to":
					parsed, parseErr := parseCategoryNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLTE = append(f.NameLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parseCategoryNameQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parseCategoryNameQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, lower)
					f.NameLTE = append(f.NameLTE, upper)
				}
			}
		default:
			switch key {
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var categorySortOptions = map[string]func(...sql.OrderTermOption) category.OrderOption{
	"id":   category.ByID,
	"name": category.ByName,
}

// CategorySortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var CategorySortKeys = []string{
	"id", "name",
}

// CategoryOrder is the single sort allow-list seam.
func CategoryOrder(r entapi.ListRequest) ([]category.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []category.OrderOption{category.ByID(sql.OrderAsc())}, nil
	}
	orderBy := make([]category.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := categorySortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(CategorySortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		orderBy = append(orderBy, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		orderBy = append(orderBy, category.ByID(lastDir))
	}
	return orderBy, nil
}

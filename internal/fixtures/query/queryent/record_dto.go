// Code generated by entapi extension from schema "Record". DO NOT EDIT.
// Source template: templates/dto.tmpl
// Regenerate with: make generate

package queryent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"github.com/githonllc/entapi/internal/fixtures/query/queryent/record"
	entapi "github.com/githonllc/entapi/runtime"
	"github.com/google/uuid"
)

// ============================================================================================
// Layered Architecture Overview:
//
// 1. CreateRequest/PatchRequest - HTTP-layer request models derived from Ent.
//    - Hidden and ReadOnly are the only annotation-side request exclusions.
//    - Optional, Default, Nillable and Immutable determine the remaining shape.
//    - Used by the Handler layer to receive HTTP requests.
//    - Certain fields (e.g., ID, audit fields) cannot be set via the HTTP API.
//    - Presence is recorded per request, so an omitted key, an explicit null and
//      a value are three different things rather than two.
//    - Apply is defined on the VALIDATED request only, so a builder cannot be
//      written without validating first.
//
// 2. Response/Summary - HTTP-layer response models derived from Ent.
//    - Hidden and Ent Sensitive fields are excluded; ReadOnly fields remain.
//    - Includes a nested edge when the edge carries api.Expand().
//      Exposing the foreign-key scalar and exposing the nested object are
//      independent decisions; neither is derived from the other.
//    - A nested edge is rendered as the target's Summary, which carries no
//      edges of its own, so expansion terminates one level down.
//    - Used by the Handler layer to return HTTP responses.
//
// Key Design Principles:
// - transport → record_wiring.go → entapi runtime → ent.Client
// - Deviation words affect HTTP generation only; they restrict nothing at the Ent layer.
// - The consumer's own code operates directly on ent entities with full ORM capabilities.
// ============================================================================================

// RecordCreateRequest is the create request for Record.
//
// Field shape follows ent's schema rather than a parallel set of booleans. A
// field ent can fill by itself — Optional, or carrying a Default() — is a
// pointer here and is written to the builder only when the caller supplied a
// value, so an omitted key leaves the schema's default in effect. A field ent
// requires and cannot default is a value type and is always written.
type RecordCreateRequest struct {
	Title  string         `json:"title"`
	Body   string         `json:"body"`
	Ref    *string        `json:"reference,omitempty"`
	Status *record.Status `json:"status,omitempty"`
	Score  *int           `json:"score,omitempty"`
	Note   *string        `json:"note,omitempty"`
	Secret string         `json:"secret"`

	// present records which keys the payload actually carried. It is what makes
	// "omitted" distinguishable from "sent as the zero value" for a field whose
	// Go type has no nil — a required int or bool got no check at all while
	// requiredness was inferred from the zero value.
	//
	// A create request cannot express "clear", so an explicit null counts as
	// absent: it leaves the field unwritten, exactly like omitting the key.
	present map[string]bool
}

// recordCreateRequestTags is the canonical JSON key of every field on
// RecordCreateRequest, in declaration order. UnmarshalJSON needs the tags as
// data, not as struct tags, to tell a case variant from an unrelated key.
var recordCreateRequestTags = []string{"title", "body", "reference", "status", "score", "note", "secret"}

// RecordCreateRequestTags returns the canonical JSON key of every field on
// RecordCreateRequest, in declaration order. Generated handlers compare raw
// request keys against this data to reject unknown keys.
//
// Like Routes(), it returns a fresh copy on every call: slices are mutable, so
// exporting the variable would let any consumer mutate validation data that
// every generated handler in the process reads. The unexported variable remains
// the source of truth; this accessor is for consumers that decline the generated
// handler and write their own bind step.
func RecordCreateRequestTags() []string {
	return append([]string(nil), recordCreateRequestTags...)
}

// UnmarshalJSON records presence, then decodes normally.
//
// The wire format is unchanged: every exported field keeps its ordinary type
// and tag, so marshalling, form binders, validators and spec generators all see
// the struct they saw before. That is the property a generic Optional[T]
// wrapper could not keep.
//
// A key that case-folds to a canonical tag without matching it exactly is
// rejected (#58, ADR-0001): encoding/json would fill the field from it while
// presence — recorded by raw key — would not, so the value decoded and then
// went unwritten. The check runs after raw decodes and BEFORE the alias decode,
// so a rejected request leaves the receiver untouched. A key that folds to no
// tag keeps its old behaviour and is ignored; refusing those is
// DisallowUnknownFields, which stays the consumer handler's decision.
func (r *RecordCreateRequest) UnmarshalJSON(b []byte) error {
	type alias RecordCreateRequest // breaks the UnmarshalJSON recursion
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(b, &raw); err != nil {
		return err
	}
	for k := range raw {
		for _, t := range recordCreateRequestTags {
			if k != t && strings.EqualFold(k, t) {
				return fmt.Errorf("%w: unknown key %q (did you mean %q?)", entapi.ErrValidation, k, t)
			}
		}
	}
	if err := json.Unmarshal(b, (*alias)(r)); err != nil {
		return err
	}
	r.present = make(map[string]bool, len(raw))
	for k, v := range raw {
		if string(v) != "null" {
			r.present[k] = true
		}
	}
	return nil
}

// has reports whether the decoded payload carried a value for a key.
//
// A request built in Go rather than decoded from JSON recorded no presence at
// all, and there the struct is the only source of truth: every field reads as
// supplied, which is what the caller assigning to it meant. UnmarshalJSON always
// allocates the map, even for {}, so this fallback cannot fire on a decoded
// request. Reporting such a request as entirely absent instead would reject
// "seats is required" for a request that plainly sets Seats.
//
// The patch request deliberately does NOT do this: there, "unknown" has to mean
// absent, or a hand-built request would read as an instruction to clear
// everything.
func (r *RecordCreateRequest) has(field string) bool {
	if r.present == nil {
		return true
	}
	return r.present[field]
}

// HasTitle reports whether the payload carried "title".
func (r *RecordCreateRequest) HasTitle() bool { return r.has("title") }

// HasBody reports whether the payload carried "body".
func (r *RecordCreateRequest) HasBody() bool { return r.has("body") }

// HasRef reports whether the payload carried "reference".
func (r *RecordCreateRequest) HasRef() bool { return r.has("reference") }

// HasStatus reports whether the payload carried "status".
func (r *RecordCreateRequest) HasStatus() bool { return r.has("status") }

// HasScore reports whether the payload carried "score".
func (r *RecordCreateRequest) HasScore() bool { return r.has("score") }

// HasNote reports whether the payload carried "note".
func (r *RecordCreateRequest) HasNote() bool { return r.has("note") }

// HasSecret reports whether the payload carried "secret".
func (r *RecordCreateRequest) HasSecret() bool { return r.has("secret") }

// ValidRecordCreateRequest wraps a RecordCreateRequest that has
// passed Validate. Apply is defined on this type and nowhere else, so a caller
// who skips validation has no method to call — a compile error rather than a
// validator that runs only when someone remembers it.
type ValidRecordCreateRequest struct{ r *RecordCreateRequest }

// HasTitle reports whether the payload carried "title".
func (v *ValidRecordCreateRequest) HasTitle() bool { return v.r.HasTitle() }

// HasBody reports whether the payload carried "body".
func (v *ValidRecordCreateRequest) HasBody() bool { return v.r.HasBody() }

// HasRef reports whether the payload carried "reference".
func (v *ValidRecordCreateRequest) HasRef() bool { return v.r.HasRef() }

// HasStatus reports whether the payload carried "status".
func (v *ValidRecordCreateRequest) HasStatus() bool { return v.r.HasStatus() }

// HasScore reports whether the payload carried "score".
func (v *ValidRecordCreateRequest) HasScore() bool { return v.r.HasScore() }

// HasNote reports whether the payload carried "note".
func (v *ValidRecordCreateRequest) HasNote() bool { return v.r.HasNote() }

// HasSecret reports whether the payload carried "secret".
func (v *ValidRecordCreateRequest) HasSecret() bool { return v.r.HasSecret() }

// Validate checks the request and returns the only type Apply accepts.
func (r *RecordCreateRequest) Validate() (*ValidRecordCreateRequest, error) {
	if r == nil {
		return nil, fmt.Errorf("%w: create request is nil", entapi.ErrValidation)
	}
	if r.Title == "" {
		return nil, fmt.Errorf("%w: title is required", entapi.ErrValidation)
	}
	if r.Body == "" {
		return nil, fmt.Errorf("%w: body is required", entapi.ErrValidation)
	}
	if r.Secret == "" {
		return nil, fmt.Errorf("%w: secret is required", entapi.ErrValidation)
	}
	if r.Status != nil {
		if err := record.StatusValidator(*r.Status); err != nil {
			return nil, fmt.Errorf("%w: %v", entapi.ErrValidation, err)
		}
	}
	return &ValidRecordCreateRequest{r: r}, nil
}

// Apply writes the request onto a create builder.
//
// A field the caller omitted is not written at all, so the schema's Default()
// still applies. Writing the zero value unconditionally is what silently
// defeated every schema default.
func (v *ValidRecordCreateRequest) Apply(b *RecordCreate) *RecordCreate {
	r := v.r
	b.SetTitle(r.Title)
	b.SetBody(r.Body)
	if r.Ref != nil {
		b.SetRef(*r.Ref)
	}
	if r.Status != nil {
		b.SetStatus(*r.Status)
	}
	if r.Score != nil {
		b.SetScore(*r.Score)
	}
	if r.Note != nil {
		b.SetNote(*r.Note)
	}
	b.SetSecret(r.Secret)
	return b
}

// RecordPatchRequest is the partial-update request for Record.
//
// Every field is a pointer and presence is recorded separately, which is what
// separates the three states a PATCH has to express: absent means "leave it
// alone", an explicit null means "clear it", and a value means "set it". With
// bare pointers the first two are the same value, and clearing a field cannot
// be expressed at all.
//
// A field the ent schema marks Immutable() is absent from this struct, because
// ent's update builders iterate MutableFields and generate no setter for one.
// encoding/json discards the key before any validator can see it, so a caller
// who sends it gets silence — rejecting that needs DisallowUnknownFields, which
// belongs to the consumer's handler.
type RecordPatchRequest struct {
	Title  *string        `json:"title,omitempty"`
	Body   *string        `json:"body,omitempty"`
	Ref    *string        `json:"reference,omitempty"`
	Status *record.Status `json:"status,omitempty"`
	Score  *int           `json:"score,omitempty"`
	Note   *string        `json:"note,omitempty"`
	Secret *string        `json:"secret,omitempty"`

	present map[string]bool
}

// recordPatchRequestTags is the canonical JSON key of every field on
// RecordPatchRequest, in declaration order.
var recordPatchRequestTags = []string{"title", "body", "reference", "status", "score", "note", "secret"}

// RecordPatchRequestTags returns the canonical JSON key of every field on
// RecordPatchRequest, in declaration order. Generated handlers compare raw
// request keys against this data to reject unknown keys.
//
// Like Routes(), it returns a fresh copy on every call: slices are mutable, so
// exporting the variable would let any consumer mutate validation data that
// every generated handler in the process reads. The unexported variable remains
// the source of truth; this accessor is for consumers that decline the generated
// handler and write their own bind step.
func RecordPatchRequestTags() []string {
	return append([]string(nil), recordPatchRequestTags...)
}

// UnmarshalJSON records which keys the payload carried, including the ones
// whose value was null — that is the whole point here, and the difference from
// the create request, where a null has nothing to clear.
//
// A key that case-folds to a canonical tag without matching it exactly is
// rejected (#58, ADR-0001). The patch side is where that mattered most: a
// case-variant key decoded into the field while Has<Field>() stayed false, so
// Apply wrote nothing and the update reported success having changed no row —
// and a payload carrying BOTH spellings could clear a field the exact key had
// just set. The check runs after raw decodes and BEFORE the alias decode, so a
// rejected request leaves the receiver untouched.
func (r *RecordPatchRequest) UnmarshalJSON(b []byte) error {
	type alias RecordPatchRequest // breaks the UnmarshalJSON recursion
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(b, &raw); err != nil {
		return err
	}
	for k := range raw {
		for _, t := range recordPatchRequestTags {
			if k != t && strings.EqualFold(k, t) {
				return fmt.Errorf("%w: unknown key %q (did you mean %q?)", entapi.ErrValidation, k, t)
			}
		}
	}
	if err := json.Unmarshal(b, (*alias)(r)); err != nil {
		return err
	}
	r.present = make(map[string]bool, len(raw))
	for k := range raw {
		r.present[k] = true
	}
	return nil
}

// has reports whether the decoded payload carried the key at all. A request
// built in Go rather than decoded from JSON has no presence, and Apply writes
// nothing for it.
func (r *RecordPatchRequest) has(field string) bool { return r.present[field] }

// HasTitle reports whether the payload carried "title".
func (r *RecordPatchRequest) HasTitle() bool { return r.has("title") }

// HasBody reports whether the payload carried "body".
func (r *RecordPatchRequest) HasBody() bool { return r.has("body") }

// HasRef reports whether the payload carried "reference".
func (r *RecordPatchRequest) HasRef() bool { return r.has("reference") }

// HasStatus reports whether the payload carried "status".
func (r *RecordPatchRequest) HasStatus() bool { return r.has("status") }

// HasScore reports whether the payload carried "score".
func (r *RecordPatchRequest) HasScore() bool { return r.has("score") }

// HasNote reports whether the payload carried "note".
func (r *RecordPatchRequest) HasNote() bool { return r.has("note") }

// HasSecret reports whether the payload carried "secret".
func (r *RecordPatchRequest) HasSecret() bool { return r.has("secret") }

// ValidRecordPatchRequest wraps a RecordPatchRequest that has passed
// Validate. It is the only type Apply is defined on.
type ValidRecordPatchRequest struct{ r *RecordPatchRequest }

// HasTitle reports whether the payload carried "title".
//
// A custom implementation receives only the validated type, so without this
// forwarder the patch's presence — the one thing that distinguishes "clear it"
// from "leave it alone" — is unreachable from the customization point that is
// supposed to act on it.
func (v *ValidRecordPatchRequest) HasTitle() bool { return v.r.HasTitle() }

// Title reports the value the payload carried for
// "title", and whether Apply will Set it.
//
// Presence is two of the three states a PATCH field has; this reads the third.
// Paired with HasTitle(), which is "present" below:
//
//	ok                    the payload carried a value; Apply will Set it
//	!ok, present          the payload carried an explicit null; Apply will
//	                      Clear it. Reachable only for a clearable field —
//	                      Validate refuses a null on anything else
//	!ok, absent           the key was not in the payload; Apply writes nothing
//
// Without it a cross-field rule cannot tell "clear this" from "set this", and
// the customization point is forced to allocate an update builder it never
// executes, Apply the request to it and read back Mutation() (#113).
func (v *ValidRecordPatchRequest) Title() (string, bool) {
	if !v.r.HasTitle() || v.r.Title == nil {
		var zero string
		return zero, false
	}
	return *v.r.Title, true
}

// HasBody reports whether the payload carried "body".
//
// A custom implementation receives only the validated type, so without this
// forwarder the patch's presence — the one thing that distinguishes "clear it"
// from "leave it alone" — is unreachable from the customization point that is
// supposed to act on it.
func (v *ValidRecordPatchRequest) HasBody() bool { return v.r.HasBody() }

// Body reports the value the payload carried for
// "body", and whether Apply will Set it.
//
// Presence is two of the three states a PATCH field has; this reads the third.
// Paired with HasBody(), which is "present" below:
//
//	ok                    the payload carried a value; Apply will Set it
//	!ok, present          the payload carried an explicit null; Apply will
//	                      Clear it. Reachable only for a clearable field —
//	                      Validate refuses a null on anything else
//	!ok, absent           the key was not in the payload; Apply writes nothing
//
// Without it a cross-field rule cannot tell "clear this" from "set this", and
// the customization point is forced to allocate an update builder it never
// executes, Apply the request to it and read back Mutation() (#113).
func (v *ValidRecordPatchRequest) Body() (string, bool) {
	if !v.r.HasBody() || v.r.Body == nil {
		var zero string
		return zero, false
	}
	return *v.r.Body, true
}

// HasRef reports whether the payload carried "reference".
//
// A custom implementation receives only the validated type, so without this
// forwarder the patch's presence — the one thing that distinguishes "clear it"
// from "leave it alone" — is unreachable from the customization point that is
// supposed to act on it.
func (v *ValidRecordPatchRequest) HasRef() bool { return v.r.HasRef() }

// Ref reports the value the payload carried for
// "reference", and whether Apply will Set it.
//
// Presence is two of the three states a PATCH field has; this reads the third.
// Paired with HasRef(), which is "present" below:
//
//	ok                    the payload carried a value; Apply will Set it
//	!ok, present          the payload carried an explicit null; Apply will
//	                      Clear it. Reachable only for a clearable field —
//	                      Validate refuses a null on anything else
//	!ok, absent           the key was not in the payload; Apply writes nothing
//
// Without it a cross-field rule cannot tell "clear this" from "set this", and
// the customization point is forced to allocate an update builder it never
// executes, Apply the request to it and read back Mutation() (#113).
func (v *ValidRecordPatchRequest) Ref() (string, bool) {
	if !v.r.HasRef() || v.r.Ref == nil {
		var zero string
		return zero, false
	}
	return *v.r.Ref, true
}

// HasStatus reports whether the payload carried "status".
//
// A custom implementation receives only the validated type, so without this
// forwarder the patch's presence — the one thing that distinguishes "clear it"
// from "leave it alone" — is unreachable from the customization point that is
// supposed to act on it.
func (v *ValidRecordPatchRequest) HasStatus() bool { return v.r.HasStatus() }

// Status reports the value the payload carried for
// "status", and whether Apply will Set it.
//
// Presence is two of the three states a PATCH field has; this reads the third.
// Paired with HasStatus(), which is "present" below:
//
//	ok                    the payload carried a value; Apply will Set it
//	!ok, present          the payload carried an explicit null; Apply will
//	                      Clear it. Reachable only for a clearable field —
//	                      Validate refuses a null on anything else
//	!ok, absent           the key was not in the payload; Apply writes nothing
//
// Without it a cross-field rule cannot tell "clear this" from "set this", and
// the customization point is forced to allocate an update builder it never
// executes, Apply the request to it and read back Mutation() (#113).
func (v *ValidRecordPatchRequest) Status() (record.Status, bool) {
	if !v.r.HasStatus() || v.r.Status == nil {
		var zero record.Status
		return zero, false
	}
	return *v.r.Status, true
}

// HasScore reports whether the payload carried "score".
//
// A custom implementation receives only the validated type, so without this
// forwarder the patch's presence — the one thing that distinguishes "clear it"
// from "leave it alone" — is unreachable from the customization point that is
// supposed to act on it.
func (v *ValidRecordPatchRequest) HasScore() bool { return v.r.HasScore() }

// Score reports the value the payload carried for
// "score", and whether Apply will Set it.
//
// Presence is two of the three states a PATCH field has; this reads the third.
// Paired with HasScore(), which is "present" below:
//
//	ok                    the payload carried a value; Apply will Set it
//	!ok, present          the payload carried an explicit null; Apply will
//	                      Clear it. Reachable only for a clearable field —
//	                      Validate refuses a null on anything else
//	!ok, absent           the key was not in the payload; Apply writes nothing
//
// Without it a cross-field rule cannot tell "clear this" from "set this", and
// the customization point is forced to allocate an update builder it never
// executes, Apply the request to it and read back Mutation() (#113).
func (v *ValidRecordPatchRequest) Score() (int, bool) {
	if !v.r.HasScore() || v.r.Score == nil {
		var zero int
		return zero, false
	}
	return *v.r.Score, true
}

// HasNote reports whether the payload carried "note".
//
// A custom implementation receives only the validated type, so without this
// forwarder the patch's presence — the one thing that distinguishes "clear it"
// from "leave it alone" — is unreachable from the customization point that is
// supposed to act on it.
func (v *ValidRecordPatchRequest) HasNote() bool { return v.r.HasNote() }

// Note reports the value the payload carried for
// "note", and whether Apply will Set it.
//
// Presence is two of the three states a PATCH field has; this reads the third.
// Paired with HasNote(), which is "present" below:
//
//	ok                    the payload carried a value; Apply will Set it
//	!ok, present          the payload carried an explicit null; Apply will
//	                      Clear it. Reachable only for a clearable field —
//	                      Validate refuses a null on anything else
//	!ok, absent           the key was not in the payload; Apply writes nothing
//
// Without it a cross-field rule cannot tell "clear this" from "set this", and
// the customization point is forced to allocate an update builder it never
// executes, Apply the request to it and read back Mutation() (#113).
func (v *ValidRecordPatchRequest) Note() (string, bool) {
	if !v.r.HasNote() || v.r.Note == nil {
		var zero string
		return zero, false
	}
	return *v.r.Note, true
}

// HasSecret reports whether the payload carried "secret".
//
// A custom implementation receives only the validated type, so without this
// forwarder the patch's presence — the one thing that distinguishes "clear it"
// from "leave it alone" — is unreachable from the customization point that is
// supposed to act on it.
func (v *ValidRecordPatchRequest) HasSecret() bool { return v.r.HasSecret() }

// Secret reports the value the payload carried for
// "secret", and whether Apply will Set it.
//
// Presence is two of the three states a PATCH field has; this reads the third.
// Paired with HasSecret(), which is "present" below:
//
//	ok                    the payload carried a value; Apply will Set it
//	!ok, present          the payload carried an explicit null; Apply will
//	                      Clear it. Reachable only for a clearable field —
//	                      Validate refuses a null on anything else
//	!ok, absent           the key was not in the payload; Apply writes nothing
//
// Without it a cross-field rule cannot tell "clear this" from "set this", and
// the customization point is forced to allocate an update builder it never
// executes, Apply the request to it and read back Mutation() (#113).
func (v *ValidRecordPatchRequest) Secret() (string, bool) {
	if !v.r.HasSecret() || v.r.Secret == nil {
		var zero string
		return zero, false
	}
	return *v.r.Secret, true
}

// Validate rejects an explicit null on a field that cannot be cleared.
//
// ent emits Clear<Field>() for Optional fields and for no others, so a null on
// anything else has no correct translation: the column is NOT NULL and there is
// no method to call. The field is named in the error, because "invalid request"
// tells the caller nothing about which key to fix.
func (r *RecordPatchRequest) Validate() (*ValidRecordPatchRequest, error) {
	if r == nil {
		return nil, fmt.Errorf("%w: patch request is nil", entapi.ErrValidation)
	}
	if r.HasTitle() && r.Title == nil {
		return nil, fmt.Errorf("%w: title cannot be null", entapi.ErrValidation)
	}
	if r.HasBody() && r.Body == nil {
		return nil, fmt.Errorf("%w: body cannot be null", entapi.ErrValidation)
	}
	if r.HasStatus() && r.Status == nil {
		return nil, fmt.Errorf("%w: status cannot be null", entapi.ErrValidation)
	}
	if r.HasSecret() && r.Secret == nil {
		return nil, fmt.Errorf("%w: secret cannot be null", entapi.ErrValidation)
	}
	if r.Status != nil {
		if err := record.StatusValidator(*r.Status); err != nil {
			return nil, fmt.Errorf("%w: %v", entapi.ErrValidation, err)
		}
	}
	return &ValidRecordPatchRequest{r: r}, nil
}

// Apply writes the request onto an update builder. Absent fields are not
// touched; the rest are set, or cleared when the caller sent an explicit null.
func (v *ValidRecordPatchRequest) Apply(b *RecordUpdateOne) *RecordUpdateOne {
	r := v.r
	if r.HasTitle() {
		b.SetTitle(*r.Title)
	}
	if r.HasBody() {
		b.SetBody(*r.Body)
	}
	if r.HasRef() {
		if r.Ref == nil {
			b.ClearRef()
		} else {
			b.SetRef(*r.Ref)
		}
	}
	if r.HasStatus() {
		b.SetStatus(*r.Status)
	}
	if r.HasScore() {
		if r.Score == nil {
			b.ClearScore()
		} else {
			b.SetScore(*r.Score)
		}
	}
	if r.HasNote() {
		if r.Note == nil {
			b.ClearNote()
		} else {
			b.SetNote(*r.Note)
		}
	}
	if r.HasSecret() {
		b.SetSecret(*r.Secret)
	}
	return b
}

// RecordSummary is the shape Record takes on another entity's response.
//
// It carries the same scalar fields as RecordResponse and no edges at all.
// That is what bounds expansion: NewRecordResponse calls summary
// constructors, and a summary constructor calls nothing, so there is no second
// level for a cycle to close through and no runtime depth counter is needed.
//
// The cost is real rather than hidden: a three-level tree comes back one level
// deep, and a deeper one needs another round trip per level.
type RecordSummary struct {
	// ID field is always included in summaries
	ID        uuid.UUID     `json:"id"`
	Title     string        `json:"title"`
	Body      string        `json:"body"`
	Ref       *string       `json:"reference,omitempty"`
	Status    record.Status `json:"status"`
	Score     *int          `json:"score,omitempty"`
	CreatedAt time.Time     `json:"created_at"`
	Note      *string       `json:"note,omitempty"`
}

// NewRecordSummary converts an entity to its summary DTO. It cannot fail:
// a summary reads no edges.
func NewRecordSummary(e *Record) *RecordSummary {
	if e == nil {
		return nil
	}
	return &RecordSummary{
		ID:        e.ID,
		Title:     e.Title,
		Body:      e.Body,
		Ref:       entapi.PtrOrNil(e.Ref),
		Status:    e.Status,
		Score:     e.Score,
		CreatedAt: e.CreatedAt,
		Note:      entapi.PtrOrNil(e.Note),
	}
}

// RecordResponse represents the response for Record.
//
// It is emitted unconditionally: an entity whose every scalar field is
// Sensitive still has a meaningful response carrying its ID, and
// RecordListResponse below refers to this type either way.
type RecordResponse struct {
	// ID field is always included in responses
	ID        uuid.UUID     `json:"id"`
	Title     string        `json:"title"`
	Body      string        `json:"body"`
	Ref       *string       `json:"reference,omitempty"`
	Status    record.Status `json:"status"`
	Score     *int          `json:"score,omitempty"`
	CreatedAt time.Time     `json:"created_at"`
	Note      *string       `json:"note,omitempty"`
}

// NewRecordResponse converts an entity to its response DTO.
//
// This entity declares no response edges, so the error is always nil. The
// signature is kept uniform so callers do not need two shapes.
func NewRecordResponse(e *Record) (*RecordResponse, error) {
	if e == nil {
		return nil, nil
	}
	r := &RecordResponse{
		ID:        e.ID,
		Title:     e.Title,
		Body:      e.Body,
		Ref:       entapi.PtrOrNil(e.Ref),
		Status:    e.Status,
		Score:     e.Score,
		CreatedAt: e.CreatedAt,
		Note:      entapi.PtrOrNil(e.Note),
	}
	return r, nil
}

// RecordQueryWithResponseEdges applies the eager-load plan for RecordResponse.
//
// The plan is generated from the response type's own edge set, so no individual
// edge can be omitted from it. Generated wiring always routes through this
// function.
//
// For a hand-written query, using this function is a convention the type
// system does not enforce. An entity loaded without this plan makes
// NewRecordResponse return a not-loaded-edge error at run time. It fails
// loudly; it never silently narrows the response.
//
// With no expanded edges, this function is the identity function. It exists so
// every entity has the same call shape.
func RecordQueryWithResponseEdges(q *RecordQuery) *RecordQuery {
	return q
}

// RecordListResponse is the NAMED, non-generic shape of one list page —
// for OpenAPI/swaggo-class annotation tooling, which cannot express the
// generic entapi.Page[RecordResponse] that ListRecords returns.
// Convert at the handler boundary with NewRecordListResponse.
// (A fifth field, PageInfo, carried cursor metadata until #6 removed it.)
type RecordListResponse struct {
	Data  []*RecordResponse `json:"data"`
	Total int               `json:"total"`
	Page  int               `json:"page"`
	Size  int               `json:"size"`
}

// NewRecordListResponse converts the page ListRecords returns into
// the named list shape. The conversion expression is the shape contract:
// if RecordListResponse and entapi.Page ever diverge in field set,
// type or order, this line stops compiling in every generated package.
// JSON tags are outside what a conversion checks; the wire-format golden
// test in the basic fixture guards those.
func NewRecordListResponse(p *entapi.Page[RecordResponse]) *RecordListResponse {
	if p == nil {
		return nil
	}
	r := RecordListResponse(*p)
	return &r
}

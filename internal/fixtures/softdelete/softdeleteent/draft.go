// Code generated by ent, DO NOT EDIT.

package softdeleteent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entapi/internal/fixtures/softdelete/softdeleteent/doc"
	"github.com/githonllc/entapi/internal/fixtures/softdelete/softdeleteent/draft"
	"github.com/google/uuid"
)

// Draft is the model entity for the Draft schema.
type Draft struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Headline holds the value of the "headline" field.
	Headline string `json:"headline,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DraftQuery when eager-loading is set.
	Edges        DraftEdges `json:"edges"`
	draft_doc    *uuid.UUID
	selectValues sql.SelectValues
}

// DraftEdges holds the relations/edges for other nodes in the graph.
type DraftEdges struct {
	// Doc holds the value of the doc edge.
	Doc *Doc `json:"doc,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DocOrErr returns the Doc value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DraftEdges) DocOrErr() (*Doc, error) {
	if e.Doc != nil {
		return e.Doc, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: doc.Label}
	}
	return nil, &NotLoadedError{edge: "doc"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Draft) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case draft.FieldHeadline:
			values[i] = new(sql.NullString)
		case draft.FieldID:
			values[i] = new(uuid.UUID)
		case draft.ForeignKeys[0]: // draft_doc
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Draft fields.
func (d *Draft) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case draft.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				d.ID = *value
			}
		case draft.FieldHeadline:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field headline", values[i])
			} else if value.Valid {
				d.Headline = value.String
			}
		case draft.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field draft_doc", values[i])
			} else if value.Valid {
				d.draft_doc = new(uuid.UUID)
				*d.draft_doc = *value.S.(*uuid.UUID)
			}
		default:
			d.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Draft.
// This includes values selected through modifiers, order, etc.
func (d *Draft) Value(name string) (ent.Value, error) {
	return d.selectValues.Get(name)
}

// QueryDoc queries the "doc" edge of the Draft entity.
func (d *Draft) QueryDoc() *DocQuery {
	return NewDraftClient(d.config).QueryDoc(d)
}

// Update returns a builder for updating this Draft.
// Note that you need to call Draft.Unwrap() before calling this method if this Draft
// was returned from a transaction, and the transaction was committed or rolled back.
func (d *Draft) Update() *DraftUpdateOne {
	return NewDraftClient(d.config).UpdateOne(d)
}

// Unwrap unwraps the Draft entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (d *Draft) Unwrap() *Draft {
	_tx, ok := d.config.driver.(*txDriver)
	if !ok {
		panic("softdeleteent: Draft is not a transactional entity")
	}
	d.config.driver = _tx.drv
	return d
}

// String implements the fmt.Stringer.
func (d *Draft) String() string {
	var builder strings.Builder
	builder.WriteString("Draft(")
	builder.WriteString(fmt.Sprintf("id=%v, ", d.ID))
	builder.WriteString("headline=")
	builder.WriteString(d.Headline)
	builder.WriteByte(')')
	return builder.String()
}

// Drafts is a parsable slice of Draft.
type Drafts []*Draft

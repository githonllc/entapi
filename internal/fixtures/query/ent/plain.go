// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entdomain/internal/fixtures/query/ent/plain"
	"github.com/google/uuid"
)

// Plain is the model entity for the Plain schema.
type Plain struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Label holds the value of the "label" field.
	Label        string `json:"label,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Plain) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case plain.FieldLabel:
			values[i] = new(sql.NullString)
		case plain.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Plain fields.
func (pl *Plain) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case plain.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pl.ID = *value
			}
		case plain.FieldLabel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field label", values[i])
			} else if value.Valid {
				pl.Label = value.String
			}
		default:
			pl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Plain.
// This includes values selected through modifiers, order, etc.
func (pl *Plain) Value(name string) (ent.Value, error) {
	return pl.selectValues.Get(name)
}

// Update returns a builder for updating this Plain.
// Note that you need to call Plain.Unwrap() before calling this method if this Plain
// was returned from a transaction, and the transaction was committed or rolled back.
func (pl *Plain) Update() *PlainUpdateOne {
	return NewPlainClient(pl.config).UpdateOne(pl)
}

// Unwrap unwraps the Plain entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pl *Plain) Unwrap() *Plain {
	_tx, ok := pl.config.driver.(*txDriver)
	if !ok {
		panic("ent: Plain is not a transactional entity")
	}
	pl.config.driver = _tx.drv
	return pl
}

// String implements the fmt.Stringer.
func (pl *Plain) String() string {
	var builder strings.Builder
	builder.WriteString("Plain(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pl.ID))
	builder.WriteString("label=")
	builder.WriteString(pl.Label)
	builder.WriteByte(')')
	return builder.String()
}

// Plains is a parsable slice of Plain.
type Plains []*Plain

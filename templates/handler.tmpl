{{/* gotype: entgo.io/ent/entc/gen.Type */}}

// Code generated by entapi extension from schema "{{ $.Name }}". DO NOT EDIT.
// Source template: templates/handler.tmpl
// Regenerate with: make generate

package {{ base $.Config.Package }}

{{- $ops := resourceOps $ }}
{{- if $ops }}

import (
{{- range handlerImports $ }}
	{{ . }}
{{- end }}

	// Runtime helpers are kept distinct from identifier-type dependencies.
	entapi "{{ entapiPkg }}"
)
{{- end }}

{{- $id := $.ID }}
{{- range $op := $ops }}
{{- $name := printf "%s" $op.Name }}
{{- if eq $name "list" }}

// {{ $op.Function }}Fn is byte-identical to {{ $op.Function }}.
type {{ $op.Function }}Fn func(context.Context, *Client, *{{ $.Name }}Filter, entapi.ListRequest) (*entapi.Page[{{ $.Name }}Response], error)

func (f {{ $op.Function }}Fn) applyOption(h *APIHandler) {
	if f == nil {
		panic("entapi: {{ $op.Function }}Fn is nil")
	}
	h.{{ $op.Field }} = f
}

func (h *APIHandler) {{ $op.Handler }}(w http.ResponseWriter, r *http.Request) {
	filter, request, err := Parse{{ $.Name }}Query(r.URL.Query())
	if err != nil {
		entapi.WriteProblem(w, http.StatusBadRequest, http.StatusText(http.StatusBadRequest), err)
		return
	}

	response, err := h.{{ $op.Field }}(r.Context(), h.client, filter, request)
	if err != nil {
		status := entapi.Status(err, http.StatusBadRequest)
		entapi.WriteProblem(w, status, http.StatusText(status), err)
		return
	}

	_ = entapi.WriteJSON(w, http.StatusOK, response)
}
{{- else if eq $name "create" }}

// {{ $op.Function }}Fn is byte-identical to {{ $op.Function }}.
type {{ $op.Function }}Fn func(context.Context, *Client, *Valid{{ $.Name }}CreateRequest) (*{{ $.Name }}Response, error)

func (f {{ $op.Function }}Fn) applyOption(h *APIHandler) {
	if f == nil {
		panic("entapi: {{ $op.Function }}Fn is nil")
	}
	h.{{ $op.Field }} = f
}

func (h *APIHandler) {{ $op.Handler }}(w http.ResponseWriter, r *http.Request) {
	var request {{ $.Name }}CreateRequest
	if err := entapi.BindJSON(w, r, {{ camelCase $.Name }}CreateRequestTags, &request); err != nil {
		status := entapi.Status(err, http.StatusBadRequest)
		entapi.WriteProblem(w, status, http.StatusText(status), err)
		return
	}
	validated, err := request.Validate()
	if err != nil {
		entapi.WriteProblem(w, http.StatusUnprocessableEntity, http.StatusText(http.StatusUnprocessableEntity), err)
		return
	}

	response, err := h.{{ $op.Field }}(r.Context(), h.client, validated)
	if err != nil {
		status := entapi.Status(err, http.StatusUnprocessableEntity)
		entapi.WriteProblem(w, status, http.StatusText(status), err)
		return
	}

	_ = entapi.WriteJSON(w, http.StatusCreated, response)
}
{{- else if eq $name "get" }}

// {{ $op.Function }}Fn is byte-identical to {{ $op.Function }}.
type {{ $op.Function }}Fn func(context.Context, *Client, {{ $id.Type }}) (*{{ $.Name }}Response, error)

func (f {{ $op.Function }}Fn) applyOption(h *APIHandler) {
	if f == nil {
		panic("entapi: {{ $op.Function }}Fn is nil")
	}
	h.{{ $op.Field }} = f
}

func (h *APIHandler) {{ $op.Handler }}(w http.ResponseWriter, r *http.Request) {
{{- if $id.IsUUID }}
	id, err := {{ idParseExpr $id }}
	if err != nil {
		entapi.WriteProblem(w, http.StatusBadRequest, http.StatusText(http.StatusBadRequest),
			fmt.Errorf("invalid path id %q: %w", r.PathValue("id"), err))
		return
	}
{{- else if $id.Type.Type.Integer }}
	parsedID, err := {{ idParseExpr $id }}
	if err != nil {
		entapi.WriteProblem(w, http.StatusBadRequest, http.StatusText(http.StatusBadRequest),
			fmt.Errorf("invalid path id %q: %w", r.PathValue("id"), err))
		return
	}
	id := {{ $id.Type }}(parsedID)
{{- else }}
	id := {{ $id.Type }}({{ idParseExpr $id }})
{{- end }}

	response, err := h.{{ $op.Field }}(r.Context(), h.client, id)
	if err != nil {
		status := http.StatusInternalServerError
		switch {
		case entapi.IsNotFound(err):
			status = http.StatusNotFound
		case entapi.IsAlreadyExists(err):
			status = http.StatusConflict
		}
		entapi.WriteProblem(w, status, http.StatusText(status), err)
		return
	}

	_ = entapi.WriteJSON(w, http.StatusOK, response)
}
{{- else if eq $name "patch" }}

// {{ $op.Function }}Fn is byte-identical to {{ $op.Function }}.
type {{ $op.Function }}Fn func(context.Context, *Client, {{ $id.Type }}, *Valid{{ $.Name }}PatchRequest) (*{{ $.Name }}Response, error)

func (f {{ $op.Function }}Fn) applyOption(h *APIHandler) {
	if f == nil {
		panic("entapi: {{ $op.Function }}Fn is nil")
	}
	h.{{ $op.Field }} = f
}

func (h *APIHandler) {{ $op.Handler }}(w http.ResponseWriter, r *http.Request) {
	var request {{ $.Name }}PatchRequest
	if err := entapi.BindJSON(w, r, {{ camelCase $.Name }}PatchRequestTags, &request); err != nil {
		status := entapi.Status(err, http.StatusBadRequest)
		entapi.WriteProblem(w, status, http.StatusText(status), err)
		return
	}
	validated, err := request.Validate()
	if err != nil {
		entapi.WriteProblem(w, http.StatusUnprocessableEntity, http.StatusText(http.StatusUnprocessableEntity), err)
		return
	}
{{- if $id.IsUUID }}
	id, err := {{ idParseExpr $id }}
	if err != nil {
		entapi.WriteProblem(w, http.StatusBadRequest, http.StatusText(http.StatusBadRequest),
			fmt.Errorf("invalid path id %q: %w", r.PathValue("id"), err))
		return
	}
{{- else if $id.Type.Type.Integer }}
	parsedID, err := {{ idParseExpr $id }}
	if err != nil {
		entapi.WriteProblem(w, http.StatusBadRequest, http.StatusText(http.StatusBadRequest),
			fmt.Errorf("invalid path id %q: %w", r.PathValue("id"), err))
		return
	}
	id := {{ $id.Type }}(parsedID)
{{- else }}
	id := {{ $id.Type }}({{ idParseExpr $id }})
{{- end }}

	response, err := h.{{ $op.Field }}(r.Context(), h.client, id, validated)
	if err != nil {
		status := entapi.Status(err, http.StatusUnprocessableEntity)
		entapi.WriteProblem(w, status, http.StatusText(status), err)
		return
	}

	_ = entapi.WriteJSON(w, http.StatusOK, response)
}
{{- else if eq $name "delete" }}

// {{ $op.Function }}Fn is byte-identical to {{ $op.Function }}.
type {{ $op.Function }}Fn func(context.Context, *Client, {{ $id.Type }}) error

func (f {{ $op.Function }}Fn) applyOption(h *APIHandler) {
	if f == nil {
		panic("entapi: {{ $op.Function }}Fn is nil")
	}
	h.{{ $op.Field }} = f
}

func (h *APIHandler) {{ $op.Handler }}(w http.ResponseWriter, r *http.Request) {
{{- if $id.IsUUID }}
	id, err := {{ idParseExpr $id }}
	if err != nil {
		entapi.WriteProblem(w, http.StatusBadRequest, http.StatusText(http.StatusBadRequest),
			fmt.Errorf("invalid path id %q: %w", r.PathValue("id"), err))
		return
	}
{{- else if $id.Type.Type.Integer }}
	parsedID, err := {{ idParseExpr $id }}
	if err != nil {
		entapi.WriteProblem(w, http.StatusBadRequest, http.StatusText(http.StatusBadRequest),
			fmt.Errorf("invalid path id %q: %w", r.PathValue("id"), err))
		return
	}
	id := {{ $id.Type }}(parsedID)
{{- else }}
	id := {{ $id.Type }}({{ idParseExpr $id }})
{{- end }}

	deleteErr := h.{{ $op.Field }}(r.Context(), h.client, id)
	if deleteErr != nil {
		status := http.StatusInternalServerError
		switch {
		case entapi.IsNotFound(deleteErr):
			status = http.StatusNotFound
		case entapi.IsAlreadyExists(deleteErr):
			status = http.StatusConflict
		}
		entapi.WriteProblem(w, status, http.StatusText(status), deleteErr)
		return
	}

	w.WriteHeader(http.StatusNoContent)
}
{{- end }}
{{- end }}

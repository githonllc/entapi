// Code generated by entdomain extension from schema "Article". DO NOT EDIT.
// Source template: templates/wiring.tmpl
// Regenerate with: make generate

package ent

import (
	"context"

	entdomain "github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/wiring/ent/article"
	"github.com/google/uuid"
)

// ============================================================================
// Wiring for Article — one free function per operation.
//
// Free functions, not methods on a generated struct: there is nothing to embed,
// no self-reference to install and no fixed set of override points. A consumer
// who needs different behaviour writes their own function and stops calling the
// generated one, which is a stronger escape hatch than any hook contract — and
// it is why the hook question becomes moot rather than merely redesigned.
//
// The ratio is the point. A generated thirty-line method body is thirty lines in
// which the generator can have guessed wrong and the caller cannot intervene. A
// single call into a routine written once, in Go, for every entity is not.
//
// The identifier type is uuid.UUID because that is what the schema says.
// Nothing below is written for a particular one.
//
// Error classification is deliberately absent: mapping a driver error to
// not-found or already-exists belongs to the runtime and is issue #13. These
// functions return what ent returned.
// ============================================================================

// articleByID fetches one Article through the eager-load plan.
//
// It goes through Query rather than ArticleClient.Get, which is the same
// query with no plan applied and therefore cannot serve a response type that
// declares edges.
func articleByID(db *Client) func(context.Context, uuid.UUID) (*Article, error) {
	return func(ctx context.Context, id uuid.UUID) (*Article, error) {
		return ArticleQueryWithResponseEdges(db.Article.Query()).Where(article.IDEQ(id)).Only(ctx)
	}
}

// articleReloaded converts a just-saved Article by reading it
// back through the eager-load plan.
//
// A mutation builder's Save returns the entity with no edges loaded, and
// ArticleResponse declares edges, so converting that value directly would
// report them as not loaded. The extra round trip is what makes a mutation
// response the same shape as a read.
func articleReloaded(ctx context.Context, db *Client) func(*Article) (*ArticleResponse, error) {
	return func(e *Article) (*ArticleResponse, error) {
		return GetArticle(ctx, db, e.ID)
	}
}

// GetArticle reads one Article and converts it to its response.
func GetArticle(ctx context.Context, db *Client, id uuid.UUID) (*ArticleResponse, error) {
	return entdomain.GetOne(ctx, articleByID(db), NewArticleResponse, id)
}

// ListArticles runs a filtered, ordered, paginated query.
//
// This is the one operation whose body is not a single call, and the reason is
// worth stating rather than hiding: ArticleOrder can fail, because a sort
// key outside ArticleSortKeys has no correct answer and serving a
// differently-ordered page instead is worse than refusing. entdomain.ListPage
// takes resolved order options, not a fallible producer of them, so the check
// happens here.
//
// A nil filter contributes no predicates, so a caller with nothing to filter by
// does not need a branch of its own.
func ListArticles(ctx context.Context, db *Client, f *ArticleFilter, r entdomain.ListRequest) (*entdomain.Page[ArticleResponse], error) {
	order, err := ArticleOrder(r)
	if err != nil {
		return nil, err
	}
	return entdomain.ListPage(ctx, ArticleQueryWithResponseEdges(db.Article.Query()), f.Predicates(), order, r, NewArticleResponse)
}

// CreateArticle inserts one Article and returns its response.
//
// It takes the VALIDATED request. Apply is defined on that type and nowhere
// else, so validation is a compile-time requirement here rather than a step
// this function could forget on the caller's behalf.
func CreateArticle(ctx context.Context, db *Client, v *ValidArticleCreateRequest) (*ArticleResponse, error) {
	return entdomain.SaveOne(ctx, v.Apply(db.Article.Create()), articleReloaded(ctx, db))
}

// UpdateArticle applies a validated patch to one Article.
//
// Fields the patch did not carry are not written at all, so a partial update
// stays partial — that property belongs to Apply, and this function does not
// re-decide it.
func UpdateArticle(ctx context.Context, db *Client, id uuid.UUID, v *ValidArticlePatchRequest) (*ArticleResponse, error) {
	return entdomain.SaveOne(ctx, v.Apply(db.Article.UpdateOneID(id)), articleReloaded(ctx, db))
}

// DeleteArticle removes one Article.
//
// No runtime routine is involved: ent's own builder is already the single line,
// and there is nothing to convert. What "delete" means for an entity with a
// soft-delete column is decided by an ent interceptor or mixin, not by a
// filename convention read out of the schema.
func DeleteArticle(ctx context.Context, db *Client, id uuid.UUID) error {
	return db.Article.DeleteOneID(id).Exec(ctx)
}

// DeleteBatchArticles removes several Articles in one statement and
// returns how many rows it deleted.
//
// The count is returned rather than discarded because ent computes it and the
// caller cannot recover it afterwards: a batch delete reports no error for an
// id that matched nothing, so "how many of the ids existed" is answerable here
// and nowhere else.
//
// An empty list deletes nothing. That is ent's own reading of IDIn with no
// arguments, not a guard written here — a guard would be a second place for the
// rule to live, and the failure it protects against is unrecoverable.
func DeleteBatchArticles(ctx context.Context, db *Client, ids []uuid.UUID) (int, error) {
	return db.Article.Delete().Where(article.IDIn(ids...)).Exec(ctx)
}

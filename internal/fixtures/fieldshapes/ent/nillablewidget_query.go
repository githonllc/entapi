// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/nillablewidget"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/predicate"
	"github.com/google/uuid"
)

// NillableWidgetQuery is the builder for querying NillableWidget entities.
type NillableWidgetQuery struct {
	config
	ctx        *QueryContext
	order      []nillablewidget.OrderOption
	inters     []Interceptor
	predicates []predicate.NillableWidget
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NillableWidgetQuery builder.
func (nwq *NillableWidgetQuery) Where(ps ...predicate.NillableWidget) *NillableWidgetQuery {
	nwq.predicates = append(nwq.predicates, ps...)
	return nwq
}

// Limit the number of records to be returned by this query.
func (nwq *NillableWidgetQuery) Limit(limit int) *NillableWidgetQuery {
	nwq.ctx.Limit = &limit
	return nwq
}

// Offset to start from.
func (nwq *NillableWidgetQuery) Offset(offset int) *NillableWidgetQuery {
	nwq.ctx.Offset = &offset
	return nwq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (nwq *NillableWidgetQuery) Unique(unique bool) *NillableWidgetQuery {
	nwq.ctx.Unique = &unique
	return nwq
}

// Order specifies how the records should be ordered.
func (nwq *NillableWidgetQuery) Order(o ...nillablewidget.OrderOption) *NillableWidgetQuery {
	nwq.order = append(nwq.order, o...)
	return nwq
}

// First returns the first NillableWidget entity from the query.
// Returns a *NotFoundError when no NillableWidget was found.
func (nwq *NillableWidgetQuery) First(ctx context.Context) (*NillableWidget, error) {
	nodes, err := nwq.Limit(1).All(setContextOp(ctx, nwq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{nillablewidget.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (nwq *NillableWidgetQuery) FirstX(ctx context.Context) *NillableWidget {
	node, err := nwq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NillableWidget ID from the query.
// Returns a *NotFoundError when no NillableWidget ID was found.
func (nwq *NillableWidgetQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = nwq.Limit(1).IDs(setContextOp(ctx, nwq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{nillablewidget.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (nwq *NillableWidgetQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := nwq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NillableWidget entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NillableWidget entity is found.
// Returns a *NotFoundError when no NillableWidget entities are found.
func (nwq *NillableWidgetQuery) Only(ctx context.Context) (*NillableWidget, error) {
	nodes, err := nwq.Limit(2).All(setContextOp(ctx, nwq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{nillablewidget.Label}
	default:
		return nil, &NotSingularError{nillablewidget.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (nwq *NillableWidgetQuery) OnlyX(ctx context.Context) *NillableWidget {
	node, err := nwq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NillableWidget ID in the query.
// Returns a *NotSingularError when more than one NillableWidget ID is found.
// Returns a *NotFoundError when no entities are found.
func (nwq *NillableWidgetQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = nwq.Limit(2).IDs(setContextOp(ctx, nwq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{nillablewidget.Label}
	default:
		err = &NotSingularError{nillablewidget.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (nwq *NillableWidgetQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := nwq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NillableWidgets.
func (nwq *NillableWidgetQuery) All(ctx context.Context) ([]*NillableWidget, error) {
	ctx = setContextOp(ctx, nwq.ctx, ent.OpQueryAll)
	if err := nwq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NillableWidget, *NillableWidgetQuery]()
	return withInterceptors[[]*NillableWidget](ctx, nwq, qr, nwq.inters)
}

// AllX is like All, but panics if an error occurs.
func (nwq *NillableWidgetQuery) AllX(ctx context.Context) []*NillableWidget {
	nodes, err := nwq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NillableWidget IDs.
func (nwq *NillableWidgetQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if nwq.ctx.Unique == nil && nwq.path != nil {
		nwq.Unique(true)
	}
	ctx = setContextOp(ctx, nwq.ctx, ent.OpQueryIDs)
	if err = nwq.Select(nillablewidget.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (nwq *NillableWidgetQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := nwq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (nwq *NillableWidgetQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, nwq.ctx, ent.OpQueryCount)
	if err := nwq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, nwq, querierCount[*NillableWidgetQuery](), nwq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (nwq *NillableWidgetQuery) CountX(ctx context.Context) int {
	count, err := nwq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (nwq *NillableWidgetQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, nwq.ctx, ent.OpQueryExist)
	switch _, err := nwq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (nwq *NillableWidgetQuery) ExistX(ctx context.Context) bool {
	exist, err := nwq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NillableWidgetQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (nwq *NillableWidgetQuery) Clone() *NillableWidgetQuery {
	if nwq == nil {
		return nil
	}
	return &NillableWidgetQuery{
		config:     nwq.config,
		ctx:        nwq.ctx.Clone(),
		order:      append([]nillablewidget.OrderOption{}, nwq.order...),
		inters:     append([]Interceptor{}, nwq.inters...),
		predicates: append([]predicate.NillableWidget{}, nwq.predicates...),
		// clone intermediate query.
		sql:  nwq.sql.Clone(),
		path: nwq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Nickname string `json:"nickname,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NillableWidget.Query().
//		GroupBy(nillablewidget.FieldNickname).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (nwq *NillableWidgetQuery) GroupBy(field string, fields ...string) *NillableWidgetGroupBy {
	nwq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NillableWidgetGroupBy{build: nwq}
	grbuild.flds = &nwq.ctx.Fields
	grbuild.label = nillablewidget.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Nickname string `json:"nickname,omitempty"`
//	}
//
//	client.NillableWidget.Query().
//		Select(nillablewidget.FieldNickname).
//		Scan(ctx, &v)
func (nwq *NillableWidgetQuery) Select(fields ...string) *NillableWidgetSelect {
	nwq.ctx.Fields = append(nwq.ctx.Fields, fields...)
	sbuild := &NillableWidgetSelect{NillableWidgetQuery: nwq}
	sbuild.label = nillablewidget.Label
	sbuild.flds, sbuild.scan = &nwq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NillableWidgetSelect configured with the given aggregations.
func (nwq *NillableWidgetQuery) Aggregate(fns ...AggregateFunc) *NillableWidgetSelect {
	return nwq.Select().Aggregate(fns...)
}

func (nwq *NillableWidgetQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range nwq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, nwq); err != nil {
				return err
			}
		}
	}
	for _, f := range nwq.ctx.Fields {
		if !nillablewidget.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if nwq.path != nil {
		prev, err := nwq.path(ctx)
		if err != nil {
			return err
		}
		nwq.sql = prev
	}
	return nil
}

func (nwq *NillableWidgetQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NillableWidget, error) {
	var (
		nodes = []*NillableWidget{}
		_spec = nwq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NillableWidget).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NillableWidget{config: nwq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, nwq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (nwq *NillableWidgetQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := nwq.querySpec()
	_spec.Node.Columns = nwq.ctx.Fields
	if len(nwq.ctx.Fields) > 0 {
		_spec.Unique = nwq.ctx.Unique != nil && *nwq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, nwq.driver, _spec)
}

func (nwq *NillableWidgetQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(nillablewidget.Table, nillablewidget.Columns, sqlgraph.NewFieldSpec(nillablewidget.FieldID, field.TypeUUID))
	_spec.From = nwq.sql
	if unique := nwq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if nwq.path != nil {
		_spec.Unique = true
	}
	if fields := nwq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, nillablewidget.FieldID)
		for i := range fields {
			if fields[i] != nillablewidget.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := nwq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := nwq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := nwq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := nwq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (nwq *NillableWidgetQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(nwq.driver.Dialect())
	t1 := builder.Table(nillablewidget.Table)
	columns := nwq.ctx.Fields
	if len(columns) == 0 {
		columns = nillablewidget.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if nwq.sql != nil {
		selector = nwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if nwq.ctx.Unique != nil && *nwq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range nwq.predicates {
		p(selector)
	}
	for _, p := range nwq.order {
		p(selector)
	}
	if offset := nwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := nwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NillableWidgetGroupBy is the group-by builder for NillableWidget entities.
type NillableWidgetGroupBy struct {
	selector
	build *NillableWidgetQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (nwgb *NillableWidgetGroupBy) Aggregate(fns ...AggregateFunc) *NillableWidgetGroupBy {
	nwgb.fns = append(nwgb.fns, fns...)
	return nwgb
}

// Scan applies the selector query and scans the result into the given value.
func (nwgb *NillableWidgetGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nwgb.build.ctx, ent.OpQueryGroupBy)
	if err := nwgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NillableWidgetQuery, *NillableWidgetGroupBy](ctx, nwgb.build, nwgb, nwgb.build.inters, v)
}

func (nwgb *NillableWidgetGroupBy) sqlScan(ctx context.Context, root *NillableWidgetQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(nwgb.fns))
	for _, fn := range nwgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*nwgb.flds)+len(nwgb.fns))
		for _, f := range *nwgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*nwgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nwgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NillableWidgetSelect is the builder for selecting fields of NillableWidget entities.
type NillableWidgetSelect struct {
	*NillableWidgetQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nws *NillableWidgetSelect) Aggregate(fns ...AggregateFunc) *NillableWidgetSelect {
	nws.fns = append(nws.fns, fns...)
	return nws
}

// Scan applies the selector query and scans the result into the given value.
func (nws *NillableWidgetSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nws.ctx, ent.OpQuerySelect)
	if err := nws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NillableWidgetQuery, *NillableWidgetSelect](ctx, nws.NillableWidgetQuery, nws, nws.inters, v)
}

func (nws *NillableWidgetSelect) sqlScan(ctx context.Context, root *NillableWidgetQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(nws.fns))
	for _, fn := range nws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*nws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

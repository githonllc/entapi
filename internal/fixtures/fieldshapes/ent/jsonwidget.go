// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/jsonwidget"
	"github.com/google/uuid"
)

// JSONWidget is the model entity for the JSONWidget schema.
type JSONWidget struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Tags holds the value of the "tags" field.
	Tags []string `json:"tags,omitempty"`
	// Meta holds the value of the "meta" field.
	Meta map[string]string `json:"meta,omitempty"`
	// RequiredTags holds the value of the "required_tags" field.
	RequiredTags []string `json:"required_tags,omitempty"`
	// RequiredMeta holds the value of the "required_meta" field.
	RequiredMeta map[string]string `json:"required_meta,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JSONWidget) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case jsonwidget.FieldTags, jsonwidget.FieldMeta, jsonwidget.FieldRequiredTags, jsonwidget.FieldRequiredMeta:
			values[i] = new([]byte)
		case jsonwidget.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JSONWidget fields.
func (jw *JSONWidget) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case jsonwidget.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				jw.ID = *value
			}
		case jsonwidget.FieldTags:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &jw.Tags); err != nil {
					return fmt.Errorf("unmarshal field tags: %w", err)
				}
			}
		case jsonwidget.FieldMeta:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field meta", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &jw.Meta); err != nil {
					return fmt.Errorf("unmarshal field meta: %w", err)
				}
			}
		case jsonwidget.FieldRequiredTags:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field required_tags", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &jw.RequiredTags); err != nil {
					return fmt.Errorf("unmarshal field required_tags: %w", err)
				}
			}
		case jsonwidget.FieldRequiredMeta:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field required_meta", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &jw.RequiredMeta); err != nil {
					return fmt.Errorf("unmarshal field required_meta: %w", err)
				}
			}
		default:
			jw.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JSONWidget.
// This includes values selected through modifiers, order, etc.
func (jw *JSONWidget) Value(name string) (ent.Value, error) {
	return jw.selectValues.Get(name)
}

// Update returns a builder for updating this JSONWidget.
// Note that you need to call JSONWidget.Unwrap() before calling this method if this JSONWidget
// was returned from a transaction, and the transaction was committed or rolled back.
func (jw *JSONWidget) Update() *JSONWidgetUpdateOne {
	return NewJSONWidgetClient(jw.config).UpdateOne(jw)
}

// Unwrap unwraps the JSONWidget entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (jw *JSONWidget) Unwrap() *JSONWidget {
	_tx, ok := jw.config.driver.(*txDriver)
	if !ok {
		panic("ent: JSONWidget is not a transactional entity")
	}
	jw.config.driver = _tx.drv
	return jw
}

// String implements the fmt.Stringer.
func (jw *JSONWidget) String() string {
	var builder strings.Builder
	builder.WriteString("JSONWidget(")
	builder.WriteString(fmt.Sprintf("id=%v, ", jw.ID))
	builder.WriteString("tags=")
	builder.WriteString(fmt.Sprintf("%v", jw.Tags))
	builder.WriteString(", ")
	builder.WriteString("meta=")
	builder.WriteString(fmt.Sprintf("%v", jw.Meta))
	builder.WriteString(", ")
	builder.WriteString("required_tags=")
	builder.WriteString(fmt.Sprintf("%v", jw.RequiredTags))
	builder.WriteString(", ")
	builder.WriteString("required_meta=")
	builder.WriteString(fmt.Sprintf("%v", jw.RequiredMeta))
	builder.WriteByte(')')
	return builder.String()
}

// JSONWidgets is a parsable slice of JSONWidget.
type JSONWidgets []*JSONWidget

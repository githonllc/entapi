linters:
  enable:
    - gofmt
    - goimports
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple

linters-settings:
  goimports:
    local-prefixes: github.com/githonllc/entapi

issues:
  exclude-dirs:
    - templates

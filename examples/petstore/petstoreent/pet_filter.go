// Code generated by entapi extension from schema "Pet". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package petstoreent

import (
	"fmt"
	"net/url"
	"sort"
	"strconv"
	"strings"

	"entgo.io/ent/dialect/sql"

	"github.com/githonllc/entapi/examples/petstore/petstoreent/pet"
	"github.com/githonllc/entapi/examples/petstore/petstoreent/predicate"
	entapi "github.com/githonllc/entapi/runtime"
)

// PetFilter is the typed query representation for Pet.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type PetFilter struct {
	// id: int
	ID      []int   `json:"id,omitempty"`
	IDNEQ   []int   `json:"id_neq,omitempty"`
	IDIn    [][]int `json:"id_in,omitempty"`
	IDNotIn [][]int `json:"id_not_in,omitempty"`
	IDGT    []int   `json:"id_gt,omitempty"`
	IDGTE   []int   `json:"id_gte,omitempty"`
	IDLT    []int   `json:"id_lt,omitempty"`
	IDLTE   []int   `json:"id_lte,omitempty"`
	// name: string
	Name             []string   `json:"name,omitempty"`
	NameNEQ          []string   `json:"name_neq,omitempty"`
	NameIn           [][]string `json:"name_in,omitempty"`
	NameNotIn        [][]string `json:"name_not_in,omitempty"`
	NameGT           []string   `json:"name_gt,omitempty"`
	NameGTE          []string   `json:"name_gte,omitempty"`
	NameLT           []string   `json:"name_lt,omitempty"`
	NameLTE          []string   `json:"name_lte,omitempty"`
	NameContains     []string   `json:"name_contains,omitempty"`
	NameHasPrefix    []string   `json:"name_prefix,omitempty"`
	NameHasSuffix    []string   `json:"name_suffix,omitempty"`
	NameContainsFold []string   `json:"name_icontains,omitempty"`
	// status: pet.Status
	Status      []pet.Status   `json:"status,omitempty"`
	StatusNEQ   []pet.Status   `json:"status_neq,omitempty"`
	StatusIn    [][]pet.Status `json:"status_in,omitempty"`
	StatusNotIn [][]pet.Status `json:"status_not_in,omitempty"`
	// price: float64, optional
	Price       []float64   `json:"price,omitempty"`
	PriceNEQ    []float64   `json:"price_neq,omitempty"`
	PriceIn     [][]float64 `json:"price_in,omitempty"`
	PriceNotIn  [][]float64 `json:"price_not_in,omitempty"`
	PriceGT     []float64   `json:"price_gt,omitempty"`
	PriceGTE    []float64   `json:"price_gte,omitempty"`
	PriceLT     []float64   `json:"price_lt,omitempty"`
	PriceLTE    []float64   `json:"price_lte,omitempty"`
	PriceIsNull []bool      `json:"price_is_null,omitempty"`
	// Q is the single free-text term matched across Searchable fields.
	Q *string `json:"_q,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *PetFilter) Predicates() []predicate.Pet {
	if f == nil {
		return nil
	}
	var ps []predicate.Pet
	entapi.AppendEach(&ps, f.ID, pet.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, pet.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, pet.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, pet.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, pet.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, pet.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, pet.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, pet.IDLTE)
	entapi.AppendEach(&ps, f.Name, pet.NameEQ)
	entapi.AppendEach(&ps, f.NameNEQ, pet.NameNEQ)
	entapi.AppendEachSlice(&ps, f.NameIn, pet.NameIn)
	entapi.AppendEachSlice(&ps, f.NameNotIn, pet.NameNotIn)
	entapi.AppendEach(&ps, f.NameGT, pet.NameGT)
	entapi.AppendEach(&ps, f.NameGTE, pet.NameGTE)
	entapi.AppendEach(&ps, f.NameLT, pet.NameLT)
	entapi.AppendEach(&ps, f.NameLTE, pet.NameLTE)
	entapi.AppendEach(&ps, f.NameContains, pet.NameContains)
	entapi.AppendEach(&ps, f.NameHasPrefix, pet.NameHasPrefix)
	entapi.AppendEach(&ps, f.NameHasSuffix, pet.NameHasSuffix)
	entapi.AppendEach(&ps, f.NameContainsFold, pet.NameContainsFold)
	entapi.AppendEach(&ps, f.Status, pet.StatusEQ)
	entapi.AppendEach(&ps, f.StatusNEQ, pet.StatusNEQ)
	entapi.AppendEachSlice(&ps, f.StatusIn, pet.StatusIn)
	entapi.AppendEachSlice(&ps, f.StatusNotIn, pet.StatusNotIn)
	entapi.AppendEach(&ps, f.Price, pet.PriceEQ)
	entapi.AppendEach(&ps, f.PriceNEQ, pet.PriceNEQ)
	entapi.AppendEachSlice(&ps, f.PriceIn, pet.PriceIn)
	entapi.AppendEachSlice(&ps, f.PriceNotIn, pet.PriceNotIn)
	entapi.AppendEach(&ps, f.PriceGT, pet.PriceGT)
	entapi.AppendEach(&ps, f.PriceGTE, pet.PriceGTE)
	entapi.AppendEach(&ps, f.PriceLT, pet.PriceLT)
	entapi.AppendEach(&ps, f.PriceLTE, pet.PriceLTE)
	for _, isNull := range f.PriceIsNull {
		if isNull {
			ps = append(ps, pet.PriceIsNil())
		} else {
			ps = append(ps, pet.PriceNotNil())
		}
	}
	if f.Q != nil && *f.Q != "" {
		ps = append(ps, pet.Or(
			pet.NameContains(*f.Q),
		))
	}
	return ps
}

func parsePetIDQueryValue(raw, whole string) (int, error) {
	parsed, err := strconv.ParseInt(raw, 10, 0)
	if err != nil {
		var zero int
		return zero, fmt.Errorf("%w: field %q value %q is not a valid int: %v", entapi.ErrValidation, "id", whole, err)
	}
	return int(parsed), nil
}

func parsePetNameQueryValue(raw, whole string) (string, error) {
	return string(raw), nil
}

func parsePetStatusQueryValue(raw, whole string) (pet.Status, error) {
	switch raw {
	case "available":
		return pet.Status(raw), nil
	case "pending":
		return pet.Status(raw), nil
	case "sold":
		return pet.Status(raw), nil
	default:
		var zero pet.Status
		return zero, fmt.Errorf("%w: field %q value %q is not an enum member; legal members: %s", entapi.ErrValidation, "status", whole, strings.Join([]string{
			"available",
			"pending",
			"sold",
		}, ", "))
	}
}

func parsePetPriceQueryValue(raw, whole string) (float64, error) {
	parsed, err := strconv.ParseFloat(raw, 64)
	if err != nil {
		var zero float64
		return zero, fmt.Errorf("%w: field %q value %q is not a valid float64: %v", entapi.ErrValidation, "price", whole, err)
	}
	return float64(parsed), nil
}

// ParsePetQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; this generated switch owns the
// field-local operator set, conversion and predicate slots.
func ParsePetQuery(q url.Values) (*PetFilter, entapi.ListRequest, error) {
	f := &PetFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				term := reserved
				f.Q = &term
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.ID = append(f.ID, parsed)
				case "ne":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDNEQ = append(f.IDNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDIn = append(f.IDIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDNotIn = append(f.IDNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGT = append(f.IDGT, parsed)
				case "ge":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "lt":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLT = append(f.IDLT, parsed)
				case "le":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "from":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "to":
					parsed, parseErr := parsePetIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parsePetIDQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parsePetIDQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, lower)
					f.IDLTE = append(f.IDLTE, upper)
				}
			}
		case "name":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "like":
				case "prefix":
				case "suffix":
				case "ilike":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, like, prefix, suffix, ilike, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Name = append(f.Name, parsed)
				case "ne":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameNEQ = append(f.NameNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetNameQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.NameIn = append(f.NameIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetNameQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.NameNotIn = append(f.NameNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGT = append(f.NameGT, parsed)
				case "ge":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, parsed)
				case "lt":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLT = append(f.NameLT, parsed)
				case "le":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLTE = append(f.NameLTE, parsed)
				case "like":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameContains = append(f.NameContains, parsed)
				case "prefix":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameHasPrefix = append(f.NameHasPrefix, parsed)
				case "suffix":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameHasSuffix = append(f.NameHasSuffix, parsed)
				case "ilike":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameContainsFold = append(f.NameContainsFold, parsed)
				case "from":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, parsed)
				case "to":
					parsed, parseErr := parsePetNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLTE = append(f.NameLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parsePetNameQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parsePetNameQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, lower)
					f.NameLTE = append(f.NameLTE, upper)
				}
			}
		case "status":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parsePetStatusQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Status = append(f.Status, parsed)
				case "ne":
					parsed, parseErr := parsePetStatusQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.StatusNEQ = append(f.StatusNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]pet.Status, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetStatusQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.StatusIn = append(f.StatusIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]pet.Status, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetStatusQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.StatusNotIn = append(f.StatusNotIn, parsedValues)
				}
			}
		case "price":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "is_null":
				case "not_null":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, is_null, not_null, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Price = append(f.Price, parsed)
				case "ne":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceNEQ = append(f.PriceNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]float64, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetPriceQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.PriceIn = append(f.PriceIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]float64, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePetPriceQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.PriceNotIn = append(f.PriceNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceGT = append(f.PriceGT, parsed)
				case "ge":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceGTE = append(f.PriceGTE, parsed)
				case "lt":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceLT = append(f.PriceLT, parsed)
				case "le":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceLTE = append(f.PriceLTE, parsed)
				case "is_null":
					if raw != "" {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses %q with a value; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, is_null, not_null, from, to, between")
					}
					f.PriceIsNull = append(f.PriceIsNull, true)
				case "not_null":
					if raw != "" {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses %q with a value; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, is_null, not_null, from, to, between")
					}
					f.PriceIsNull = append(f.PriceIsNull, false)
				case "from":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceGTE = append(f.PriceGTE, parsed)
				case "to":
					parsed, parseErr := parsePetPriceQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceLTE = append(f.PriceLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parsePetPriceQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parsePetPriceQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriceGTE = append(f.PriceGTE, lower)
					f.PriceLTE = append(f.PriceLTE, upper)
				}
			}
		default:
			switch key {
			case "deleted_at":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			case "photo_urls":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			case "created_at":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			case "category_id":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var petSortOptions = map[string]func(...sql.OrderTermOption) pet.OrderOption{
	"id":         pet.ByID,
	"name":       pet.ByName,
	"status":     pet.ByStatus,
	"price":      pet.ByPrice,
	"created_at": pet.ByCreatedAt,
}

// PetSortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var PetSortKeys = []string{
	"id", "name", "status", "price", "created_at",
}

// PetOrder is the single sort allow-list seam.
func PetOrder(r entapi.ListRequest) ([]pet.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []pet.OrderOption{pet.ByID(sql.OrderAsc())}, nil
	}
	orderBy := make([]pet.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := petSortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(PetSortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		orderBy = append(orderBy, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		orderBy = append(orderBy, pet.ByID(lastDir))
	}
	return orderBy, nil
}

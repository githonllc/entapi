// Code generated by entdomain extension from schema "NamedTypeWidget" (entschema/schema/namedtypewidget.go). DO NOT EDIT.
// Source template: backend/pkg/entdomain/templates/base_service.tmpl
// Regenerate with: make generate

package ent

import (
	"context"
	"fmt"

	"github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/namedtypewidget"
	"github.com/google/uuid"
)

// BaseNamedTypeWidgetServiceHooks defines hook extension points for NamedTypeWidget CRUD operations.
// Implement this interface in your service struct and call SetSelf to enable hooks.
type BaseNamedTypeWidgetServiceHooks interface {
	BeforeCreate(ctx context.Context, req *NamedTypeWidgetCreateRequest) error
	AfterCreate(ctx context.Context, entity *NamedTypeWidget) (*NamedTypeWidget, error)
	BeforeUpdate(ctx context.Context, id uuid.UUID, req *NamedTypeWidgetUpdateRequest) error
	AfterUpdate(ctx context.Context, entity *NamedTypeWidget) (*NamedTypeWidget, error)
	BeforeDelete(ctx context.Context, id uuid.UUID) error
	AfterDelete(ctx context.Context, id uuid.UUID) error
}

// BaseNamedTypeWidgetService provides CRUD operations for NamedTypeWidget with Before/After hooks.
// Embed this in your service struct and call SetSelf to enable hook overrides.
//
// Example:
//
//	type myNamedTypeWidgetService struct {
//	    ent.BaseNamedTypeWidgetService
//	}
//
//	func NewMyNamedTypeWidgetService(db *ent.Client) *myNamedTypeWidgetService {
//	    s := &myNamedTypeWidgetService{
//	        BaseNamedTypeWidgetService: ent.BaseNamedTypeWidgetService{DB: db},
//	    }
//	    s.SetSelf(s) // enable hook dispatch to this struct
//	    return s
//	}
//
//	func (s *myNamedTypeWidgetService) BeforeCreate(ctx context.Context, req *NamedTypeWidgetCreateRequest) error {
//	    // custom validation
//	    return nil
//	}
type BaseNamedTypeWidgetService struct {
	DB   *Client
	self BaseNamedTypeWidgetServiceHooks
}

// SetSelf sets the hook receiver. Call this with your embedding struct
// so that hook dispatch goes through your overrides instead of the no-op defaults.
func (s *BaseNamedTypeWidgetService) SetSelf(hooks BaseNamedTypeWidgetServiceHooks) {
	s.self = hooks
}

// hooks returns the hook receiver, falling back to self (no-op defaults) if SetSelf was not called.
func (s *BaseNamedTypeWidgetService) hooks() BaseNamedTypeWidgetServiceHooks {
	if s.self != nil {
		return s.self
	}
	return s
}

// ---------------------------------------------------------------------------
// Default no-op hook implementations
// ---------------------------------------------------------------------------

func (s *BaseNamedTypeWidgetService) BeforeCreate(_ context.Context, _ *NamedTypeWidgetCreateRequest) error {
	return nil
}

func (s *BaseNamedTypeWidgetService) AfterCreate(_ context.Context, entity *NamedTypeWidget) (*NamedTypeWidget, error) {
	return entity, nil
}

func (s *BaseNamedTypeWidgetService) BeforeUpdate(_ context.Context, _ uuid.UUID, _ *NamedTypeWidgetUpdateRequest) error {
	return nil
}

func (s *BaseNamedTypeWidgetService) AfterUpdate(_ context.Context, entity *NamedTypeWidget) (*NamedTypeWidget, error) {
	return entity, nil
}

func (s *BaseNamedTypeWidgetService) BeforeDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

func (s *BaseNamedTypeWidgetService) AfterDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

// ---------------------------------------------------------------------------
// CRUD methods
// ---------------------------------------------------------------------------

// GetByID retrieves a NamedTypeWidget by ID.
func (s *BaseNamedTypeWidgetService) GetByID(ctx context.Context, id uuid.UUID) (*NamedTypeWidget, error) {
	return s.DB.NamedTypeWidget.Get(ctx, id)
}

// Create creates a new NamedTypeWidget from a CreateRequest.
func (s *BaseNamedTypeWidgetService) Create(ctx context.Context, req *NamedTypeWidgetCreateRequest) (*NamedTypeWidget, error) {
	if err := s.hooks().BeforeCreate(ctx, req); err != nil {
		return nil, err
	}

	builder := s.DB.NamedTypeWidget.Create()
	ApplyNamedTypeWidgetCreateRequest(builder, req)

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterCreate(ctx, entity)
}

// Update performs a partial update of NamedTypeWidget, only setting non-nil fields from the request.
func (s *BaseNamedTypeWidgetService) Update(ctx context.Context, id uuid.UUID, req *NamedTypeWidgetUpdateRequest) (*NamedTypeWidget, error) {
	if err := s.hooks().BeforeUpdate(ctx, id, req); err != nil {
		return nil, err
	}

	builder := s.DB.NamedTypeWidget.UpdateOneID(id)
	ApplyNamedTypeWidgetUpdateRequest(builder, req)

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsNotFound(err) {
			return nil, fmt.Errorf("%w: namedtypewidget %s", entdomain.ErrNotFound, id)
		}
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterUpdate(ctx, entity)
}

// Delete deletes a NamedTypeWidget by ID.
func (s *BaseNamedTypeWidgetService) Delete(ctx context.Context, id uuid.UUID) error {
	if err := s.hooks().BeforeDelete(ctx, id); err != nil {
		return err
	}
	err := s.DB.NamedTypeWidget.DeleteOneID(id).Exec(ctx)
	if err != nil {
		if IsNotFound(err) {
			return fmt.Errorf("%w: namedtypewidget %s", entdomain.ErrNotFound, id)
		}
		return err
	}

	return s.hooks().AfterDelete(ctx, id)
}

// DeleteBatch deletes multiple NamedTypeWidgets by IDs.
// NOTE: Before/After hooks are NOT invoked for batch operations.
// If per-item validation is needed, iterate with Delete() instead.
func (s *BaseNamedTypeWidgetService) DeleteBatch(ctx context.Context, ids []uuid.UUID) error {
	if len(ids) == 0 {
		return nil
	}
	_, err := s.DB.NamedTypeWidget.Delete().
		Where(namedtypewidget.IDIn(ids...)).
		Exec(ctx)
	return err
}

// ListWithCursor returns cursor-paginated entities using ID-based ordering.
func (s *BaseNamedTypeWidgetService) ListWithCursor(ctx context.Context, limit int, cursor, order string) ([]*NamedTypeWidget, string, error) {
	query := s.DB.NamedTypeWidget.Query()

	if cursor != "" {
		cursorID, err := uuid.Parse(cursor)
		if err != nil {
			return nil, "", fmt.Errorf("%w: invalid cursor", entdomain.ErrValidation)
		}
		if order == "asc" {
			query = query.Where(namedtypewidget.IDGT(cursorID))
		} else {
			query = query.Where(namedtypewidget.IDLT(cursorID))
		}
	}

	if order == "asc" {
		query = query.Order(Asc(namedtypewidget.FieldID))
	} else {
		query = query.Order(Desc(namedtypewidget.FieldID))
	}

	entities, err := query.Limit(limit + 1).All(ctx)
	if err != nil {
		return nil, "", err
	}

	var nextCursor string
	if len(entities) > limit {
		entities = entities[:limit]
		nextCursor = entities[len(entities)-1].ID.String()
	}

	return entities, nextCursor, nil
}

// ---------------------------------------------------------------------------
// Builder helpers: Apply requests to ent builders
// ---------------------------------------------------------------------------

// ApplyNamedTypeWidgetCreateRequest applies all fields from a CreateRequest to an ent Create builder.
// Exported for use in custom service methods that need manual builder control.
func ApplyNamedTypeWidgetCreateRequest(builder *NamedTypeWidgetCreate, req *NamedTypeWidgetCreateRequest) {
	if req.Labels != nil {
		builder.SetLabels(*req.Labels)
	}
	if req.Attrs != nil {
		builder.SetAttrs(*req.Attrs)
	}
	builder.SetRequiredLabels(req.RequiredLabels)
	builder.SetRequiredAttrs(req.RequiredAttrs)
}

// ApplyNamedTypeWidgetUpdateRequest applies non-nil fields from an UpdateRequest to an ent UpdateOne builder.
// Only fields that are explicitly set (non-nil) in the request are applied — true partial update.
func ApplyNamedTypeWidgetUpdateRequest(builder *NamedTypeWidgetUpdateOne, req *NamedTypeWidgetUpdateRequest) {
	if req.Labels != nil {
		builder.SetLabels(*req.Labels)
	}
	if req.Attrs != nil {
		builder.SetAttrs(*req.Attrs)
	}
	if req.RequiredLabels != nil {
		builder.SetRequiredLabels(*req.RequiredLabels)
	}
	if req.RequiredAttrs != nil {
		builder.SetRequiredAttrs(*req.RequiredAttrs)
	}
}

// ---------------------------------------------------------------------------
// Entity → Response conversion
// ---------------------------------------------------------------------------

// NamedTypeWidgetEntToResponse converts an ent NamedTypeWidget entity directly to a Response DTO.
// Exported for use in service-level queries that bypass the base service.
func NamedTypeWidgetEntToResponse(entity *NamedTypeWidget) *NamedTypeWidgetResponse {
	if entity == nil {
		return nil
	}

	resp := &NamedTypeWidgetResponse{
		ID:             entity.ID,
		Labels:         entdomain.PtrNilSafe(entity.Labels),
		Attrs:          entdomain.PtrNilSafe(entity.Attrs),
		RequiredLabels: entity.RequiredLabels,
		RequiredAttrs:  entity.RequiredAttrs,
	}
	return resp
}

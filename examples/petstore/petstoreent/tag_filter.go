// Code generated by entapi extension from schema "Tag". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package petstoreent

import (
	"fmt"
	"net/url"
	"sort"
	"strconv"
	"strings"

	"entgo.io/ent/dialect/sql"

	"github.com/githonllc/entapi/examples/petstore/petstoreent/predicate"
	"github.com/githonllc/entapi/examples/petstore/petstoreent/tag"
	entapi "github.com/githonllc/entapi/runtime"
)

// TagFilter is the typed query representation for Tag.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type TagFilter struct {
	// id: int
	ID      []int   `json:"id,omitempty"`
	IDNEQ   []int   `json:"id_neq,omitempty"`
	IDIn    [][]int `json:"id_in,omitempty"`
	IDNotIn [][]int `json:"id_not_in,omitempty"`
	IDGT    []int   `json:"id_gt,omitempty"`
	IDGTE   []int   `json:"id_gte,omitempty"`
	IDLT    []int   `json:"id_lt,omitempty"`
	IDLTE   []int   `json:"id_lte,omitempty"`
	// name: string
	Name      []string   `json:"name,omitempty"`
	NameNEQ   []string   `json:"name_neq,omitempty"`
	NameIn    [][]string `json:"name_in,omitempty"`
	NameNotIn [][]string `json:"name_not_in,omitempty"`
	NameGT    []string   `json:"name_gt,omitempty"`
	NameGTE   []string   `json:"name_gte,omitempty"`
	NameLT    []string   `json:"name_lt,omitempty"`
	NameLTE   []string   `json:"name_lte,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *TagFilter) Predicates() []predicate.Tag {
	if f == nil {
		return nil
	}
	var ps []predicate.Tag
	entapi.AppendEach(&ps, f.ID, tag.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, tag.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, tag.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, tag.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, tag.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, tag.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, tag.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, tag.IDLTE)
	entapi.AppendEach(&ps, f.Name, tag.NameEQ)
	entapi.AppendEach(&ps, f.NameNEQ, tag.NameNEQ)
	entapi.AppendEachSlice(&ps, f.NameIn, tag.NameIn)
	entapi.AppendEachSlice(&ps, f.NameNotIn, tag.NameNotIn)
	entapi.AppendEach(&ps, f.NameGT, tag.NameGT)
	entapi.AppendEach(&ps, f.NameGTE, tag.NameGTE)
	entapi.AppendEach(&ps, f.NameLT, tag.NameLT)
	entapi.AppendEach(&ps, f.NameLTE, tag.NameLTE)
	return ps
}

func parseTagIDQueryValue(raw, whole string) (int, error) {
	parsed, err := strconv.ParseInt(raw, 10, 0)
	if err != nil {
		var zero int
		return zero, fmt.Errorf("%w: field %q value %q is not a valid int: %v", entapi.ErrValidation, "id", whole, err)
	}
	return int(parsed), nil
}

func parseTagNameQueryValue(raw, whole string) (string, error) {
	return string(raw), nil
}

// ParseTagQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; this generated switch owns the
// field-local operator set, conversion and predicate slots.
func ParseTagQuery(q url.Values) (*TagFilter, entapi.ListRequest, error) {
	f := &TagFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				err = fmt.Errorf("%w: _q value %q is invalid because Tag has no Searchable fields", entapi.ErrValidation, reserved)
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.ID = append(f.ID, parsed)
				case "ne":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDNEQ = append(f.IDNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTagIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDIn = append(f.IDIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTagIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDNotIn = append(f.IDNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGT = append(f.IDGT, parsed)
				case "ge":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "lt":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLT = append(f.IDLT, parsed)
				case "le":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "from":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "to":
					parsed, parseErr := parseTagIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parseTagIDQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parseTagIDQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, lower)
					f.IDLTE = append(f.IDLTE, upper)
				}
			}
		case "name":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Name = append(f.Name, parsed)
				case "ne":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameNEQ = append(f.NameNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTagNameQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.NameIn = append(f.NameIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTagNameQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.NameNotIn = append(f.NameNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGT = append(f.NameGT, parsed)
				case "ge":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, parsed)
				case "lt":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLT = append(f.NameLT, parsed)
				case "le":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLTE = append(f.NameLTE, parsed)
				case "from":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, parsed)
				case "to":
					parsed, parseErr := parseTagNameQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameLTE = append(f.NameLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parseTagNameQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parseTagNameQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.NameGTE = append(f.NameGTE, lower)
					f.NameLTE = append(f.NameLTE, upper)
				}
			}
		default:
			switch key {
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var tagSortOptions = map[string]func(...sql.OrderTermOption) tag.OrderOption{
	"id": tag.ByID,
}

// TagSortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var TagSortKeys = []string{
	"id",
}

// TagOrder is the single sort allow-list seam.
func TagOrder(r entapi.ListRequest) ([]tag.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []tag.OrderOption{tag.ByID(sql.OrderAsc())}, nil
	}
	orderBy := make([]tag.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := tagSortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(TagSortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		orderBy = append(orderBy, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		orderBy = append(orderBy, tag.ByID(lastDir))
	}
	return orderBy, nil
}

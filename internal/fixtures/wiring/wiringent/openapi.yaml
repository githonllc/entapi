# Code generated by entapi extension for this schema as a whole. DO NOT EDIT.
# Source template: templates/openapi.tmpl
# Regenerate with: make generate
#
# Delete the line above and this file is yours, permanently: cleanup identifies
# what it may remove by that marker and by nothing else.
#
# There is deliberately no `servers` entry. A mount prefix is a deployment fact
# — http.StripPrefix runs in the consumer's main, long after generation, and one
# build can serve at /api/v1 and at the bare root at the same time — so any
# value here could only be a guess. 3.1's default is a relative "/", which is
# the one entry that cannot lie.
#
# Unlike every other file this extension writes, this one has no phase-1 syntax
# gate: the standard library has no YAML parser, so a template bug reaches disk
# and is caught afterwards, by the fixture assertions and the e2e validator.
openapi: 3.1.0
info:
  title: "wiringent API"
  version: "0.0.0"
paths:
  "/articles":
    get:
      operationId: "listArticles"
      summary: "ListArticles"
      tags: ["Article"]
      parameters:
        - name: "id"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on id. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "title"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|like|prefix|suffix|ilike|from|to|between):)?"}
          description: "Filter on title. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, like, prefix, suffix, ilike, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "rank"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|is_null|not_null|from|to|between):)?"}
          description: "Filter on rank. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, is_null, not_null, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "created_at"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on created_at. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "_page"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "One-indexed page number. It must appear at most once; zero and negative values are rejected with 400."
        - name: "_size"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "Page size. It must appear at most once; zero and negative values are rejected with 400. Omitted it is 20, and a value above 1000 is clamped to 1000 rather than refused."
        - name: "_sort"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Comma-separated sort keys, each optionally suffixed with \":asc\" or \":desc\" (ascending by default). Legal keys: id, title, created_at. The primary key is appended as a tiebreak when it is absent from the list. Any other key is rejected with 400."
        - name: "_q"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Free-text term matched as a substring against any of title."
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/ArticleListResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    post:
      operationId: "createArticle"
      summary: "CreateArticle"
      tags: ["Article"]
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/ArticleCreateRequest"}
      responses:
        "201":
          description: "Created"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/ArticleResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/articles/{id}":
    get:
      operationId: "getArticle"
      summary: "GetArticle"
      tags: ["Article"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/ArticleResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    patch:
      operationId: "patchArticle"
      summary: "PatchArticle"
      tags: ["Article"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/ArticlePatchRequest"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/ArticleResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/authors":
    get:
      operationId: "listAuthors"
      summary: "ListAuthors"
      tags: ["Author"]
      parameters:
        - name: "id"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on id. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "name"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|like|prefix|suffix|ilike|from|to|between):)?"}
          description: "Filter on name. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, like, prefix, suffix, ilike, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "_page"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "One-indexed page number. It must appear at most once; zero and negative values are rejected with 400."
        - name: "_size"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "Page size. It must appear at most once; zero and negative values are rejected with 400. Omitted it is 20, and a value above 1000 is clamped to 1000 rather than refused."
        - name: "_sort"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Comma-separated sort keys, each optionally suffixed with \":asc\" or \":desc\" (ascending by default). Legal keys: id, name. The primary key is appended as a tiebreak when it is absent from the list. Any other key is rejected with 400."
        - name: "_q"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Free-text term matched as a substring against any of name."
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/AuthorListResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    post:
      operationId: "createAuthor"
      summary: "CreateAuthor"
      tags: ["Author"]
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/AuthorCreateRequest"}
      responses:
        "201":
          description: "Created"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/AuthorResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/authors/{id}":
    get:
      operationId: "getAuthor"
      summary: "GetAuthor"
      tags: ["Author"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/AuthorResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    patch:
      operationId: "patchAuthor"
      summary: "PatchAuthor"
      tags: ["Author"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/AuthorPatchRequest"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/AuthorResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
    delete:
      operationId: "deleteAuthor"
      summary: "DeleteAuthor"
      tags: ["Author"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "204":
          description: "No Content"
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/notes":
    get:
      operationId: "listNotes"
      summary: "ListNotes"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on id. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "body"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on body. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "_page"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "One-indexed page number. It must appear at most once; zero and negative values are rejected with 400."
        - name: "_size"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "Page size. It must appear at most once; zero and negative values are rejected with 400. Omitted it is 20, and a value above 1000 is clamped to 1000 rather than refused."
        - name: "_sort"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Comma-separated sort keys, each optionally suffixed with \":asc\" or \":desc\" (ascending by default). Legal keys: id, body. The primary key is appended as a tiebreak when it is absent from the list. Any other key is rejected with 400."
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteListResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    post:
      operationId: "createNote"
      summary: "CreateNote"
      tags: ["Note"]
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/NoteCreateRequest"}
      responses:
        "201":
          description: "Created"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/notes/{id}":
    get:
      operationId: "getNote"
      summary: "GetNote"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    patch:
      operationId: "patchNote"
      summary: "PatchNote"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/NotePatchRequest"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
    delete:
      operationId: "deleteNote"
      summary: "DeleteNote"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "204":
          description: "No Content"
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/patchlesses":
    get:
      operationId: "listPatchlesses"
      summary: "ListPatchlesses"
      tags: ["Patchless"]
      parameters:
        - name: "id"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on id. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "_page"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "One-indexed page number. It must appear at most once; zero and negative values are rejected with 400."
        - name: "_size"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "Page size. It must appear at most once; zero and negative values are rejected with 400. Omitted it is 20, and a value above 1000 is clamped to 1000 rather than refused."
        - name: "_sort"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Comma-separated sort keys, each optionally suffixed with \":asc\" or \":desc\" (ascending by default). Legal keys: id. The primary key is appended as a tiebreak when it is absent from the list. Any other key is rejected with 400."
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/PatchlessListResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    post:
      operationId: "createPatchless"
      summary: "CreatePatchless"
      tags: ["Patchless"]
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/PatchlessCreateRequest"}
      responses:
        "201":
          description: "Created"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/PatchlessResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/patchlesses/{id}":
    get:
      operationId: "getPatchless"
      summary: "GetPatchless"
      tags: ["Patchless"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/PatchlessResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    delete:
      operationId: "deletePatchless"
      summary: "DeletePatchless"
      tags: ["Patchless"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "204":
          description: "No Content"
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
components:
  responses:
    Problem400:
      description: "Bad Request"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem404:
      description: "Not Found"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem409:
      description: "Conflict"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem413:
      description: "Request Entity Too Large"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem415:
      description: "Unsupported Media Type"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem422:
      description: "Unprocessable Entity"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem500:
      description: "Internal Server Error"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
  schemas:
    "Problem":
      description: "RFC 9457 problem detail. Every generated error response carries this shape."
      type: object
      properties:
        "type": {"type": "string"}
        "title": {"type": "string"}
        "status": {"type": "integer"}
        "detail": {"type": "string"}
        "field": {"type": "string"}
      required:
        - "type"
        - "title"
        - "status"
    "ArticleSummary":
      description: "Article as it appears on another entity's response. It carries no edges, which is what bounds expansion to one level."
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "title": {"type": "string"}
        "rank": {"type": "integer", "format": "int64"}
        "created_at": {"type": "string", "format": "date-time"}
        "author_id": {"type": "string", "format": "uuid"}
      required:
        - "id"
        - "title"
        - "created_at"
        - "author_id"
    "ArticleResponse":
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "title": {"type": "string"}
        "rank": {"type": "integer", "format": "int64"}
        "created_at": {"type": "string", "format": "date-time"}
        "author_id": {"type": "string", "format": "uuid"}
        "author": {"oneOf": [{"$ref": "#/components/schemas/AuthorSummary"}, {"type": "null"}]}
      required:
        - "id"
        - "title"
        - "created_at"
        - "author_id"
        - "author"
    "ArticleListResponse":
      description: "One page. The envelope is flat: data, total, page and size, with no nested pagination object."
      type: object
      properties:
        "data": {"type": "array", "items": {"$ref": "#/components/schemas/ArticleResponse"}}
        "total": {"type": "integer"}
        "page": {"type": "integer"}
        "size": {"type": "integer"}
      required:
        - "data"
        - "total"
        - "page"
        - "size"
    "ArticleCreateRequest":
      description: "An omitted key leaves the schema's own default in effect. An explicit null counts as omitted: a create request has nothing to clear."
      type: object
      properties:
        "title": {"type": "string"}
        "rank": {"type": "integer", "format": "int64"}
        "author_id": {"type": "string", "format": "uuid"}
      required:
        - "title"
        - "author_id"
    "ArticlePatchRequest":
      description: "An omitted key is left alone. An explicit null clears the field, and is rejected with 422 on a field this schema declares non-nullable."
      type: object
      properties:
        "title": {"type": "string"}
        "rank": {"type": ["integer", "null"], "format": "int64"}
        "author_id": {"type": "string", "format": "uuid"}
    "AuthorSummary":
      description: "Author as it appears on another entity's response. It carries no edges, which is what bounds expansion to one level."
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "name": {"type": "string"}
        "email": {"type": "string"}
      required:
        - "id"
        - "name"
    "AuthorResponse":
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "name": {"type": "string"}
        "email": {"type": "string"}
        "articles": {"type": "array", "items": {"$ref": "#/components/schemas/ArticleSummary"}}
      required:
        - "id"
        - "name"
        - "articles"
    "AuthorListResponse":
      description: "One page. The envelope is flat: data, total, page and size, with no nested pagination object."
      type: object
      properties:
        "data": {"type": "array", "items": {"$ref": "#/components/schemas/AuthorResponse"}}
        "total": {"type": "integer"}
        "page": {"type": "integer"}
        "size": {"type": "integer"}
      required:
        - "data"
        - "total"
        - "page"
        - "size"
    "AuthorCreateRequest":
      description: "An omitted key leaves the schema's own default in effect. An explicit null counts as omitted: a create request has nothing to clear."
      type: object
      properties:
        "name": {"type": "string"}
        "email": {"type": "string"}
      required:
        - "name"
    "AuthorPatchRequest":
      description: "An omitted key is left alone. An explicit null clears the field, and is rejected with 422 on a field this schema declares non-nullable."
      type: object
      properties:
        "name": {"type": "string"}
        "email": {"type": ["string", "null"]}
    "NoteSummary":
      description: "Note as it appears on another entity's response. It carries no edges, which is what bounds expansion to one level."
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "body": {"type": "string"}
      required:
        - "id"
        - "body"
    "NoteResponse":
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "body": {"type": "string"}
      required:
        - "id"
        - "body"
    "NoteListResponse":
      description: "One page. The envelope is flat: data, total, page and size, with no nested pagination object."
      type: object
      properties:
        "data": {"type": "array", "items": {"$ref": "#/components/schemas/NoteResponse"}}
        "total": {"type": "integer"}
        "page": {"type": "integer"}
        "size": {"type": "integer"}
      required:
        - "data"
        - "total"
        - "page"
        - "size"
    "NoteCreateRequest":
      description: "An omitted key leaves the schema's own default in effect. An explicit null counts as omitted: a create request has nothing to clear."
      type: object
      properties:
        "body": {"type": "string"}
      required:
        - "body"
    "NotePatchRequest":
      description: "An omitted key is left alone. An explicit null clears the field, and is rejected with 422 on a field this schema declares non-nullable."
      type: object
      properties:
        "body": {"type": "string"}
    "PatchlessSummary":
      description: "Patchless as it appears on another entity's response. It carries no edges, which is what bounds expansion to one level."
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "label": {"type": "string"}
        "updated_at": {"type": "string", "format": "date-time"}
      required:
        - "id"
        - "label"
        - "updated_at"
    "PatchlessResponse":
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "label": {"type": "string"}
        "updated_at": {"type": "string", "format": "date-time"}
      required:
        - "id"
        - "label"
        - "updated_at"
    "PatchlessListResponse":
      description: "One page. The envelope is flat: data, total, page and size, with no nested pagination object."
      type: object
      properties:
        "data": {"type": "array", "items": {"$ref": "#/components/schemas/PatchlessResponse"}}
        "total": {"type": "integer"}
        "page": {"type": "integer"}
        "size": {"type": "integer"}
      required:
        - "data"
        - "total"
        - "page"
        - "size"
    "PatchlessCreateRequest":
      description: "An omitted key leaves the schema's own default in effect. An explicit null counts as omitted: a create request has nothing to clear."
      type: object
      properties:
        "label": {"type": "string"}
    "PatchlessPatchRequest":
      description: "An omitted key is left alone. An explicit null clears the field, and is rejected with 422 on a field this schema declares non-nullable."
      type: object
      properties: {}

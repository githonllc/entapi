// Code generated by entdomain extension from schema "EnumWidget" (entschema/schema/enumwidget.go). DO NOT EDIT.
// Source template: backend/pkg/entdomain/templates/base_service.tmpl
// Regenerate with: make generate

package ent

import (
	"context"
	"fmt"

	"github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/enumwidget"
	"github.com/google/uuid"
)

// BaseEnumWidgetServiceHooks defines hook extension points for EnumWidget CRUD operations.
// Implement this interface in your service struct and call SetSelf to enable hooks.
type BaseEnumWidgetServiceHooks interface {
	BeforeCreate(ctx context.Context, req *EnumWidgetCreateRequest) error
	AfterCreate(ctx context.Context, entity *EnumWidget) (*EnumWidget, error)
	BeforeUpdate(ctx context.Context, id uuid.UUID, req *EnumWidgetPatchRequest) error
	AfterUpdate(ctx context.Context, entity *EnumWidget) (*EnumWidget, error)
	BeforeDelete(ctx context.Context, id uuid.UUID) error
	AfterDelete(ctx context.Context, id uuid.UUID) error
}

// BaseEnumWidgetService provides CRUD operations for EnumWidget with Before/After hooks.
// Embed this in your service struct and call SetSelf to enable hook overrides.
//
// Example:
//
//	type myEnumWidgetService struct {
//	    ent.BaseEnumWidgetService
//	}
//
//	func NewMyEnumWidgetService(db *ent.Client) *myEnumWidgetService {
//	    s := &myEnumWidgetService{
//	        BaseEnumWidgetService: ent.BaseEnumWidgetService{DB: db},
//	    }
//	    s.SetSelf(s) // enable hook dispatch to this struct
//	    return s
//	}
//
//	func (s *myEnumWidgetService) BeforeCreate(ctx context.Context, req *EnumWidgetCreateRequest) error {
//	    // custom validation
//	    return nil
//	}
type BaseEnumWidgetService struct {
	DB   *Client
	self BaseEnumWidgetServiceHooks
}

// SetSelf sets the hook receiver. Call this with your embedding struct
// so that hook dispatch goes through your overrides instead of the no-op defaults.
func (s *BaseEnumWidgetService) SetSelf(hooks BaseEnumWidgetServiceHooks) {
	s.self = hooks
}

// hooks returns the hook receiver, falling back to self (no-op defaults) if SetSelf was not called.
func (s *BaseEnumWidgetService) hooks() BaseEnumWidgetServiceHooks {
	if s.self != nil {
		return s.self
	}
	return s
}

// ---------------------------------------------------------------------------
// Default no-op hook implementations
// ---------------------------------------------------------------------------

func (s *BaseEnumWidgetService) BeforeCreate(_ context.Context, _ *EnumWidgetCreateRequest) error {
	return nil
}

func (s *BaseEnumWidgetService) AfterCreate(_ context.Context, entity *EnumWidget) (*EnumWidget, error) {
	return entity, nil
}

func (s *BaseEnumWidgetService) BeforeUpdate(_ context.Context, _ uuid.UUID, _ *EnumWidgetPatchRequest) error {
	return nil
}

func (s *BaseEnumWidgetService) AfterUpdate(_ context.Context, entity *EnumWidget) (*EnumWidget, error) {
	return entity, nil
}

func (s *BaseEnumWidgetService) BeforeDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

func (s *BaseEnumWidgetService) AfterDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

// ---------------------------------------------------------------------------
// CRUD methods
// ---------------------------------------------------------------------------

// GetByID retrieves a EnumWidget by ID.
func (s *BaseEnumWidgetService) GetByID(ctx context.Context, id uuid.UUID) (*EnumWidget, error) {
	return s.DB.EnumWidget.Get(ctx, id)
}

// Create creates a new EnumWidget from a CreateRequest.
//
// Validation is not optional here, and not because this method remembers to
// call it: Apply is defined on the validated request only, so there is no
// version of this method that skips it and still compiles.
func (s *BaseEnumWidgetService) Create(ctx context.Context, req *EnumWidgetCreateRequest) (*EnumWidget, error) {
	if err := s.hooks().BeforeCreate(ctx, req); err != nil {
		return nil, err
	}

	valid, err := req.Validate()
	if err != nil {
		return nil, err
	}
	builder := valid.Apply(s.DB.EnumWidget.Create())

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterCreate(ctx, entity)
}

// Update performs a partial update of EnumWidget: a field the caller omitted
// is left alone, an explicit null clears a clearable field, and a value sets it.
func (s *BaseEnumWidgetService) Update(ctx context.Context, id uuid.UUID, req *EnumWidgetPatchRequest) (*EnumWidget, error) {
	if err := s.hooks().BeforeUpdate(ctx, id, req); err != nil {
		return nil, err
	}

	valid, err := req.Validate()
	if err != nil {
		return nil, err
	}
	builder := valid.Apply(s.DB.EnumWidget.UpdateOneID(id))

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsNotFound(err) {
			return nil, fmt.Errorf("%w: enumwidget %s", entdomain.ErrNotFound, id)
		}
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterUpdate(ctx, entity)
}

// Delete deletes a EnumWidget by ID.
func (s *BaseEnumWidgetService) Delete(ctx context.Context, id uuid.UUID) error {
	if err := s.hooks().BeforeDelete(ctx, id); err != nil {
		return err
	}
	err := s.DB.EnumWidget.DeleteOneID(id).Exec(ctx)
	if err != nil {
		if IsNotFound(err) {
			return fmt.Errorf("%w: enumwidget %s", entdomain.ErrNotFound, id)
		}
		return err
	}

	return s.hooks().AfterDelete(ctx, id)
}

// DeleteBatch deletes multiple EnumWidgets by IDs.
// NOTE: Before/After hooks are NOT invoked for batch operations.
// If per-item validation is needed, iterate with Delete() instead.
func (s *BaseEnumWidgetService) DeleteBatch(ctx context.Context, ids []uuid.UUID) error {
	if len(ids) == 0 {
		return nil
	}
	_, err := s.DB.EnumWidget.Delete().
		Where(enumwidget.IDIn(ids...)).
		Exec(ctx)
	return err
}

// ListWithCursor returns cursor-paginated entities using ID-based ordering.
func (s *BaseEnumWidgetService) ListWithCursor(ctx context.Context, limit int, cursor, order string) ([]*EnumWidget, string, error) {
	query := s.DB.EnumWidget.Query()

	if cursor != "" {
		cursorID, err := uuid.Parse(cursor)
		if err != nil {
			return nil, "", fmt.Errorf("%w: invalid cursor", entdomain.ErrValidation)
		}
		if order == "asc" {
			query = query.Where(enumwidget.IDGT(cursorID))
		} else {
			query = query.Where(enumwidget.IDLT(cursorID))
		}
	}

	if order == "asc" {
		query = query.Order(Asc(enumwidget.FieldID))
	} else {
		query = query.Order(Desc(enumwidget.FieldID))
	}

	entities, err := query.Limit(limit + 1).All(ctx)
	if err != nil {
		return nil, "", err
	}

	var nextCursor string
	if len(entities) > limit {
		entities = entities[:limit]
		nextCursor = entities[len(entities)-1].ID.String()
	}

	return entities, nextCursor, nil
}

// ---------------------------------------------------------------------------
// Builder helpers: Apply requests to ent builders
//
// There are none any more, deliberately. ApplyEnumWidgetCreateRequest and
// ApplyEnumWidgetUpdateRequest used to be exported free functions taking a raw
// request, which is exactly the path that let a caller reach a builder without
// validating. Apply now lives on ValidEnumWidgetCreateRequest and
// ValidEnumWidgetPatchRequest (see enumwidget_dto.go), so custom
// service methods call req.Validate() first and get the same builder control.
// ---------------------------------------------------------------------------

// ---------------------------------------------------------------------------
// Entity → Response conversion
// ---------------------------------------------------------------------------

// EnumWidgetEntToResponse converts an ent EnumWidget entity directly to a Response DTO.
// Exported for use in service-level queries that bypass the base service.
func EnumWidgetEntToResponse(entity *EnumWidget) *EnumWidgetResponse {
	if entity == nil {
		return nil
	}

	resp := &EnumWidgetResponse{
		ID:     entity.ID,
		Status: entity.Status,
		Tier:   entdomain.PtrOrNil(entity.Tier),
	}
	return resp
}

// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/fieldshapesent"
)

// The EnumWidgetFunc type is an adapter to allow the use of ordinary
// function as EnumWidget mutator.
type EnumWidgetFunc func(context.Context, *fieldshapesent.EnumWidgetMutation) (fieldshapesent.Value, error)

// Mutate calls f(ctx, m).
func (f EnumWidgetFunc) Mutate(ctx context.Context, m fieldshapesent.Mutation) (fieldshapesent.Value, error) {
	if mv, ok := m.(*fieldshapesent.EnumWidgetMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *fieldshapesent.EnumWidgetMutation", m)
}

// The JSONWidgetFunc type is an adapter to allow the use of ordinary
// function as JSONWidget mutator.
type JSONWidgetFunc func(context.Context, *fieldshapesent.JSONWidgetMutation) (fieldshapesent.Value, error)

// Mutate calls f(ctx, m).
func (f JSONWidgetFunc) Mutate(ctx context.Context, m fieldshapesent.Mutation) (fieldshapesent.Value, error) {
	if mv, ok := m.(*fieldshapesent.JSONWidgetMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *fieldshapesent.JSONWidgetMutation", m)
}

// The NamedTypeWidgetFunc type is an adapter to allow the use of ordinary
// function as NamedTypeWidget mutator.
type NamedTypeWidgetFunc func(context.Context, *fieldshapesent.NamedTypeWidgetMutation) (fieldshapesent.Value, error)

// Mutate calls f(ctx, m).
func (f NamedTypeWidgetFunc) Mutate(ctx context.Context, m fieldshapesent.Mutation) (fieldshapesent.Value, error) {
	if mv, ok := m.(*fieldshapesent.NamedTypeWidgetMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *fieldshapesent.NamedTypeWidgetMutation", m)
}

// The NillableWidgetFunc type is an adapter to allow the use of ordinary
// function as NillableWidget mutator.
type NillableWidgetFunc func(context.Context, *fieldshapesent.NillableWidgetMutation) (fieldshapesent.Value, error)

// Mutate calls f(ctx, m).
func (f NillableWidgetFunc) Mutate(ctx context.Context, m fieldshapesent.Mutation) (fieldshapesent.Value, error) {
	if mv, ok := m.(*fieldshapesent.NillableWidgetMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *fieldshapesent.NillableWidgetMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, fieldshapesent.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m fieldshapesent.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m fieldshapesent.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m fieldshapesent.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op fieldshapesent.Op) Condition {
	return func(_ context.Context, m fieldshapesent.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m fieldshapesent.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m fieldshapesent.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m fieldshapesent.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk fieldshapesent.Hook, cond Condition) fieldshapesent.Hook {
	return func(next fieldshapesent.Mutator) fieldshapesent.Mutator {
		return fieldshapesent.MutateFunc(func(ctx context.Context, m fieldshapesent.Mutation) (fieldshapesent.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, fieldshapesent.Delete|fieldshapesent.Create)
func On(hk fieldshapesent.Hook, op fieldshapesent.Op) fieldshapesent.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, fieldshapesent.Update|fieldshapesent.UpdateOne)
func Unless(hk fieldshapesent.Hook, op fieldshapesent.Op) fieldshapesent.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) fieldshapesent.Hook {
	return func(fieldshapesent.Mutator) fieldshapesent.Mutator {
		return fieldshapesent.MutateFunc(func(context.Context, fieldshapesent.Mutation) (fieldshapesent.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []fieldshapesent.Hook {
//		return []fieldshapesent.Hook{
//			Reject(fieldshapesent.Delete|fieldshapesent.Update),
//		}
//	}
func Reject(op fieldshapesent.Op) fieldshapesent.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []fieldshapesent.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...fieldshapesent.Hook) Chain {
	return Chain{append([]fieldshapesent.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() fieldshapesent.Hook {
	return func(mutator fieldshapesent.Mutator) fieldshapesent.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...fieldshapesent.Hook) Chain {
	newHooks := make([]fieldshapesent.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}

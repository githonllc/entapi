# entdomain

Ent 之上的 CRUD 面生成器：一次性 schema 声明 → 生成 DTO、查询面、wiring 与（规划中的）HTTP 层。
本文件只是术语表——实现细节住在 docs/，裁决住在 docs/adr/ 与 DESIGN 文档。

## Language

### 注解模型（v-next，DESIGN-v3 拷问中）

**资源（Resource）**:
被 `api.Resource()` 标记、由此获得完整生成 CRUD 面的实体。实体级唯一开关。
_Avoid_: entity annotation, DomainConfig

**沉默（Silence）**:
字段的默认状态——零注解，HTTP 面形状完全由 ent 事实（Optional/Immutable/Default/Sensitive/类型）推导。
_Avoid_: default scopes

**偏离（Deviation）**:
ent 事实推不出的意图，以字段级注解显式声明。v-next 词表恰好五个：Hidden / ReadOnly / Searchable / Filterable / Sortable。
_Avoid_: scope, DomainField

**Hidden**:
只在 service 层存在的字段——不进任何 HTTP 面（请求、响应、查询）。
_Avoid_: internal, excluded

**ReadOnly**:
服务端管理的字段——进响应，永不进 create/patch 请求。审计字段（created_at 等）的显式声明，不靠 Default+Immutable 推导。
_Avoid_: OutputOnly, AuditLog

**查询三维度**:
Searchable（子串扫描）/ Filterable（算子参数）/ Sortable（排序白名单）。始终显式标记，永不推导（ADR-0005）。

**Expand**:
边级唯一偏离词——把边选入响应（目标实体的 Summary，一层）。默认不展开；永不从外键位置推导；目标非 Resource 即拒绝。
_Avoid_: DomainEdge, eager edge

**Except**:
Resource 的操作子集选项——`api.Resource().Except(api.OpDelete, …)` 关掉指定 HTTP 端点。全字段 Immutable 而未 Except 更新操作 → 生成期拒绝。

**换脑（With-override）**:
按操作整单元替换默认实现：槽位类型与生成的 wiring 函数签名逐字相同，默认值即 wiring 函数本身。生成代码调用你，你从不嵌入它。
_Avoid_: override, embed, SetSelf

**external 门**:
非 CRUD 端点的出路——消费者直接在自己的 mux 上注册，框架零机制。生成的 DTO/校验照用。

### 查询线格式（op-in-value，源自 entigo）

**op-in-value**:
过滤参数的线上语法：`field=op:value`，按第一个冒号切分算子与值（`age=gt:30`、`name=ilike:simon`）。裸值与未知前缀都回落为 eq 字面值（`title=12:30` 可用）；已知算子打在不允许它的字段上是 400（门控大声）；裸空值忽略、显式 `eq:` 匹配空串兼作字面值转义舱。算子集在 codegen 期定死。
_Avoid_: filter DSL, query language

**保留参数命名空间**:
`_` 前缀是框架的、裸名是 schema 的——恰四个保留参数 `_sort`/`_page`/`_size`/`_q`，无别名。叫 `size`/`sort` 的字段照常过滤，零歧义零特判。`_` 开头的字段名进查询面 → 生成期拒绝。
_Avoid_: 裸 sort/page/size/q 作保留参数, $ 前缀

**_sort**:
`_sort=field:desc,field2`——逗号分隔多字段，裸字段名即 asc，白名单只来自 Sortable 标记，非法字段 400，PK tiebreak 永远追加在末尾（ADR-0002）。
_Avoid_: sort_by, order

**_q**:
自由文本析取——对全部 Searchable 字段做 Contains 后 OR。与 Filterable 的结构化 AND 面是两个查询面，不合并（ADR-0005）。
_Avoid_: search, keyword

### 生成物

**marker（所有权标记）**:
生成文件首行的 `Code generated by entdomain extension` 注释。它是所有权契约：cleanup 只删带 marker 的文件；消费者删掉这一行，文件永远归其所有（ADR-0004）。
_Avoid_: header comment

**wiring**:
每实体六个操作的自由函数层（List/Get/Create/Update/Delete/DeleteBatch），接受 validated request，经 ErrorMap 恰好映射一次错误。
_Avoid_: service, base service

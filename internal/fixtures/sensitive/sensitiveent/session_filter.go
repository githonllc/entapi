// Code generated by entapi extension from schema "Session". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package sensitiveent

import (
	"encoding"
	"fmt"
	"net/url"
	"sort"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entapi/internal/fixtures/sensitive/sensitiveent/predicate"
	"github.com/githonllc/entapi/internal/fixtures/sensitive/sensitiveent/session"
	entapi "github.com/githonllc/entapi/runtime"
	"github.com/google/uuid"
)

// SessionFilter is the typed query representation for Session.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type SessionFilter struct {
	// id: uuid.UUID
	ID      []uuid.UUID   `json:"id,omitempty"`
	IDNEQ   []uuid.UUID   `json:"id_neq,omitempty"`
	IDIn    [][]uuid.UUID `json:"id_in,omitempty"`
	IDNotIn [][]uuid.UUID `json:"id_not_in,omitempty"`
	IDGT    []uuid.UUID   `json:"id_gt,omitempty"`
	IDGTE   []uuid.UUID   `json:"id_gte,omitempty"`
	IDLT    []uuid.UUID   `json:"id_lt,omitempty"`
	IDLTE   []uuid.UUID   `json:"id_lte,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *SessionFilter) Predicates() []predicate.Session {
	if f == nil {
		return nil
	}
	var ps []predicate.Session
	entapi.AppendEach(&ps, f.ID, session.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, session.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, session.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, session.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, session.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, session.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, session.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, session.IDLTE)
	return ps
}

func parseSessionIDQueryValue(raw, whole string) (uuid.UUID, error) {
	var parsed uuid.UUID
	if err := encoding.TextUnmarshaler(&parsed).UnmarshalText([]byte(raw)); err != nil {
		return parsed, fmt.Errorf("%w: field %q value %q is invalid: %v", entapi.ErrValidation, "id", whole, err)
	}
	return parsed, nil
}

// ParseSessionQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; the generated operator tables own
// the field-local operator set, conversion and predicate slots.
func ParseSessionQuery(q url.Values) (*SessionFilter, entapi.ListRequest, error) {
	f := &SessionFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				err = fmt.Errorf("%w: _q value %q is invalid because Session has no Searchable fields", entapi.ErrValidation, reserved)
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			if err := entapi.ParseFieldValues("id", values, false, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between", parseSessionIDQueryValue, []entapi.QueryOp[uuid.UUID]{
				{Prefix: "eq", Kind: entapi.OpKindValue, One: &f.ID},
				{Prefix: "ne", Kind: entapi.OpKindValue, One: &f.IDNEQ},
				{Prefix: "in", Kind: entapi.OpKindList, Many: &f.IDIn},
				{Prefix: "not_in", Kind: entapi.OpKindList, Many: &f.IDNotIn},
				{Prefix: "gt", Kind: entapi.OpKindValue, One: &f.IDGT},
				{Prefix: "ge", Kind: entapi.OpKindValue, One: &f.IDGTE},
				{Prefix: "lt", Kind: entapi.OpKindValue, One: &f.IDLT},
				{Prefix: "le", Kind: entapi.OpKindValue, One: &f.IDLTE},
				{Prefix: "from", Kind: entapi.OpKindValue, One: &f.IDGTE},
				{Prefix: "to", Kind: entapi.OpKindValue, One: &f.IDLTE},
				{Prefix: "between", Kind: entapi.OpKindRange, One: &f.IDGTE, Second: &f.IDLTE},
			}); err != nil {
				return nil, entapi.ListRequest{}, err
			}
		default:
			switch key {
			case "user_agent":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var sessionSortOptions = map[string]func(...sql.OrderTermOption) session.OrderOption{
	"id": session.ByID,
}

// SessionSortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var SessionSortKeys = []string{
	"id",
}

// SessionOrder is the single sort allow-list seam.
func SessionOrder(r entapi.ListRequest) ([]session.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []session.OrderOption{session.ByID(sql.OrderAsc())}, nil
	}
	orderBy := make([]session.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := sessionSortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(SessionSortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		orderBy = append(orderBy, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		orderBy = append(orderBy, session.ByID(lastDir))
	}
	return orderBy, nil
}

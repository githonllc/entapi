// Code generated by ent, DO NOT EDIT.

package queryent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/githonllc/entapi/internal/fixtures/query/queryent/plain"
	"github.com/githonllc/entapi/internal/fixtures/query/queryent/predicate"
)

// PlainUpdate is the builder for updating Plain entities.
type PlainUpdate struct {
	config
	hooks    []Hook
	mutation *PlainMutation
}

// Where appends a list predicates to the PlainUpdate builder.
func (pu *PlainUpdate) Where(ps ...predicate.Plain) *PlainUpdate {
	pu.mutation.Where(ps...)
	return pu
}

// SetLabel sets the "label" field.
func (pu *PlainUpdate) SetLabel(s string) *PlainUpdate {
	pu.mutation.SetLabel(s)
	return pu
}

// SetNillableLabel sets the "label" field if the given value is not nil.
func (pu *PlainUpdate) SetNillableLabel(s *string) *PlainUpdate {
	if s != nil {
		pu.SetLabel(*s)
	}
	return pu
}

// Mutation returns the PlainMutation object of the builder.
func (pu *PlainUpdate) Mutation() *PlainMutation {
	return pu.mutation
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (pu *PlainUpdate) Save(ctx context.Context) (int, error) {
	return withHooks(ctx, pu.sqlSave, pu.mutation, pu.hooks)
}

// SaveX is like Save, but panics if an error occurs.
func (pu *PlainUpdate) SaveX(ctx context.Context) int {
	affected, err := pu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (pu *PlainUpdate) Exec(ctx context.Context) error {
	_, err := pu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pu *PlainUpdate) ExecX(ctx context.Context) {
	if err := pu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (pu *PlainUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := sqlgraph.NewUpdateSpec(plain.Table, plain.Columns, sqlgraph.NewFieldSpec(plain.FieldID, field.TypeUUID))
	if ps := pu.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := pu.mutation.Label(); ok {
		_spec.SetField(plain.FieldLabel, field.TypeString, value)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, pu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{plain.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return 0, err
	}
	pu.mutation.done = true
	return n, nil
}

// PlainUpdateOne is the builder for updating a single Plain entity.
type PlainUpdateOne struct {
	config
	fields   []string
	hooks    []Hook
	mutation *PlainMutation
}

// SetLabel sets the "label" field.
func (puo *PlainUpdateOne) SetLabel(s string) *PlainUpdateOne {
	puo.mutation.SetLabel(s)
	return puo
}

// SetNillableLabel sets the "label" field if the given value is not nil.
func (puo *PlainUpdateOne) SetNillableLabel(s *string) *PlainUpdateOne {
	if s != nil {
		puo.SetLabel(*s)
	}
	return puo
}

// Mutation returns the PlainMutation object of the builder.
func (puo *PlainUpdateOne) Mutation() *PlainMutation {
	return puo.mutation
}

// Where appends a list predicates to the PlainUpdate builder.
func (puo *PlainUpdateOne) Where(ps ...predicate.Plain) *PlainUpdateOne {
	puo.mutation.Where(ps...)
	return puo
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (puo *PlainUpdateOne) Select(field string, fields ...string) *PlainUpdateOne {
	puo.fields = append([]string{field}, fields...)
	return puo
}

// Save executes the query and returns the updated Plain entity.
func (puo *PlainUpdateOne) Save(ctx context.Context) (*Plain, error) {
	return withHooks(ctx, puo.sqlSave, puo.mutation, puo.hooks)
}

// SaveX is like Save, but panics if an error occurs.
func (puo *PlainUpdateOne) SaveX(ctx context.Context) *Plain {
	node, err := puo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (puo *PlainUpdateOne) Exec(ctx context.Context) error {
	_, err := puo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (puo *PlainUpdateOne) ExecX(ctx context.Context) {
	if err := puo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (puo *PlainUpdateOne) sqlSave(ctx context.Context) (_node *Plain, err error) {
	_spec := sqlgraph.NewUpdateSpec(plain.Table, plain.Columns, sqlgraph.NewFieldSpec(plain.FieldID, field.TypeUUID))
	id, ok := puo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "id", err: errors.New(`queryent: missing "Plain.id" for update`)}
	}
	_spec.Node.ID.Value = id
	if fields := puo.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, plain.FieldID)
		for _, f := range fields {
			if !plain.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("queryent: invalid field %q for query", f)}
			}
			if f != plain.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := puo.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := puo.mutation.Label(); ok {
		_spec.SetField(plain.FieldLabel, field.TypeString, value)
	}
	_node = &Plain{config: puo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, puo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{plain.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	puo.mutation.done = true
	return _node, nil
}

// Code generated by entapi extension from schema "Secret". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package edgesent

import (
	"encoding"
	"fmt"
	"net/url"
	"sort"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entapi/internal/fixtures/edges/edgesent/predicate"
	"github.com/githonllc/entapi/internal/fixtures/edges/edgesent/secret"
	entapi "github.com/githonllc/entapi/runtime"
	"github.com/google/uuid"
)

// SecretFilter is the typed query representation for Secret.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type SecretFilter struct {
	// id: uuid.UUID
	ID      []uuid.UUID   `json:"id,omitempty"`
	IDNEQ   []uuid.UUID   `json:"id_neq,omitempty"`
	IDIn    [][]uuid.UUID `json:"id_in,omitempty"`
	IDNotIn [][]uuid.UUID `json:"id_not_in,omitempty"`
	IDGT    []uuid.UUID   `json:"id_gt,omitempty"`
	IDGTE   []uuid.UUID   `json:"id_gte,omitempty"`
	IDLT    []uuid.UUID   `json:"id_lt,omitempty"`
	IDLTE   []uuid.UUID   `json:"id_lte,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *SecretFilter) Predicates() []predicate.Secret {
	if f == nil {
		return nil
	}
	var ps []predicate.Secret
	entapi.AppendEach(&ps, f.ID, secret.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, secret.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, secret.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, secret.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, secret.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, secret.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, secret.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, secret.IDLTE)
	return ps
}

func parseSecretIDQueryValue(raw, whole string) (uuid.UUID, error) {
	var parsed uuid.UUID
	if err := encoding.TextUnmarshaler(&parsed).UnmarshalText([]byte(raw)); err != nil {
		return parsed, fmt.Errorf("%w: field %q value %q is invalid: %v", entapi.ErrValidation, "id", whole, err)
	}
	return parsed, nil
}

// ParseSecretQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; this generated switch owns the
// field-local operator set, conversion and predicate slots.
func ParseSecretQuery(q url.Values) (*SecretFilter, entapi.ListRequest, error) {
	f := &SecretFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				err = fmt.Errorf("%w: _q value %q is invalid because Secret has no Searchable fields", entapi.ErrValidation, reserved)
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			if err := entapi.ParseFieldValues("id", values, false, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between", parseSecretIDQueryValue, []entapi.QueryOp[uuid.UUID]{
				{Prefix: "eq", Kind: entapi.OpKindValue, One: &f.ID},
				{Prefix: "ne", Kind: entapi.OpKindValue, One: &f.IDNEQ},
				{Prefix: "in", Kind: entapi.OpKindList, Many: &f.IDIn},
				{Prefix: "not_in", Kind: entapi.OpKindList, Many: &f.IDNotIn},
				{Prefix: "gt", Kind: entapi.OpKindValue, One: &f.IDGT},
				{Prefix: "ge", Kind: entapi.OpKindValue, One: &f.IDGTE},
				{Prefix: "lt", Kind: entapi.OpKindValue, One: &f.IDLT},
				{Prefix: "le", Kind: entapi.OpKindValue, One: &f.IDLTE},
				{Prefix: "from", Kind: entapi.OpKindValue, One: &f.IDGTE},
				{Prefix: "to", Kind: entapi.OpKindValue, One: &f.IDLTE},
				{Prefix: "between", Kind: entapi.OpKindRange, One: &f.IDGTE, Second: &f.IDLTE},
			}); err != nil {
				return nil, entapi.ListRequest{}, err
			}
		default:
			switch key {
			case "token":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var secretSortOptions = map[string]func(...sql.OrderTermOption) secret.OrderOption{
	"id": secret.ByID,
}

// SecretSortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var SecretSortKeys = []string{
	"id",
}

// SecretOrder is the single sort allow-list seam.
func SecretOrder(r entapi.ListRequest) ([]secret.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []secret.OrderOption{secret.ByID(sql.OrderAsc())}, nil
	}
	orderBy := make([]secret.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := secretSortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(SecretSortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		orderBy = append(orderBy, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		orderBy = append(orderBy, secret.ByID(lastDir))
	}
	return orderBy, nil
}

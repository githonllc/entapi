// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/nillablewidget"
	"github.com/google/uuid"
)

// NillableWidget is the model entity for the NillableWidget schema.
type NillableWidget struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Nickname holds the value of the "nickname" field.
	Nickname *string `json:"nickname,omitempty"`
	// Handle holds the value of the "handle" field.
	Handle *string `json:"handle,omitempty"`
	// Quota holds the value of the "quota" field.
	Quota        *int `json:"quota,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NillableWidget) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case nillablewidget.FieldQuota:
			values[i] = new(sql.NullInt64)
		case nillablewidget.FieldNickname, nillablewidget.FieldHandle:
			values[i] = new(sql.NullString)
		case nillablewidget.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NillableWidget fields.
func (nw *NillableWidget) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case nillablewidget.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				nw.ID = *value
			}
		case nillablewidget.FieldNickname:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field nickname", values[i])
			} else if value.Valid {
				nw.Nickname = new(string)
				*nw.Nickname = value.String
			}
		case nillablewidget.FieldHandle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field handle", values[i])
			} else if value.Valid {
				nw.Handle = new(string)
				*nw.Handle = value.String
			}
		case nillablewidget.FieldQuota:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quota", values[i])
			} else if value.Valid {
				nw.Quota = new(int)
				*nw.Quota = int(value.Int64)
			}
		default:
			nw.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NillableWidget.
// This includes values selected through modifiers, order, etc.
func (nw *NillableWidget) Value(name string) (ent.Value, error) {
	return nw.selectValues.Get(name)
}

// Update returns a builder for updating this NillableWidget.
// Note that you need to call NillableWidget.Unwrap() before calling this method if this NillableWidget
// was returned from a transaction, and the transaction was committed or rolled back.
func (nw *NillableWidget) Update() *NillableWidgetUpdateOne {
	return NewNillableWidgetClient(nw.config).UpdateOne(nw)
}

// Unwrap unwraps the NillableWidget entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (nw *NillableWidget) Unwrap() *NillableWidget {
	_tx, ok := nw.config.driver.(*txDriver)
	if !ok {
		panic("ent: NillableWidget is not a transactional entity")
	}
	nw.config.driver = _tx.drv
	return nw
}

// String implements the fmt.Stringer.
func (nw *NillableWidget) String() string {
	var builder strings.Builder
	builder.WriteString("NillableWidget(")
	builder.WriteString(fmt.Sprintf("id=%v, ", nw.ID))
	if v := nw.Nickname; v != nil {
		builder.WriteString("nickname=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := nw.Handle; v != nil {
		builder.WriteString("handle=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := nw.Quota; v != nil {
		builder.WriteString("quota=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteByte(')')
	return builder.String()
}

// NillableWidgets is a parsable slice of NillableWidget.
type NillableWidgets []*NillableWidget

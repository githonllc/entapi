# Code generated by entapi extension for this schema as a whole. DO NOT EDIT.
# Source template: templates/openapi.tmpl
# Regenerate with: make generate
#
# Delete the line above and this file is yours, permanently: cleanup identifies
# what it may remove by that marker and by nothing else.
#
# There is deliberately no `servers` entry. A mount prefix is a deployment fact
# — http.StripPrefix runs in the consumer's main, long after generation, and one
# build can serve at /api/v1 and at the bare root at the same time — so any
# value here could only be a guess. 3.1's default is a relative "/", which is
# the one entry that cannot lie.
#
# Unlike every other file this extension writes, this one has no phase-1 syntax
# gate: the standard library has no YAML parser, so a template bug reaches disk
# and is caught afterwards, by the fixture assertions and the e2e validator.
openapi: 3.1.0
info:
  title: "softdeleteent API"
  version: "0.0.0"
paths:
  "/docs":
    get:
      operationId: "listDocs"
      summary: "ListDocs"
      tags: ["Doc"]
      parameters:
        - name: "id"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on id. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "_page"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "One-indexed page number. It must appear at most once; zero and negative values are rejected with 400."
        - name: "_size"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "Page size. It must appear at most once; zero and negative values are rejected with 400. Omitted it is 20, and a value above 1000 is clamped to 1000 rather than refused."
        - name: "_sort"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Comma-separated sort keys, each optionally suffixed with \":asc\" or \":desc\" (ascending by default). Legal keys: id. The primary key is appended as a tiebreak when it is absent from the list. Any other key is rejected with 400."
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/DocListResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    post:
      operationId: "createDoc"
      summary: "CreateDoc"
      tags: ["Doc"]
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/DocCreateRequest"}
      responses:
        "201":
          description: "Created"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/DocResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/docs/{id}":
    get:
      operationId: "getDoc"
      summary: "GetDoc"
      tags: ["Doc"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/DocResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    patch:
      operationId: "patchDoc"
      summary: "PatchDoc"
      tags: ["Doc"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/DocPatchRequest"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/DocResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
    delete:
      operationId: "deleteDoc"
      summary: "DeleteDoc"
      tags: ["Doc"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "204":
          description: "No Content"
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/ledgers":
    get:
      operationId: "listLedgers"
      summary: "ListLedgers"
      tags: ["Ledger"]
      parameters:
        - name: "id"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on id. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "_page"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "One-indexed page number. It must appear at most once; zero and negative values are rejected with 400."
        - name: "_size"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "Page size. It must appear at most once; zero and negative values are rejected with 400. Omitted it is 20, and a value above 1000 is clamped to 1000 rather than refused."
        - name: "_sort"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Comma-separated sort keys, each optionally suffixed with \":asc\" or \":desc\" (ascending by default). Legal keys: id. The primary key is appended as a tiebreak when it is absent from the list. Any other key is rejected with 400."
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/LedgerListResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    post:
      operationId: "createLedger"
      summary: "CreateLedger"
      tags: ["Ledger"]
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/LedgerCreateRequest"}
      responses:
        "201":
          description: "Created"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/LedgerResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/ledgers/{id}":
    get:
      operationId: "getLedger"
      summary: "GetLedger"
      tags: ["Ledger"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/LedgerResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    patch:
      operationId: "patchLedger"
      summary: "PatchLedger"
      tags: ["Ledger"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/LedgerPatchRequest"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/LedgerResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
    delete:
      operationId: "deleteLedger"
      summary: "DeleteLedger"
      tags: ["Ledger"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "204":
          description: "No Content"
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/notes":
    get:
      operationId: "listNotes"
      summary: "ListNotes"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: query
          required: false
          schema: {"type": "string", "pattern": "^(?:(eq|ne|in|not_in|gt|ge|lt|le|from|to|between):)?"}
          description: "Filter on id. A bare value means eq. Any other comparison is written as \"<operator>:<value>\"; this field accepts eq, ne, in, not_in, gt, ge, lt, le, from, to, between. A prefix outside the global operator vocabulary is taken literally, so \"eq:\" is the escape for a value that begins with one. An operator in that vocabulary but outside this field's set is rejected with 400. Repeating the parameter is legal and ANDs the resulting predicates. \"in:\" and \"between:\" split their value on commas, so a value containing a comma cannot be expressed through them; \"is_null:\" and \"not_null:\" take no value at all."
        - name: "_page"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "One-indexed page number. It must appear at most once; zero and negative values are rejected with 400."
        - name: "_size"
          in: query
          required: false
          schema: {"type": "integer", "minimum": 1}
          description: "Page size. It must appear at most once; zero and negative values are rejected with 400. Omitted it is 20, and a value above 1000 is clamped to 1000 rather than refused."
        - name: "_sort"
          in: query
          required: false
          schema: {"type": "string"}
          description: "Comma-separated sort keys, each optionally suffixed with \":asc\" or \":desc\" (ascending by default). Legal keys: id. The primary key is appended as a tiebreak when it is absent from the list. Any other key is rejected with 400."
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteListResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    post:
      operationId: "createNote"
      summary: "CreateNote"
      tags: ["Note"]
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/NoteCreateRequest"}
      responses:
        "201":
          description: "Created"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
  "/notes/{id}":
    get:
      operationId: "getNote"
      summary: "GetNote"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
    patch:
      operationId: "patchNote"
      summary: "PatchNote"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      requestBody:
        required: true
        content:
          application/json:
            schema: {"$ref": "#/components/schemas/NotePatchRequest"}
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema: {"$ref": "#/components/schemas/NoteResponse"}
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "409": {"$ref": "#/components/responses/Problem409"}
        "413": {"$ref": "#/components/responses/Problem413"}
        "415": {"$ref": "#/components/responses/Problem415"}
        "422": {"$ref": "#/components/responses/Problem422"}
        "500": {"$ref": "#/components/responses/Problem500"}
    delete:
      operationId: "deleteNote"
      summary: "DeleteNote"
      tags: ["Note"]
      parameters:
        - name: "id"
          in: path
          required: true
          schema: {"type": "string", "format": "uuid"}
      responses:
        "204":
          description: "No Content"
        "400": {"$ref": "#/components/responses/Problem400"}
        "404": {"$ref": "#/components/responses/Problem404"}
        "500": {"$ref": "#/components/responses/Problem500"}
components:
  responses:
    Problem400:
      description: "Bad Request"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem404:
      description: "Not Found"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem409:
      description: "Conflict"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem413:
      description: "Request Entity Too Large"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem415:
      description: "Unsupported Media Type"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem422:
      description: "Unprocessable Entity"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
    Problem500:
      description: "Internal Server Error"
      content:
        application/problem+json:
          schema: {"$ref": "#/components/schemas/Problem"}
  schemas:
    "Problem":
      description: "RFC 9457 problem detail. Every generated error response carries this shape."
      type: object
      properties:
        "type": {"type": "string"}
        "title": {"type": "string"}
        "status": {"type": "integer"}
        "detail": {"type": "string"}
        "field": {"type": "string"}
      required:
        - "type"
        - "title"
        - "status"
    "DocSummary":
      description: "Doc as it appears on another entity's response. It carries no edges, which is what bounds expansion to one level."
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "title": {"type": "string"}
      required:
        - "id"
        - "title"
    "DocResponse":
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "title": {"type": "string"}
        "note": {"oneOf": [{"$ref": "#/components/schemas/NoteSummary"}, {"type": "null"}]}
      required:
        - "id"
        - "title"
        - "note"
    "DocListResponse":
      description: "One page. The envelope is flat: data, total, page and size, with no nested pagination object."
      type: object
      properties:
        "data": {"type": "array", "items": {"$ref": "#/components/schemas/DocResponse"}}
        "total": {"type": "integer"}
        "page": {"type": "integer"}
        "size": {"type": "integer"}
      required:
        - "data"
        - "total"
        - "page"
        - "size"
    "DocCreateRequest":
      description: "An omitted key leaves the schema's own default in effect. An explicit null counts as omitted: a create request has nothing to clear."
      type: object
      properties:
        "title": {"type": "string"}
      required:
        - "title"
    "DocPatchRequest":
      description: "An omitted key is left alone. An explicit null clears the field, and is rejected with 422 on a field this schema declares non-nullable."
      type: object
      properties:
        "title": {"type": "string"}
    "LedgerSummary":
      description: "Ledger as it appears on another entity's response. It carries no edges, which is what bounds expansion to one level."
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "entry": {"type": "string"}
      required:
        - "id"
        - "entry"
    "LedgerResponse":
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "entry": {"type": "string"}
      required:
        - "id"
        - "entry"
    "LedgerListResponse":
      description: "One page. The envelope is flat: data, total, page and size, with no nested pagination object."
      type: object
      properties:
        "data": {"type": "array", "items": {"$ref": "#/components/schemas/LedgerResponse"}}
        "total": {"type": "integer"}
        "page": {"type": "integer"}
        "size": {"type": "integer"}
      required:
        - "data"
        - "total"
        - "page"
        - "size"
    "LedgerCreateRequest":
      description: "An omitted key leaves the schema's own default in effect. An explicit null counts as omitted: a create request has nothing to clear."
      type: object
      properties:
        "entry": {"type": "string"}
      required:
        - "entry"
    "LedgerPatchRequest":
      description: "An omitted key is left alone. An explicit null clears the field, and is rejected with 422 on a field this schema declares non-nullable."
      type: object
      properties:
        "entry": {"type": "string"}
    "NoteSummary":
      description: "Note as it appears on another entity's response. It carries no edges, which is what bounds expansion to one level."
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "body": {"type": "string"}
      required:
        - "id"
        - "body"
    "NoteResponse":
      type: object
      properties:
        "id": {"type": "string", "format": "uuid"}
        "body": {"type": "string"}
        "docs": {"type": "array", "items": {"$ref": "#/components/schemas/DocSummary"}}
      required:
        - "id"
        - "body"
        - "docs"
    "NoteListResponse":
      description: "One page. The envelope is flat: data, total, page and size, with no nested pagination object."
      type: object
      properties:
        "data": {"type": "array", "items": {"$ref": "#/components/schemas/NoteResponse"}}
        "total": {"type": "integer"}
        "page": {"type": "integer"}
        "size": {"type": "integer"}
      required:
        - "data"
        - "total"
        - "page"
        - "size"
    "NoteCreateRequest":
      description: "An omitted key leaves the schema's own default in effect. An explicit null counts as omitted: a create request has nothing to clear."
      type: object
      properties:
        "body": {"type": "string"}
      required:
        - "body"
    "NotePatchRequest":
      description: "An omitted key is left alone. An explicit null clears the field, and is rejected with 422 on a field this schema declares non-nullable."
      type: object
      properties:
        "body": {"type": "string"}

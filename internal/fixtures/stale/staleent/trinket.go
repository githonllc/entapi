// Code generated by ent, DO NOT EDIT.

package staleent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entdomain/internal/fixtures/stale/staleent/trinket"
	"github.com/google/uuid"
)

// Trinket is the model entity for the Trinket schema.
type Trinket struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Label holds the value of the "label" field.
	Label        string `json:"label,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Trinket) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case trinket.FieldLabel:
			values[i] = new(sql.NullString)
		case trinket.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Trinket fields.
func (t *Trinket) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case trinket.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				t.ID = *value
			}
		case trinket.FieldLabel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field label", values[i])
			} else if value.Valid {
				t.Label = value.String
			}
		default:
			t.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Trinket.
// This includes values selected through modifiers, order, etc.
func (t *Trinket) Value(name string) (ent.Value, error) {
	return t.selectValues.Get(name)
}

// Update returns a builder for updating this Trinket.
// Note that you need to call Trinket.Unwrap() before calling this method if this Trinket
// was returned from a transaction, and the transaction was committed or rolled back.
func (t *Trinket) Update() *TrinketUpdateOne {
	return NewTrinketClient(t.config).UpdateOne(t)
}

// Unwrap unwraps the Trinket entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (t *Trinket) Unwrap() *Trinket {
	_tx, ok := t.config.driver.(*txDriver)
	if !ok {
		panic("staleent: Trinket is not a transactional entity")
	}
	t.config.driver = _tx.drv
	return t
}

// String implements the fmt.Stringer.
func (t *Trinket) String() string {
	var builder strings.Builder
	builder.WriteString("Trinket(")
	builder.WriteString(fmt.Sprintf("id=%v, ", t.ID))
	builder.WriteString("label=")
	builder.WriteString(t.Label)
	builder.WriteByte(')')
	return builder.String()
}

// Trinkets is a parsable slice of Trinket.
type Trinkets []*Trinket

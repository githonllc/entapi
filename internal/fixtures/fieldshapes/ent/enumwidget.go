// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/enumwidget"
	"github.com/google/uuid"
)

// EnumWidget is the model entity for the EnumWidget schema.
type EnumWidget struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Status holds the value of the "status" field.
	Status enumwidget.Status `json:"status,omitempty"`
	// Tier holds the value of the "tier" field.
	Tier         enumwidget.Tier `json:"tier,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EnumWidget) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case enumwidget.FieldStatus, enumwidget.FieldTier:
			values[i] = new(sql.NullString)
		case enumwidget.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EnumWidget fields.
func (ew *EnumWidget) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case enumwidget.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ew.ID = *value
			}
		case enumwidget.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				ew.Status = enumwidget.Status(value.String)
			}
		case enumwidget.FieldTier:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tier", values[i])
			} else if value.Valid {
				ew.Tier = enumwidget.Tier(value.String)
			}
		default:
			ew.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EnumWidget.
// This includes values selected through modifiers, order, etc.
func (ew *EnumWidget) Value(name string) (ent.Value, error) {
	return ew.selectValues.Get(name)
}

// Update returns a builder for updating this EnumWidget.
// Note that you need to call EnumWidget.Unwrap() before calling this method if this EnumWidget
// was returned from a transaction, and the transaction was committed or rolled back.
func (ew *EnumWidget) Update() *EnumWidgetUpdateOne {
	return NewEnumWidgetClient(ew.config).UpdateOne(ew)
}

// Unwrap unwraps the EnumWidget entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ew *EnumWidget) Unwrap() *EnumWidget {
	_tx, ok := ew.config.driver.(*txDriver)
	if !ok {
		panic("ent: EnumWidget is not a transactional entity")
	}
	ew.config.driver = _tx.drv
	return ew
}

// String implements the fmt.Stringer.
func (ew *EnumWidget) String() string {
	var builder strings.Builder
	builder.WriteString("EnumWidget(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ew.ID))
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", ew.Status))
	builder.WriteString(", ")
	builder.WriteString("tier=")
	builder.WriteString(fmt.Sprintf("%v", ew.Tier))
	builder.WriteByte(')')
	return builder.String()
}

// EnumWidgets is a parsable slice of EnumWidget.
type EnumWidgets []*EnumWidget

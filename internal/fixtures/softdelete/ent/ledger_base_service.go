// Code generated by entdomain extension from schema "Ledger" (entschema/schema/ledger.go). DO NOT EDIT.
// Source template: backend/pkg/entdomain/templates/base_service.tmpl
// Regenerate with: make generate

package ent

import (
	"context"
	"fmt"

	"github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/softdelete/ent/ledger"
	"github.com/google/uuid"
)

// BaseLedgerServiceHooks defines hook extension points for Ledger CRUD operations.
// Implement this interface in your service struct and call SetSelf to enable hooks.
type BaseLedgerServiceHooks interface {
	BeforeCreate(ctx context.Context, req *LedgerCreateRequest) error
	AfterCreate(ctx context.Context, entity *Ledger) (*Ledger, error)
	BeforeUpdate(ctx context.Context, id uuid.UUID, req *LedgerPatchRequest) error
	AfterUpdate(ctx context.Context, entity *Ledger) (*Ledger, error)
	BeforeDelete(ctx context.Context, id uuid.UUID) error
	AfterDelete(ctx context.Context, id uuid.UUID) error
}

// BaseLedgerService provides CRUD operations for Ledger with Before/After hooks.
// Embed this in your service struct and call SetSelf to enable hook overrides.
//
// Example:
//
//	type myLedgerService struct {
//	    ent.BaseLedgerService
//	}
//
//	func NewMyLedgerService(db *ent.Client) *myLedgerService {
//	    s := &myLedgerService{
//	        BaseLedgerService: ent.BaseLedgerService{DB: db},
//	    }
//	    s.SetSelf(s) // enable hook dispatch to this struct
//	    return s
//	}
//
//	func (s *myLedgerService) BeforeCreate(ctx context.Context, req *LedgerCreateRequest) error {
//	    // custom validation
//	    return nil
//	}
type BaseLedgerService struct {
	DB   *Client
	self BaseLedgerServiceHooks
}

// SetSelf sets the hook receiver. Call this with your embedding struct
// so that hook dispatch goes through your overrides instead of the no-op defaults.
func (s *BaseLedgerService) SetSelf(hooks BaseLedgerServiceHooks) {
	s.self = hooks
}

// hooks returns the hook receiver, falling back to self (no-op defaults) if SetSelf was not called.
func (s *BaseLedgerService) hooks() BaseLedgerServiceHooks {
	if s.self != nil {
		return s.self
	}
	return s
}

// ---------------------------------------------------------------------------
// Default no-op hook implementations
// ---------------------------------------------------------------------------

func (s *BaseLedgerService) BeforeCreate(_ context.Context, _ *LedgerCreateRequest) error {
	return nil
}

func (s *BaseLedgerService) AfterCreate(_ context.Context, entity *Ledger) (*Ledger, error) {
	return entity, nil
}

func (s *BaseLedgerService) BeforeUpdate(_ context.Context, _ uuid.UUID, _ *LedgerPatchRequest) error {
	return nil
}

func (s *BaseLedgerService) AfterUpdate(_ context.Context, entity *Ledger) (*Ledger, error) {
	return entity, nil
}

func (s *BaseLedgerService) BeforeDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

func (s *BaseLedgerService) AfterDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

// ---------------------------------------------------------------------------
// CRUD methods
// ---------------------------------------------------------------------------

// GetByID retrieves a Ledger by ID.
func (s *BaseLedgerService) GetByID(ctx context.Context, id uuid.UUID) (*Ledger, error) {
	return s.DB.Ledger.Get(ctx, id)
}

// Create creates a new Ledger from a CreateRequest.
//
// Validation is not optional here, and not because this method remembers to
// call it: Apply is defined on the validated request only, so there is no
// version of this method that skips it and still compiles.
func (s *BaseLedgerService) Create(ctx context.Context, req *LedgerCreateRequest) (*Ledger, error) {
	if err := s.hooks().BeforeCreate(ctx, req); err != nil {
		return nil, err
	}

	valid, err := req.Validate()
	if err != nil {
		return nil, err
	}
	builder := valid.Apply(s.DB.Ledger.Create())

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterCreate(ctx, entity)
}

// Update performs a partial update of Ledger: a field the caller omitted
// is left alone, an explicit null clears a clearable field, and a value sets it.
func (s *BaseLedgerService) Update(ctx context.Context, id uuid.UUID, req *LedgerPatchRequest) (*Ledger, error) {
	if err := s.hooks().BeforeUpdate(ctx, id, req); err != nil {
		return nil, err
	}

	valid, err := req.Validate()
	if err != nil {
		return nil, err
	}
	builder := valid.Apply(s.DB.Ledger.UpdateOneID(id))

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsNotFound(err) {
			return nil, fmt.Errorf("%w: ledger %s", entdomain.ErrNotFound, id)
		}
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterUpdate(ctx, entity)
}

// Delete deletes a Ledger by ID.
func (s *BaseLedgerService) Delete(ctx context.Context, id uuid.UUID) error {
	if err := s.hooks().BeforeDelete(ctx, id); err != nil {
		return err
	}
	err := s.DB.Ledger.DeleteOneID(id).Exec(ctx)
	if err != nil {
		if IsNotFound(err) {
			return fmt.Errorf("%w: ledger %s", entdomain.ErrNotFound, id)
		}
		return err
	}

	return s.hooks().AfterDelete(ctx, id)
}

// DeleteBatch deletes multiple Ledgers by IDs.
// NOTE: Before/After hooks are NOT invoked for batch operations.
// If per-item validation is needed, iterate with Delete() instead.
func (s *BaseLedgerService) DeleteBatch(ctx context.Context, ids []uuid.UUID) error {
	if len(ids) == 0 {
		return nil
	}
	_, err := s.DB.Ledger.Delete().
		Where(ledger.IDIn(ids...)).
		Exec(ctx)
	return err
}

// ListWithCursor returns cursor-paginated entities using ID-based ordering.
func (s *BaseLedgerService) ListWithCursor(ctx context.Context, limit int, cursor, order string) ([]*Ledger, string, error) {
	query := s.DB.Ledger.Query()

	if cursor != "" {
		cursorID, err := uuid.Parse(cursor)
		if err != nil {
			return nil, "", fmt.Errorf("%w: invalid cursor", entdomain.ErrValidation)
		}
		if order == "asc" {
			query = query.Where(ledger.IDGT(cursorID))
		} else {
			query = query.Where(ledger.IDLT(cursorID))
		}
	}

	if order == "asc" {
		query = query.Order(Asc(ledger.FieldID))
	} else {
		query = query.Order(Desc(ledger.FieldID))
	}

	entities, err := query.Limit(limit + 1).All(ctx)
	if err != nil {
		return nil, "", err
	}

	var nextCursor string
	if len(entities) > limit {
		entities = entities[:limit]
		nextCursor = entities[len(entities)-1].ID.String()
	}

	return entities, nextCursor, nil
}

// ---------------------------------------------------------------------------
// Builder helpers: Apply requests to ent builders
//
// There are none any more, deliberately. ApplyLedgerCreateRequest and
// ApplyLedgerUpdateRequest used to be exported free functions taking a raw
// request, which is exactly the path that let a caller reach a builder without
// validating. Apply now lives on ValidLedgerCreateRequest and
// ValidLedgerPatchRequest (see ledger_dto.go), so custom
// service methods call req.Validate() first and get the same builder control.
// ---------------------------------------------------------------------------

// ---------------------------------------------------------------------------
// Entity → Response conversion
// ---------------------------------------------------------------------------

// LedgerEntToResponse converts an ent Ledger entity directly to a Response DTO.
// Exported for use in service-level queries that bypass the base service.
func LedgerEntToResponse(entity *Ledger) *LedgerResponse {
	if entity == nil {
		return nil
	}

	resp := &LedgerResponse{
		ID:    entity.ID,
		Entry: entity.Entry,
	}
	return resp
}

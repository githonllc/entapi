// Code generated by entapi extension from schema "Plain". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package queryent

import (
	"encoding"
	"fmt"
	"net/url"
	"sort"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entapi/internal/fixtures/query/queryent/plain"
	"github.com/githonllc/entapi/internal/fixtures/query/queryent/predicate"
	entapi "github.com/githonllc/entapi/runtime"
	"github.com/google/uuid"
)

// PlainFilter is the typed query representation for Plain.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type PlainFilter struct {
	// id: uuid.UUID
	ID      []uuid.UUID   `json:"id,omitempty"`
	IDNEQ   []uuid.UUID   `json:"id_neq,omitempty"`
	IDIn    [][]uuid.UUID `json:"id_in,omitempty"`
	IDNotIn [][]uuid.UUID `json:"id_not_in,omitempty"`
	IDGT    []uuid.UUID   `json:"id_gt,omitempty"`
	IDGTE   []uuid.UUID   `json:"id_gte,omitempty"`
	IDLT    []uuid.UUID   `json:"id_lt,omitempty"`
	IDLTE   []uuid.UUID   `json:"id_lte,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *PlainFilter) Predicates() []predicate.Plain {
	if f == nil {
		return nil
	}
	var ps []predicate.Plain
	entapi.AppendEach(&ps, f.ID, plain.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, plain.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, plain.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, plain.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, plain.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, plain.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, plain.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, plain.IDLTE)
	return ps
}

func parsePlainIDQueryValue(raw, whole string) (uuid.UUID, error) {
	var parsed uuid.UUID
	if err := encoding.TextUnmarshaler(&parsed).UnmarshalText([]byte(raw)); err != nil {
		return parsed, fmt.Errorf("%w: field %q value %q is invalid: %v", entapi.ErrValidation, "id", whole, err)
	}
	return parsed, nil
}

// ParsePlainQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; this generated switch owns the
// field-local operator set, conversion and predicate slots.
func ParsePlainQuery(q url.Values) (*PlainFilter, entapi.ListRequest, error) {
	f := &PlainFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				err = fmt.Errorf("%w: _q value %q is invalid because Plain has no Searchable fields", entapi.ErrValidation, reserved)
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.ID = append(f.ID, parsed)
				case "ne":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDNEQ = append(f.IDNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]uuid.UUID, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePlainIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDIn = append(f.IDIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]uuid.UUID, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parsePlainIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDNotIn = append(f.IDNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGT = append(f.IDGT, parsed)
				case "ge":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "lt":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLT = append(f.IDLT, parsed)
				case "le":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "from":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "to":
					parsed, parseErr := parsePlainIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parsePlainIDQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parsePlainIDQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, lower)
					f.IDLTE = append(f.IDLTE, upper)
				}
			}
		default:
			switch key {
			case "label":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var plainSortOptions = map[string]func(...sql.OrderTermOption) plain.OrderOption{
	"id": plain.ByID,
}

// PlainSortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var PlainSortKeys = []string{
	"id",
}

// PlainOrder is the single sort allow-list seam.
func PlainOrder(r entapi.ListRequest) ([]plain.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []plain.OrderOption{plain.ByID(sql.OrderAsc())}, nil
	}
	order := make([]plain.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := plainSortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(PlainSortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		order = append(order, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		order = append(order, plain.ByID(lastDir))
	}
	return order, nil
}

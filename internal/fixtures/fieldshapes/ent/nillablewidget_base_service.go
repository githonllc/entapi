// Code generated by entdomain extension from schema "NillableWidget" (entschema/schema/nillablewidget.go). DO NOT EDIT.
// Source template: backend/pkg/entdomain/templates/base_service.tmpl
// Regenerate with: make generate

package ent

import (
	"context"
	"fmt"

	"github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/nillablewidget"
	"github.com/google/uuid"
)

// BaseNillableWidgetServiceHooks defines hook extension points for NillableWidget CRUD operations.
// Implement this interface in your service struct and call SetSelf to enable hooks.
type BaseNillableWidgetServiceHooks interface {
	BeforeCreate(ctx context.Context, req *NillableWidgetCreateRequest) error
	AfterCreate(ctx context.Context, entity *NillableWidget) (*NillableWidget, error)
	BeforeUpdate(ctx context.Context, id uuid.UUID, req *NillableWidgetPatchRequest) error
	AfterUpdate(ctx context.Context, entity *NillableWidget) (*NillableWidget, error)
	BeforeDelete(ctx context.Context, id uuid.UUID) error
	AfterDelete(ctx context.Context, id uuid.UUID) error
}

// BaseNillableWidgetService provides CRUD operations for NillableWidget with Before/After hooks.
// Embed this in your service struct and call SetSelf to enable hook overrides.
//
// Example:
//
//	type myNillableWidgetService struct {
//	    ent.BaseNillableWidgetService
//	}
//
//	func NewMyNillableWidgetService(db *ent.Client) *myNillableWidgetService {
//	    s := &myNillableWidgetService{
//	        BaseNillableWidgetService: ent.BaseNillableWidgetService{DB: db},
//	    }
//	    s.SetSelf(s) // enable hook dispatch to this struct
//	    return s
//	}
//
//	func (s *myNillableWidgetService) BeforeCreate(ctx context.Context, req *NillableWidgetCreateRequest) error {
//	    // custom validation
//	    return nil
//	}
type BaseNillableWidgetService struct {
	DB   *Client
	self BaseNillableWidgetServiceHooks
}

// SetSelf sets the hook receiver. Call this with your embedding struct
// so that hook dispatch goes through your overrides instead of the no-op defaults.
func (s *BaseNillableWidgetService) SetSelf(hooks BaseNillableWidgetServiceHooks) {
	s.self = hooks
}

// hooks returns the hook receiver, falling back to self (no-op defaults) if SetSelf was not called.
func (s *BaseNillableWidgetService) hooks() BaseNillableWidgetServiceHooks {
	if s.self != nil {
		return s.self
	}
	return s
}

// ---------------------------------------------------------------------------
// Default no-op hook implementations
// ---------------------------------------------------------------------------

func (s *BaseNillableWidgetService) BeforeCreate(_ context.Context, _ *NillableWidgetCreateRequest) error {
	return nil
}

func (s *BaseNillableWidgetService) AfterCreate(_ context.Context, entity *NillableWidget) (*NillableWidget, error) {
	return entity, nil
}

func (s *BaseNillableWidgetService) BeforeUpdate(_ context.Context, _ uuid.UUID, _ *NillableWidgetPatchRequest) error {
	return nil
}

func (s *BaseNillableWidgetService) AfterUpdate(_ context.Context, entity *NillableWidget) (*NillableWidget, error) {
	return entity, nil
}

func (s *BaseNillableWidgetService) BeforeDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

func (s *BaseNillableWidgetService) AfterDelete(_ context.Context, _ uuid.UUID) error {
	return nil
}

// ---------------------------------------------------------------------------
// CRUD methods
// ---------------------------------------------------------------------------

// GetByID retrieves a NillableWidget by ID.
func (s *BaseNillableWidgetService) GetByID(ctx context.Context, id uuid.UUID) (*NillableWidget, error) {
	return s.DB.NillableWidget.Get(ctx, id)
}

// Create creates a new NillableWidget from a CreateRequest.
//
// Validation is not optional here, and not because this method remembers to
// call it: Apply is defined on the validated request only, so there is no
// version of this method that skips it and still compiles.
func (s *BaseNillableWidgetService) Create(ctx context.Context, req *NillableWidgetCreateRequest) (*NillableWidget, error) {
	if err := s.hooks().BeforeCreate(ctx, req); err != nil {
		return nil, err
	}

	valid, err := req.Validate()
	if err != nil {
		return nil, err
	}
	builder := valid.Apply(s.DB.NillableWidget.Create())

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterCreate(ctx, entity)
}

// Update performs a partial update of NillableWidget: a field the caller omitted
// is left alone, an explicit null clears a clearable field, and a value sets it.
func (s *BaseNillableWidgetService) Update(ctx context.Context, id uuid.UUID, req *NillableWidgetPatchRequest) (*NillableWidget, error) {
	if err := s.hooks().BeforeUpdate(ctx, id, req); err != nil {
		return nil, err
	}

	valid, err := req.Validate()
	if err != nil {
		return nil, err
	}
	builder := valid.Apply(s.DB.NillableWidget.UpdateOneID(id))

	entity, err := builder.Save(ctx)
	if err != nil {
		if IsNotFound(err) {
			return nil, fmt.Errorf("%w: nillablewidget %s", entdomain.ErrNotFound, id)
		}
		if IsConstraintError(err) {
			return nil, fmt.Errorf("%w: %v", entdomain.ErrAlreadyExists, err)
		}
		return nil, err
	}

	return s.hooks().AfterUpdate(ctx, entity)
}

// Delete deletes a NillableWidget by ID.
func (s *BaseNillableWidgetService) Delete(ctx context.Context, id uuid.UUID) error {
	if err := s.hooks().BeforeDelete(ctx, id); err != nil {
		return err
	}
	err := s.DB.NillableWidget.DeleteOneID(id).Exec(ctx)
	if err != nil {
		if IsNotFound(err) {
			return fmt.Errorf("%w: nillablewidget %s", entdomain.ErrNotFound, id)
		}
		return err
	}

	return s.hooks().AfterDelete(ctx, id)
}

// DeleteBatch deletes multiple NillableWidgets by IDs.
// NOTE: Before/After hooks are NOT invoked for batch operations.
// If per-item validation is needed, iterate with Delete() instead.
func (s *BaseNillableWidgetService) DeleteBatch(ctx context.Context, ids []uuid.UUID) error {
	if len(ids) == 0 {
		return nil
	}
	_, err := s.DB.NillableWidget.Delete().
		Where(nillablewidget.IDIn(ids...)).
		Exec(ctx)
	return err
}

// ListWithCursor returns cursor-paginated entities using ID-based ordering.
func (s *BaseNillableWidgetService) ListWithCursor(ctx context.Context, limit int, cursor, order string) ([]*NillableWidget, string, error) {
	query := s.DB.NillableWidget.Query()

	if cursor != "" {
		cursorID, err := uuid.Parse(cursor)
		if err != nil {
			return nil, "", fmt.Errorf("%w: invalid cursor", entdomain.ErrValidation)
		}
		if order == "asc" {
			query = query.Where(nillablewidget.IDGT(cursorID))
		} else {
			query = query.Where(nillablewidget.IDLT(cursorID))
		}
	}

	if order == "asc" {
		query = query.Order(Asc(nillablewidget.FieldID))
	} else {
		query = query.Order(Desc(nillablewidget.FieldID))
	}

	entities, err := query.Limit(limit + 1).All(ctx)
	if err != nil {
		return nil, "", err
	}

	var nextCursor string
	if len(entities) > limit {
		entities = entities[:limit]
		nextCursor = entities[len(entities)-1].ID.String()
	}

	return entities, nextCursor, nil
}

// ---------------------------------------------------------------------------
// Builder helpers: Apply requests to ent builders
//
// There are none any more, deliberately. ApplyNillableWidgetCreateRequest and
// ApplyNillableWidgetUpdateRequest used to be exported free functions taking a raw
// request, which is exactly the path that let a caller reach a builder without
// validating. Apply now lives on ValidNillableWidgetCreateRequest and
// ValidNillableWidgetPatchRequest (see nillablewidget_dto.go), so custom
// service methods call req.Validate() first and get the same builder control.
// ---------------------------------------------------------------------------

// ---------------------------------------------------------------------------
// Entity → Response conversion
// ---------------------------------------------------------------------------

// NillableWidgetEntToResponse converts an ent NillableWidget entity directly to a Response DTO.
// Exported for use in service-level queries that bypass the base service.
func NillableWidgetEntToResponse(entity *NillableWidget) *NillableWidgetResponse {
	if entity == nil {
		return nil
	}

	resp := &NillableWidgetResponse{
		ID:       entity.ID,
		Nickname: entity.Nickname,
		Handle:   entity.Handle,
		Quota:    entity.Quota,
	}
	return resp
}

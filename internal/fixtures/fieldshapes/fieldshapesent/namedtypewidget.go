// Code generated by ent, DO NOT EDIT.

package fieldshapesent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/fieldshapesent/namedtypewidget"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/fieldshapesent/schema"
	"github.com/google/uuid"
)

// NamedTypeWidget is the model entity for the NamedTypeWidget schema.
type NamedTypeWidget struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Labels holds the value of the "labels" field.
	Labels schema.Tags `json:"labels,omitempty"`
	// Attrs holds the value of the "attrs" field.
	Attrs schema.Attrs `json:"attrs,omitempty"`
	// RequiredLabels holds the value of the "required_labels" field.
	RequiredLabels schema.Tags `json:"required_labels,omitempty"`
	// RequiredAttrs holds the value of the "required_attrs" field.
	RequiredAttrs schema.Attrs `json:"required_attrs,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NamedTypeWidget) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case namedtypewidget.FieldLabels, namedtypewidget.FieldAttrs, namedtypewidget.FieldRequiredLabels, namedtypewidget.FieldRequiredAttrs:
			values[i] = new([]byte)
		case namedtypewidget.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NamedTypeWidget fields.
func (ntw *NamedTypeWidget) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case namedtypewidget.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ntw.ID = *value
			}
		case namedtypewidget.FieldLabels:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field labels", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &ntw.Labels); err != nil {
					return fmt.Errorf("unmarshal field labels: %w", err)
				}
			}
		case namedtypewidget.FieldAttrs:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field attrs", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &ntw.Attrs); err != nil {
					return fmt.Errorf("unmarshal field attrs: %w", err)
				}
			}
		case namedtypewidget.FieldRequiredLabels:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field required_labels", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &ntw.RequiredLabels); err != nil {
					return fmt.Errorf("unmarshal field required_labels: %w", err)
				}
			}
		case namedtypewidget.FieldRequiredAttrs:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field required_attrs", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &ntw.RequiredAttrs); err != nil {
					return fmt.Errorf("unmarshal field required_attrs: %w", err)
				}
			}
		default:
			ntw.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NamedTypeWidget.
// This includes values selected through modifiers, order, etc.
func (ntw *NamedTypeWidget) Value(name string) (ent.Value, error) {
	return ntw.selectValues.Get(name)
}

// Update returns a builder for updating this NamedTypeWidget.
// Note that you need to call NamedTypeWidget.Unwrap() before calling this method if this NamedTypeWidget
// was returned from a transaction, and the transaction was committed or rolled back.
func (ntw *NamedTypeWidget) Update() *NamedTypeWidgetUpdateOne {
	return NewNamedTypeWidgetClient(ntw.config).UpdateOne(ntw)
}

// Unwrap unwraps the NamedTypeWidget entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ntw *NamedTypeWidget) Unwrap() *NamedTypeWidget {
	_tx, ok := ntw.config.driver.(*txDriver)
	if !ok {
		panic("fieldshapesent: NamedTypeWidget is not a transactional entity")
	}
	ntw.config.driver = _tx.drv
	return ntw
}

// String implements the fmt.Stringer.
func (ntw *NamedTypeWidget) String() string {
	var builder strings.Builder
	builder.WriteString("NamedTypeWidget(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ntw.ID))
	builder.WriteString("labels=")
	builder.WriteString(fmt.Sprintf("%v", ntw.Labels))
	builder.WriteString(", ")
	builder.WriteString("attrs=")
	builder.WriteString(fmt.Sprintf("%v", ntw.Attrs))
	builder.WriteString(", ")
	builder.WriteString("required_labels=")
	builder.WriteString(fmt.Sprintf("%v", ntw.RequiredLabels))
	builder.WriteString(", ")
	builder.WriteString("required_attrs=")
	builder.WriteString(fmt.Sprintf("%v", ntw.RequiredAttrs))
	builder.WriteByte(')')
	return builder.String()
}

// NamedTypeWidgets is a parsable slice of NamedTypeWidget.
type NamedTypeWidgets []*NamedTypeWidget

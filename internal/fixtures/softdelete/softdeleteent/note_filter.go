// Code generated by entapi extension from schema "Note". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package softdeleteent

import (
	"encoding"
	"fmt"
	"net/url"
	"sort"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/githonllc/entapi/internal/fixtures/softdelete/softdeleteent/note"
	"github.com/githonllc/entapi/internal/fixtures/softdelete/softdeleteent/predicate"
	entapi "github.com/githonllc/entapi/runtime"
	"github.com/google/uuid"
)

// NoteFilter is the typed query representation for Note.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type NoteFilter struct {
	// id: uuid.UUID
	ID      []uuid.UUID   `json:"id,omitempty"`
	IDNEQ   []uuid.UUID   `json:"id_neq,omitempty"`
	IDIn    [][]uuid.UUID `json:"id_in,omitempty"`
	IDNotIn [][]uuid.UUID `json:"id_not_in,omitempty"`
	IDGT    []uuid.UUID   `json:"id_gt,omitempty"`
	IDGTE   []uuid.UUID   `json:"id_gte,omitempty"`
	IDLT    []uuid.UUID   `json:"id_lt,omitempty"`
	IDLTE   []uuid.UUID   `json:"id_lte,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *NoteFilter) Predicates() []predicate.Note {
	if f == nil {
		return nil
	}
	var ps []predicate.Note
	entapi.AppendEach(&ps, f.ID, note.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, note.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, note.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, note.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, note.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, note.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, note.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, note.IDLTE)
	return ps
}

func parseNoteIDQueryValue(raw, whole string) (uuid.UUID, error) {
	var parsed uuid.UUID
	if err := encoding.TextUnmarshaler(&parsed).UnmarshalText([]byte(raw)); err != nil {
		return parsed, fmt.Errorf("%w: field %q value %q is invalid: %v", entapi.ErrValidation, "id", whole, err)
	}
	return parsed, nil
}

// ParseNoteQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; this generated switch owns the
// field-local operator set, conversion and predicate slots.
func ParseNoteQuery(q url.Values) (*NoteFilter, entapi.ListRequest, error) {
	f := &NoteFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				err = fmt.Errorf("%w: _q value %q is invalid because Note has no Searchable fields", entapi.ErrValidation, reserved)
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			if err := entapi.ParseFieldValues("id", values, false, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between", parseNoteIDQueryValue, []entapi.QueryOp[uuid.UUID]{
				{Prefix: "eq", Kind: entapi.OpKindValue, One: &f.ID},
				{Prefix: "ne", Kind: entapi.OpKindValue, One: &f.IDNEQ},
				{Prefix: "in", Kind: entapi.OpKindList, Many: &f.IDIn},
				{Prefix: "not_in", Kind: entapi.OpKindList, Many: &f.IDNotIn},
				{Prefix: "gt", Kind: entapi.OpKindValue, One: &f.IDGT},
				{Prefix: "ge", Kind: entapi.OpKindValue, One: &f.IDGTE},
				{Prefix: "lt", Kind: entapi.OpKindValue, One: &f.IDLT},
				{Prefix: "le", Kind: entapi.OpKindValue, One: &f.IDLTE},
				{Prefix: "from", Kind: entapi.OpKindValue, One: &f.IDGTE},
				{Prefix: "to", Kind: entapi.OpKindValue, One: &f.IDLTE},
				{Prefix: "between", Kind: entapi.OpKindRange, One: &f.IDGTE, Second: &f.IDLTE},
			}); err != nil {
				return nil, entapi.ListRequest{}, err
			}
		default:
			switch key {
			case "body":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var noteSortOptions = map[string]func(...sql.OrderTermOption) note.OrderOption{
	"id": note.ByID,
}

// NoteSortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var NoteSortKeys = []string{
	"id",
}

// NoteOrder is the single sort allow-list seam.
func NoteOrder(r entapi.ListRequest) ([]note.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []note.OrderOption{note.ByID(sql.OrderAsc())}, nil
	}
	orderBy := make([]note.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := noteSortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(NoteSortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		orderBy = append(orderBy, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		orderBy = append(orderBy, note.ByID(lastDir))
	}
	return orderBy, nil
}

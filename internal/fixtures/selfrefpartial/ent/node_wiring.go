// Code generated by entdomain extension from schema "Node". DO NOT EDIT.
// Source template: templates/wiring.tmpl
// Regenerate with: make generate

package ent

import (
	"context"

	entdomain "github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/selfrefpartial/ent/node"
	"github.com/google/uuid"
)

// ============================================================================
// Wiring for Node — one free function per operation.
//
// Free functions, not methods on a generated struct: there is nothing to embed,
// no self-reference to install and no fixed set of override points. A consumer
// who needs different behaviour writes their own function and stops calling the
// generated one, which is a stronger escape hatch than any hook contract — and
// it is why the hook question becomes moot rather than merely redesigned.
//
// The ratio is the point. A generated thirty-line method body is thirty lines in
// which the generator can have guessed wrong and the caller cannot intervene. A
// single call into a routine written once, in Go, for every entity is not.
//
// The identifier type is uuid.UUID because that is what the schema says.
// Nothing below is written for a particular one.
//
// Error classification is deliberately absent: mapping a driver error to
// not-found or already-exists belongs to the runtime and is issue #13. These
// functions return what ent returned.
// ============================================================================

// nodeByID fetches one Node through the eager-load plan.
//
// It goes through Query rather than NodeClient.Get, which is the same
// query with no plan applied and therefore cannot serve a response type that
// declares edges.
func nodeByID(db *Client) func(context.Context, uuid.UUID) (*Node, error) {
	return func(ctx context.Context, id uuid.UUID) (*Node, error) {
		return NodeQueryWithResponseEdges(db.Node.Query()).Where(node.IDEQ(id)).Only(ctx)
	}
}

// nodeReloaded converts a just-saved Node by reading it
// back through the eager-load plan.
//
// A mutation builder's Save returns the entity with no edges loaded, and
// NodeResponse declares edges, so converting that value directly would
// report them as not loaded. The extra round trip is what makes a mutation
// response the same shape as a read.
func nodeReloaded(ctx context.Context, db *Client) func(*Node) (*NodeResponse, error) {
	return func(e *Node) (*NodeResponse, error) {
		return GetNode(ctx, db, e.ID)
	}
}

// GetNode reads one Node and converts it to its response.
func GetNode(ctx context.Context, db *Client, id uuid.UUID) (*NodeResponse, error) {
	return entdomain.GetOne(ctx, nodeByID(db), NewNodeResponse, id)
}

// ListNodes runs a filtered, ordered, paginated query.
//
// This is the one operation whose body is not a single call, and the reason is
// worth stating rather than hiding: NodeOrder can fail, because a sort
// key outside NodeSortKeys has no correct answer and serving a
// differently-ordered page instead is worse than refusing. entdomain.ListPage
// takes resolved order options, not a fallible producer of them, so the check
// happens here.
//
// A nil filter contributes no predicates, so a caller with nothing to filter by
// does not need a branch of its own.
func ListNodes(ctx context.Context, db *Client, f *NodeFilter, r entdomain.ListRequest) (*entdomain.Page[NodeResponse], error) {
	order, err := NodeOrder(r)
	if err != nil {
		return nil, err
	}
	return entdomain.ListPage(ctx, NodeQueryWithResponseEdges(db.Node.Query()), f.Predicates(), order, r, NewNodeResponse)
}

// CreateNode inserts one Node and returns its response.
//
// It takes the VALIDATED request. Apply is defined on that type and nowhere
// else, so validation is a compile-time requirement here rather than a step
// this function could forget on the caller's behalf.
func CreateNode(ctx context.Context, db *Client, v *ValidNodeCreateRequest) (*NodeResponse, error) {
	return entdomain.SaveOne(ctx, v.Apply(db.Node.Create()), nodeReloaded(ctx, db))
}

// UpdateNode applies a validated patch to one Node.
//
// Fields the patch did not carry are not written at all, so a partial update
// stays partial — that property belongs to Apply, and this function does not
// re-decide it.
func UpdateNode(ctx context.Context, db *Client, id uuid.UUID, v *ValidNodePatchRequest) (*NodeResponse, error) {
	return entdomain.SaveOne(ctx, v.Apply(db.Node.UpdateOneID(id)), nodeReloaded(ctx, db))
}

// DeleteNode removes one Node.
//
// No runtime routine is involved: ent's own builder is already the single line,
// and there is nothing to convert. What "delete" means for an entity with a
// soft-delete column is decided by an ent interceptor or mixin, not by a
// filename convention read out of the schema.
func DeleteNode(ctx context.Context, db *Client, id uuid.UUID) error {
	return db.Node.DeleteOneID(id).Exec(ctx)
}

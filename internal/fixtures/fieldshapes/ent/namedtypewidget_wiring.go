// Code generated by entdomain extension from schema "NamedTypeWidget". DO NOT EDIT.
// Source template: templates/wiring.tmpl
// Regenerate with: make generate

package ent

import (
	"context"

	entdomain "github.com/githonllc/entdomain"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/ent/namedtypewidget"
	"github.com/google/uuid"
)

// ============================================================================
// Wiring for NamedTypeWidget — one free function per operation.
//
// Free functions, not methods on a generated struct: there is nothing to embed,
// no self-reference to install and no fixed set of override points. A consumer
// who needs different behaviour writes their own function and stops calling the
// generated one, which is a stronger escape hatch than any hook contract — and
// it is why the hook question becomes moot rather than merely redesigned.
//
// The ratio is the point. A generated thirty-line method body is thirty lines in
// which the generator can have guessed wrong and the caller cannot intervene. A
// single call into a routine written once, in Go, for every entity is not.
//
// The identifier type is uuid.UUID because that is what the schema says.
// Nothing below is written for a particular one.
//
// Error classification is deliberately absent: mapping a driver error to
// not-found or already-exists belongs to the runtime and is issue #13. These
// functions return what ent returned.
// ============================================================================

// namedTypeWidgetByID fetches one NamedTypeWidget through the eager-load plan.
//
// It goes through Query rather than NamedTypeWidgetClient.Get, which is the same
// query with no plan applied.
func namedTypeWidgetByID(db *Client) func(context.Context, uuid.UUID) (*NamedTypeWidget, error) {
	return func(ctx context.Context, id uuid.UUID) (*NamedTypeWidget, error) {
		return NamedTypeWidgetQueryWithResponseEdges(db.NamedTypeWidget.Query()).Where(namedtypewidget.IDEQ(id)).Only(ctx)
	}
}

// GetNamedTypeWidget reads one NamedTypeWidget and converts it to its response.
func GetNamedTypeWidget(ctx context.Context, db *Client, id uuid.UUID) (*NamedTypeWidgetResponse, error) {
	return entdomain.GetOne(ctx, namedTypeWidgetByID(db), NewNamedTypeWidgetResponse, id)
}

// ListNamedTypeWidgets runs a filtered, ordered, paginated query.
//
// This is the one operation whose body is not a single call, and the reason is
// worth stating rather than hiding: NamedTypeWidgetOrder can fail, because a sort
// key outside NamedTypeWidgetSortKeys has no correct answer and serving a
// differently-ordered page instead is worse than refusing. entdomain.ListPage
// takes resolved order options, not a fallible producer of them, so the check
// happens here.
//
// A nil filter contributes no predicates, so a caller with nothing to filter by
// does not need a branch of its own.
func ListNamedTypeWidgets(ctx context.Context, db *Client, f *NamedTypeWidgetFilter, r entdomain.ListRequest) (*entdomain.Page[NamedTypeWidgetResponse], error) {
	order, err := NamedTypeWidgetOrder(r)
	if err != nil {
		return nil, err
	}
	return entdomain.ListPage(ctx, NamedTypeWidgetQueryWithResponseEdges(db.NamedTypeWidget.Query()), f.Predicates(), order, r, NewNamedTypeWidgetResponse)
}

// CreateNamedTypeWidget inserts one NamedTypeWidget and returns its response.
//
// It takes the VALIDATED request. Apply is defined on that type and nowhere
// else, so validation is a compile-time requirement here rather than a step
// this function could forget on the caller's behalf.
func CreateNamedTypeWidget(ctx context.Context, db *Client, v *ValidNamedTypeWidgetCreateRequest) (*NamedTypeWidgetResponse, error) {
	return entdomain.SaveOne(ctx, v.Apply(db.NamedTypeWidget.Create()), NewNamedTypeWidgetResponse)
}

// UpdateNamedTypeWidget applies a validated patch to one NamedTypeWidget.
//
// Fields the patch did not carry are not written at all, so a partial update
// stays partial — that property belongs to Apply, and this function does not
// re-decide it.
func UpdateNamedTypeWidget(ctx context.Context, db *Client, id uuid.UUID, v *ValidNamedTypeWidgetPatchRequest) (*NamedTypeWidgetResponse, error) {
	return entdomain.SaveOne(ctx, v.Apply(db.NamedTypeWidget.UpdateOneID(id)), NewNamedTypeWidgetResponse)
}

// DeleteNamedTypeWidget removes one NamedTypeWidget.
//
// No runtime routine is involved: ent's own builder is already the single line,
// and there is nothing to convert. What "delete" means for an entity with a
// soft-delete column is decided by an ent interceptor or mixin, not by a
// filename convention read out of the schema.
func DeleteNamedTypeWidget(ctx context.Context, db *Client, id uuid.UUID) error {
	return db.NamedTypeWidget.DeleteOneID(id).Exec(ctx)
}

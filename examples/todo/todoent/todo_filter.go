// Code generated by entapi extension from schema "Todo". DO NOT EDIT.
// Source template: templates/filter.tmpl
// Regenerate with: make generate

package todoent

import (
	"fmt"
	"net/url"
	"sort"
	"strconv"
	"strings"

	"entgo.io/ent/dialect/sql"

	"github.com/githonllc/entapi/examples/todo/todoent/predicate"
	"github.com/githonllc/entapi/examples/todo/todoent/todo"
	entapi "github.com/githonllc/entapi/runtime"
)

// TodoFilter is the typed query representation for Todo.
// Repeated URL parameters occupy repeated slots and Predicates ANDs them.
type TodoFilter struct {
	// id: int
	ID      []int   `json:"id,omitempty"`
	IDNEQ   []int   `json:"id_neq,omitempty"`
	IDIn    [][]int `json:"id_in,omitempty"`
	IDNotIn [][]int `json:"id_not_in,omitempty"`
	IDGT    []int   `json:"id_gt,omitempty"`
	IDGTE   []int   `json:"id_gte,omitempty"`
	IDLT    []int   `json:"id_lt,omitempty"`
	IDLTE   []int   `json:"id_lte,omitempty"`
	// title: string
	Title             []string   `json:"title,omitempty"`
	TitleNEQ          []string   `json:"title_neq,omitempty"`
	TitleIn           [][]string `json:"title_in,omitempty"`
	TitleNotIn        [][]string `json:"title_not_in,omitempty"`
	TitleGT           []string   `json:"title_gt,omitempty"`
	TitleGTE          []string   `json:"title_gte,omitempty"`
	TitleLT           []string   `json:"title_lt,omitempty"`
	TitleLTE          []string   `json:"title_lte,omitempty"`
	TitleContains     []string   `json:"title_contains,omitempty"`
	TitleHasPrefix    []string   `json:"title_prefix,omitempty"`
	TitleHasSuffix    []string   `json:"title_suffix,omitempty"`
	TitleContainsFold []string   `json:"title_icontains,omitempty"`
	// done: bool, optional
	Done       []bool `json:"done,omitempty"`
	DoneNEQ    []bool `json:"done_neq,omitempty"`
	DoneIsNull []bool `json:"done_is_null,omitempty"`
	// priority: int, optional
	Priority       []int   `json:"priority,omitempty"`
	PriorityNEQ    []int   `json:"priority_neq,omitempty"`
	PriorityIn     [][]int `json:"priority_in,omitempty"`
	PriorityNotIn  [][]int `json:"priority_not_in,omitempty"`
	PriorityGT     []int   `json:"priority_gt,omitempty"`
	PriorityGTE    []int   `json:"priority_gte,omitempty"`
	PriorityLT     []int   `json:"priority_lt,omitempty"`
	PriorityLTE    []int   `json:"priority_lte,omitempty"`
	PriorityIsNull []bool  `json:"priority_is_null,omitempty"`
	// Q is the single free-text term matched across Searchable fields.
	Q *string `json:"_q,omitempty"`
}

// Predicates converts the typed filter into conjunctive ent predicates.
func (f *TodoFilter) Predicates() []predicate.Todo {
	if f == nil {
		return nil
	}
	var ps []predicate.Todo
	entapi.AppendEach(&ps, f.ID, todo.IDEQ)
	entapi.AppendEach(&ps, f.IDNEQ, todo.IDNEQ)
	entapi.AppendEachSlice(&ps, f.IDIn, todo.IDIn)
	entapi.AppendEachSlice(&ps, f.IDNotIn, todo.IDNotIn)
	entapi.AppendEach(&ps, f.IDGT, todo.IDGT)
	entapi.AppendEach(&ps, f.IDGTE, todo.IDGTE)
	entapi.AppendEach(&ps, f.IDLT, todo.IDLT)
	entapi.AppendEach(&ps, f.IDLTE, todo.IDLTE)
	entapi.AppendEach(&ps, f.Title, todo.TitleEQ)
	entapi.AppendEach(&ps, f.TitleNEQ, todo.TitleNEQ)
	entapi.AppendEachSlice(&ps, f.TitleIn, todo.TitleIn)
	entapi.AppendEachSlice(&ps, f.TitleNotIn, todo.TitleNotIn)
	entapi.AppendEach(&ps, f.TitleGT, todo.TitleGT)
	entapi.AppendEach(&ps, f.TitleGTE, todo.TitleGTE)
	entapi.AppendEach(&ps, f.TitleLT, todo.TitleLT)
	entapi.AppendEach(&ps, f.TitleLTE, todo.TitleLTE)
	entapi.AppendEach(&ps, f.TitleContains, todo.TitleContains)
	entapi.AppendEach(&ps, f.TitleHasPrefix, todo.TitleHasPrefix)
	entapi.AppendEach(&ps, f.TitleHasSuffix, todo.TitleHasSuffix)
	entapi.AppendEach(&ps, f.TitleContainsFold, todo.TitleContainsFold)
	entapi.AppendEach(&ps, f.Done, todo.DoneEQ)
	entapi.AppendEach(&ps, f.DoneNEQ, todo.DoneNEQ)
	for _, isNull := range f.DoneIsNull {
		if isNull {
			ps = append(ps, todo.DoneIsNil())
		} else {
			ps = append(ps, todo.DoneNotNil())
		}
	}
	entapi.AppendEach(&ps, f.Priority, todo.PriorityEQ)
	entapi.AppendEach(&ps, f.PriorityNEQ, todo.PriorityNEQ)
	entapi.AppendEachSlice(&ps, f.PriorityIn, todo.PriorityIn)
	entapi.AppendEachSlice(&ps, f.PriorityNotIn, todo.PriorityNotIn)
	entapi.AppendEach(&ps, f.PriorityGT, todo.PriorityGT)
	entapi.AppendEach(&ps, f.PriorityGTE, todo.PriorityGTE)
	entapi.AppendEach(&ps, f.PriorityLT, todo.PriorityLT)
	entapi.AppendEach(&ps, f.PriorityLTE, todo.PriorityLTE)
	for _, isNull := range f.PriorityIsNull {
		if isNull {
			ps = append(ps, todo.PriorityIsNil())
		} else {
			ps = append(ps, todo.PriorityNotNil())
		}
	}
	if f.Q != nil && *f.Q != "" {
		ps = append(ps, todo.Or(
			todo.TitleContains(*f.Q),
		))
	}
	return ps
}

func parseTodoIDQueryValue(raw, whole string) (int, error) {
	parsed, err := strconv.ParseInt(raw, 10, 0)
	if err != nil {
		var zero int
		return zero, fmt.Errorf("%w: field %q value %q is not a valid int: %v", entapi.ErrValidation, "id", whole, err)
	}
	return int(parsed), nil
}

func parseTodoTitleQueryValue(raw, whole string) (string, error) {
	return string(raw), nil
}

func parseTodoDoneQueryValue(raw, whole string) (bool, error) {
	parsed, err := strconv.ParseBool(raw)
	if err != nil {
		var zero bool
		return zero, fmt.Errorf("%w: field %q value %q is not a bool: %v", entapi.ErrValidation, "done", whole, err)
	}
	return bool(parsed), nil
}

func parseTodoPriorityQueryValue(raw, whole string) (int, error) {
	parsed, err := strconv.ParseInt(raw, 10, 0)
	if err != nil {
		var zero int
		return zero, fmt.Errorf("%w: field %q value %q is not a valid int: %v", entapi.ErrValidation, "priority", whole, err)
	}
	return int(parsed), nil
}

// ParseTodoQuery parses the complete URL query contract in sorted-key
// order. Runtime code owns lexical grammar; this generated switch owns the
// field-local operator set, conversion and predicate slots.
func ParseTodoQuery(q url.Values) (*TodoFilter, entapi.ListRequest, error) {
	f := &TodoFilter{}
	var request entapi.ListRequest
	keys := make([]string, 0, len(q))
	for key := range q {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		values := q[key]
		reserved, recognized, err := entapi.ReservedQueryValue(key, values)
		if err != nil {
			return nil, entapi.ListRequest{}, err
		}
		if recognized {
			switch key {
			case "_sort":
				request.Sort, err = entapi.ParseSortSpecs(reserved)
			case "_page":
				request.Page, err = entapi.ParsePageParam(key, reserved)
			case "_size":
				request.Size, err = entapi.ParsePageParam(key, reserved)
			case "_q":
				term := reserved
				f.Q = &term
			}
			if err != nil {
				return nil, entapi.ListRequest{}, err
			}
			continue
		}
		if strings.HasPrefix(key, "_") {
			return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown reserved query parameter %q", entapi.ErrValidation, key)
		}
		switch key {
		case "id":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.ID = append(f.ID, parsed)
				case "ne":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDNEQ = append(f.IDNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTodoIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDIn = append(f.IDIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTodoIDQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.IDNotIn = append(f.IDNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGT = append(f.IDGT, parsed)
				case "ge":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "lt":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLT = append(f.IDLT, parsed)
				case "le":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "from":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, parsed)
				case "to":
					parsed, parseErr := parseTodoIDQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDLTE = append(f.IDLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parseTodoIDQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parseTodoIDQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.IDGTE = append(f.IDGTE, lower)
					f.IDLTE = append(f.IDLTE, upper)
				}
			}
		case "title":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "like":
				case "prefix":
				case "suffix":
				case "ilike":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, like, prefix, suffix, ilike, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Title = append(f.Title, parsed)
				case "ne":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleNEQ = append(f.TitleNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTodoTitleQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.TitleIn = append(f.TitleIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]string, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTodoTitleQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.TitleNotIn = append(f.TitleNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleGT = append(f.TitleGT, parsed)
				case "ge":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleGTE = append(f.TitleGTE, parsed)
				case "lt":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleLT = append(f.TitleLT, parsed)
				case "le":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleLTE = append(f.TitleLTE, parsed)
				case "like":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleContains = append(f.TitleContains, parsed)
				case "prefix":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleHasPrefix = append(f.TitleHasPrefix, parsed)
				case "suffix":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleHasSuffix = append(f.TitleHasSuffix, parsed)
				case "ilike":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleContainsFold = append(f.TitleContainsFold, parsed)
				case "from":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleGTE = append(f.TitleGTE, parsed)
				case "to":
					parsed, parseErr := parseTodoTitleQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleLTE = append(f.TitleLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parseTodoTitleQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parseTodoTitleQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.TitleGTE = append(f.TitleGTE, lower)
					f.TitleLTE = append(f.TitleLTE, upper)
				}
			}
		case "done":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "is_null":
				case "not_null":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, is_null, not_null")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseTodoDoneQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Done = append(f.Done, parsed)
				case "ne":
					parsed, parseErr := parseTodoDoneQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.DoneNEQ = append(f.DoneNEQ, parsed)
				case "is_null":
					if raw != "" {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses %q with a value; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, is_null, not_null")
					}
					f.DoneIsNull = append(f.DoneIsNull, true)
				case "not_null":
					if raw != "" {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses %q with a value; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, is_null, not_null")
					}
					f.DoneIsNull = append(f.DoneIsNull, false)
				}
			}
		case "priority":
			for _, whole := range values {
				if whole == "" {
					continue
				}
				op, raw, explicit := entapi.SplitOp(whole)
				if !explicit {
					op = "eq"
				}
				switch op {
				case "eq":
				case "ne":
				case "in":
				case "not_in":
				case "gt":
				case "ge":
				case "lt":
				case "le":
				case "is_null":
				case "not_null":
				case "from":
				case "to":
				case "between":
				default:
					if entapi.KnownQueryOperator(op) {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses operator %q; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, is_null, not_null, from, to, between")
					}
					op, raw = "eq", whole
				}
				switch op {
				case "eq":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.Priority = append(f.Priority, parsed)
				case "ne":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityNEQ = append(f.PriorityNEQ, parsed)
				case "in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTodoPriorityQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.PriorityIn = append(f.PriorityIn, parsedValues)
				case "not_in":
					parts := strings.Split(raw, ",")
					parsedValues := make([]int, 0, len(parts))
					for _, part := range parts {
						parsed, parseErr := parseTodoPriorityQueryValue(part, whole)
						if parseErr != nil {
							return nil, entapi.ListRequest{}, parseErr
						}
						parsedValues = append(parsedValues, parsed)
					}
					f.PriorityNotIn = append(f.PriorityNotIn, parsedValues)
				case "gt":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityGT = append(f.PriorityGT, parsed)
				case "ge":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityGTE = append(f.PriorityGTE, parsed)
				case "lt":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityLT = append(f.PriorityLT, parsed)
				case "le":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityLTE = append(f.PriorityLTE, parsed)
				case "is_null":
					if raw != "" {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses %q with a value; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, is_null, not_null, from, to, between")
					}
					f.PriorityIsNull = append(f.PriorityIsNull, true)
				case "not_null":
					if raw != "" {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses %q with a value; legal operators: %s", entapi.ErrValidation, key, whole, op, "eq, ne, in, not_in, gt, ge, lt, le, is_null, not_null, from, to, between")
					}
					f.PriorityIsNull = append(f.PriorityIsNull, false)
				case "from":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityGTE = append(f.PriorityGTE, parsed)
				case "to":
					parsed, parseErr := parseTodoPriorityQueryValue(raw, whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityLTE = append(f.PriorityLTE, parsed)
				case "between":
					parts := strings.Split(raw, ",")
					if len(parts) != 2 {
						return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q value %q uses between with %d parts; exactly two are required", entapi.ErrValidation, key, whole, len(parts))
					}
					lower, parseErr := parseTodoPriorityQueryValue(parts[0], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					upper, parseErr := parseTodoPriorityQueryValue(parts[1], whole)
					if parseErr != nil {
						return nil, entapi.ListRequest{}, parseErr
					}
					f.PriorityGTE = append(f.PriorityGTE, lower)
					f.PriorityLTE = append(f.PriorityLTE, upper)
				}
			}
		default:
			switch key {
			case "created_at":
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: field %q with value %q is not Filterable", entapi.ErrValidation, key, values)
			default:
				return nil, entapi.ListRequest{}, fmt.Errorf("%w: unknown query field %q with value %q", entapi.ErrValidation, key, values)
			}
		}
	}
	return f, request, nil
}

var todoSortOptions = map[string]func(...sql.OrderTermOption) todo.OrderOption{
	"id":         todo.ByID,
	"title":      todo.ByTitle,
	"priority":   todo.ByPriority,
	"created_at": todo.ByCreatedAt,
}

// TodoSortKeys is the complete sort allow-list. The primary key is
// naturally Sortable; every other key requires api.Sortable().
var TodoSortKeys = []string{
	"id", "title", "priority", "created_at",
}

// TodoOrder is the single sort allow-list seam.
func TodoOrder(r entapi.ListRequest) ([]todo.OrderOption, error) {
	if len(r.Sort) == 0 {
		return []todo.OrderOption{todo.ByID(sql.OrderAsc())}, nil
	}
	order := make([]todo.OrderOption, 0, len(r.Sort)+1)
	idPresent := false
	lastDir := sql.OrderAsc()
	for _, spec := range r.Sort {
		by, ok := todoSortOptions[spec.Key]
		if !ok {
			return nil, fmt.Errorf("%w: cannot sort by %q; legal keys: %s", entapi.ErrValidation, spec.Key, strings.Join(TodoSortKeys, ", "))
		}
		dir := sql.OrderAsc()
		if spec.Desc {
			dir = sql.OrderDesc()
		}
		order = append(order, by(dir))
		lastDir = dir
		if spec.Key == "id" {
			idPresent = true
		}
	}
	if !idPresent {
		order = append(order, todo.ByID(lastDir))
	}
	return order, nil
}

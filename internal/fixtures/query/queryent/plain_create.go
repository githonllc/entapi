// Code generated by ent, DO NOT EDIT.

package queryent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/githonllc/entapi/internal/fixtures/query/queryent/plain"
	"github.com/google/uuid"
)

// PlainCreate is the builder for creating a Plain entity.
type PlainCreate struct {
	config
	mutation *PlainMutation
	hooks    []Hook
}

// SetLabel sets the "label" field.
func (pc *PlainCreate) SetLabel(s string) *PlainCreate {
	pc.mutation.SetLabel(s)
	return pc
}

// SetID sets the "id" field.
func (pc *PlainCreate) SetID(u uuid.UUID) *PlainCreate {
	pc.mutation.SetID(u)
	return pc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (pc *PlainCreate) SetNillableID(u *uuid.UUID) *PlainCreate {
	if u != nil {
		pc.SetID(*u)
	}
	return pc
}

// Mutation returns the PlainMutation object of the builder.
func (pc *PlainCreate) Mutation() *PlainMutation {
	return pc.mutation
}

// Save creates the Plain in the database.
func (pc *PlainCreate) Save(ctx context.Context) (*Plain, error) {
	pc.defaults()
	return withHooks(ctx, pc.sqlSave, pc.mutation, pc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PlainCreate) SaveX(ctx context.Context) *Plain {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PlainCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PlainCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (pc *PlainCreate) defaults() {
	if _, ok := pc.mutation.ID(); !ok {
		v := plain.DefaultID()
		pc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PlainCreate) check() error {
	if _, ok := pc.mutation.Label(); !ok {
		return &ValidationError{Name: "label", err: errors.New(`queryent: missing required field "Plain.label"`)}
	}
	return nil
}

func (pc *PlainCreate) sqlSave(ctx context.Context) (*Plain, error) {
	if err := pc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	pc.mutation.id = &_node.ID
	pc.mutation.done = true
	return _node, nil
}

func (pc *PlainCreate) createSpec() (*Plain, *sqlgraph.CreateSpec) {
	var (
		_node = &Plain{config: pc.config}
		_spec = sqlgraph.NewCreateSpec(plain.Table, sqlgraph.NewFieldSpec(plain.FieldID, field.TypeUUID))
	)
	if id, ok := pc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := pc.mutation.Label(); ok {
		_spec.SetField(plain.FieldLabel, field.TypeString, value)
		_node.Label = value
	}
	return _node, _spec
}

// PlainCreateBulk is the builder for creating many Plain entities in bulk.
type PlainCreateBulk struct {
	config
	err      error
	builders []*PlainCreate
}

// Save creates the Plain entities in the database.
func (pcb *PlainCreateBulk) Save(ctx context.Context) ([]*Plain, error) {
	if pcb.err != nil {
		return nil, pcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Plain, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PlainMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PlainCreateBulk) SaveX(ctx context.Context) []*Plain {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PlainCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PlainCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}

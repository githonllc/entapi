// Code generated by ent, DO NOT EDIT.

package fieldshapesent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/fieldshapesent/namedtypewidget"
	"github.com/githonllc/entdomain/internal/fixtures/fieldshapes/fieldshapesent/predicate"
	"github.com/google/uuid"
)

// NamedTypeWidgetQuery is the builder for querying NamedTypeWidget entities.
type NamedTypeWidgetQuery struct {
	config
	ctx        *QueryContext
	order      []namedtypewidget.OrderOption
	inters     []Interceptor
	predicates []predicate.NamedTypeWidget
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NamedTypeWidgetQuery builder.
func (ntwq *NamedTypeWidgetQuery) Where(ps ...predicate.NamedTypeWidget) *NamedTypeWidgetQuery {
	ntwq.predicates = append(ntwq.predicates, ps...)
	return ntwq
}

// Limit the number of records to be returned by this query.
func (ntwq *NamedTypeWidgetQuery) Limit(limit int) *NamedTypeWidgetQuery {
	ntwq.ctx.Limit = &limit
	return ntwq
}

// Offset to start from.
func (ntwq *NamedTypeWidgetQuery) Offset(offset int) *NamedTypeWidgetQuery {
	ntwq.ctx.Offset = &offset
	return ntwq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ntwq *NamedTypeWidgetQuery) Unique(unique bool) *NamedTypeWidgetQuery {
	ntwq.ctx.Unique = &unique
	return ntwq
}

// Order specifies how the records should be ordered.
func (ntwq *NamedTypeWidgetQuery) Order(o ...namedtypewidget.OrderOption) *NamedTypeWidgetQuery {
	ntwq.order = append(ntwq.order, o...)
	return ntwq
}

// First returns the first NamedTypeWidget entity from the query.
// Returns a *NotFoundError when no NamedTypeWidget was found.
func (ntwq *NamedTypeWidgetQuery) First(ctx context.Context) (*NamedTypeWidget, error) {
	nodes, err := ntwq.Limit(1).All(setContextOp(ctx, ntwq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{namedtypewidget.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) FirstX(ctx context.Context) *NamedTypeWidget {
	node, err := ntwq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NamedTypeWidget ID from the query.
// Returns a *NotFoundError when no NamedTypeWidget ID was found.
func (ntwq *NamedTypeWidgetQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ntwq.Limit(1).IDs(setContextOp(ctx, ntwq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{namedtypewidget.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ntwq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NamedTypeWidget entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NamedTypeWidget entity is found.
// Returns a *NotFoundError when no NamedTypeWidget entities are found.
func (ntwq *NamedTypeWidgetQuery) Only(ctx context.Context) (*NamedTypeWidget, error) {
	nodes, err := ntwq.Limit(2).All(setContextOp(ctx, ntwq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{namedtypewidget.Label}
	default:
		return nil, &NotSingularError{namedtypewidget.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) OnlyX(ctx context.Context) *NamedTypeWidget {
	node, err := ntwq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NamedTypeWidget ID in the query.
// Returns a *NotSingularError when more than one NamedTypeWidget ID is found.
// Returns a *NotFoundError when no entities are found.
func (ntwq *NamedTypeWidgetQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ntwq.Limit(2).IDs(setContextOp(ctx, ntwq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{namedtypewidget.Label}
	default:
		err = &NotSingularError{namedtypewidget.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ntwq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NamedTypeWidgets.
func (ntwq *NamedTypeWidgetQuery) All(ctx context.Context) ([]*NamedTypeWidget, error) {
	ctx = setContextOp(ctx, ntwq.ctx, ent.OpQueryAll)
	if err := ntwq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NamedTypeWidget, *NamedTypeWidgetQuery]()
	return withInterceptors[[]*NamedTypeWidget](ctx, ntwq, qr, ntwq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) AllX(ctx context.Context) []*NamedTypeWidget {
	nodes, err := ntwq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NamedTypeWidget IDs.
func (ntwq *NamedTypeWidgetQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ntwq.ctx.Unique == nil && ntwq.path != nil {
		ntwq.Unique(true)
	}
	ctx = setContextOp(ctx, ntwq.ctx, ent.OpQueryIDs)
	if err = ntwq.Select(namedtypewidget.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ntwq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ntwq *NamedTypeWidgetQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ntwq.ctx, ent.OpQueryCount)
	if err := ntwq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ntwq, querierCount[*NamedTypeWidgetQuery](), ntwq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) CountX(ctx context.Context) int {
	count, err := ntwq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ntwq *NamedTypeWidgetQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ntwq.ctx, ent.OpQueryExist)
	switch _, err := ntwq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("fieldshapesent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ntwq *NamedTypeWidgetQuery) ExistX(ctx context.Context) bool {
	exist, err := ntwq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NamedTypeWidgetQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ntwq *NamedTypeWidgetQuery) Clone() *NamedTypeWidgetQuery {
	if ntwq == nil {
		return nil
	}
	return &NamedTypeWidgetQuery{
		config:     ntwq.config,
		ctx:        ntwq.ctx.Clone(),
		order:      append([]namedtypewidget.OrderOption{}, ntwq.order...),
		inters:     append([]Interceptor{}, ntwq.inters...),
		predicates: append([]predicate.NamedTypeWidget{}, ntwq.predicates...),
		// clone intermediate query.
		sql:  ntwq.sql.Clone(),
		path: ntwq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Labels schema.Tags `json:"labels,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NamedTypeWidget.Query().
//		GroupBy(namedtypewidget.FieldLabels).
//		Aggregate(fieldshapesent.Count()).
//		Scan(ctx, &v)
func (ntwq *NamedTypeWidgetQuery) GroupBy(field string, fields ...string) *NamedTypeWidgetGroupBy {
	ntwq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NamedTypeWidgetGroupBy{build: ntwq}
	grbuild.flds = &ntwq.ctx.Fields
	grbuild.label = namedtypewidget.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Labels schema.Tags `json:"labels,omitempty"`
//	}
//
//	client.NamedTypeWidget.Query().
//		Select(namedtypewidget.FieldLabels).
//		Scan(ctx, &v)
func (ntwq *NamedTypeWidgetQuery) Select(fields ...string) *NamedTypeWidgetSelect {
	ntwq.ctx.Fields = append(ntwq.ctx.Fields, fields...)
	sbuild := &NamedTypeWidgetSelect{NamedTypeWidgetQuery: ntwq}
	sbuild.label = namedtypewidget.Label
	sbuild.flds, sbuild.scan = &ntwq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NamedTypeWidgetSelect configured with the given aggregations.
func (ntwq *NamedTypeWidgetQuery) Aggregate(fns ...AggregateFunc) *NamedTypeWidgetSelect {
	return ntwq.Select().Aggregate(fns...)
}

func (ntwq *NamedTypeWidgetQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ntwq.inters {
		if inter == nil {
			return fmt.Errorf("fieldshapesent: uninitialized interceptor (forgotten import fieldshapesent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ntwq); err != nil {
				return err
			}
		}
	}
	for _, f := range ntwq.ctx.Fields {
		if !namedtypewidget.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("fieldshapesent: invalid field %q for query", f)}
		}
	}
	if ntwq.path != nil {
		prev, err := ntwq.path(ctx)
		if err != nil {
			return err
		}
		ntwq.sql = prev
	}
	return nil
}

func (ntwq *NamedTypeWidgetQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NamedTypeWidget, error) {
	var (
		nodes = []*NamedTypeWidget{}
		_spec = ntwq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NamedTypeWidget).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NamedTypeWidget{config: ntwq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ntwq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ntwq *NamedTypeWidgetQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ntwq.querySpec()
	_spec.Node.Columns = ntwq.ctx.Fields
	if len(ntwq.ctx.Fields) > 0 {
		_spec.Unique = ntwq.ctx.Unique != nil && *ntwq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ntwq.driver, _spec)
}

func (ntwq *NamedTypeWidgetQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(namedtypewidget.Table, namedtypewidget.Columns, sqlgraph.NewFieldSpec(namedtypewidget.FieldID, field.TypeUUID))
	_spec.From = ntwq.sql
	if unique := ntwq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ntwq.path != nil {
		_spec.Unique = true
	}
	if fields := ntwq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, namedtypewidget.FieldID)
		for i := range fields {
			if fields[i] != namedtypewidget.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ntwq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ntwq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ntwq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ntwq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ntwq *NamedTypeWidgetQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ntwq.driver.Dialect())
	t1 := builder.Table(namedtypewidget.Table)
	columns := ntwq.ctx.Fields
	if len(columns) == 0 {
		columns = namedtypewidget.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ntwq.sql != nil {
		selector = ntwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ntwq.ctx.Unique != nil && *ntwq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ntwq.predicates {
		p(selector)
	}
	for _, p := range ntwq.order {
		p(selector)
	}
	if offset := ntwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ntwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NamedTypeWidgetGroupBy is the group-by builder for NamedTypeWidget entities.
type NamedTypeWidgetGroupBy struct {
	selector
	build *NamedTypeWidgetQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ntwgb *NamedTypeWidgetGroupBy) Aggregate(fns ...AggregateFunc) *NamedTypeWidgetGroupBy {
	ntwgb.fns = append(ntwgb.fns, fns...)
	return ntwgb
}

// Scan applies the selector query and scans the result into the given value.
func (ntwgb *NamedTypeWidgetGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ntwgb.build.ctx, ent.OpQueryGroupBy)
	if err := ntwgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NamedTypeWidgetQuery, *NamedTypeWidgetGroupBy](ctx, ntwgb.build, ntwgb, ntwgb.build.inters, v)
}

func (ntwgb *NamedTypeWidgetGroupBy) sqlScan(ctx context.Context, root *NamedTypeWidgetQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ntwgb.fns))
	for _, fn := range ntwgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ntwgb.flds)+len(ntwgb.fns))
		for _, f := range *ntwgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ntwgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ntwgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NamedTypeWidgetSelect is the builder for selecting fields of NamedTypeWidget entities.
type NamedTypeWidgetSelect struct {
	*NamedTypeWidgetQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ntws *NamedTypeWidgetSelect) Aggregate(fns ...AggregateFunc) *NamedTypeWidgetSelect {
	ntws.fns = append(ntws.fns, fns...)
	return ntws
}

// Scan applies the selector query and scans the result into the given value.
func (ntws *NamedTypeWidgetSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ntws.ctx, ent.OpQuerySelect)
	if err := ntws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NamedTypeWidgetQuery, *NamedTypeWidgetSelect](ctx, ntws.NamedTypeWidgetQuery, ntws, ntws.inters, v)
}

func (ntws *NamedTypeWidgetSelect) sqlScan(ctx context.Context, root *NamedTypeWidgetQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ntws.fns))
	for _, fn := range ntws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ntws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ntws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
